# GoJá Delivery — Plano de Migração

> Documento de controle do que **ainda não foi migrado** do sistema legado para o backend/painel moderno (TypeScript + React). Cada item tem etapas e um checkbox `[ ]` para marcarmos `[x]` conforme for realizado.

**Última atualização:** 01/09/2026

---

## Contexto atual (já migrado / rodando)

Backend moderno (`delivery_toop-admin/backend/src`) e painel React (`delivery_toop-admin/frontend-react`) já cobrem:

- **CORE delivery/e-commerce:** auth (login/cadastro store), empresas, categorias, produtos, pedidos, carrinho, banners, review, filiais, upload.
- **Estoque:** StockItem / StockBatch / StockMovement.
- **Mobility (subset):** drivers, bookings (aceitar/rejeitar/contra-oferta/iniciar/completar/cancelar/avaliar/QR), wallet do motorista, mensagens por corrida, promo.
- **Painel React** com: Dashboard, Empresas, Categorias, Banners, Pedidos, Usuários, Entregadores, Motoristas, Corridas, Cupons, Wallet, Pagamentos, Configurações, Relatórios, Perfil.
- **4 apps web:** web-client (cliente+rides), store (loja+estoque), deliveryman-app, landpage.
- **3 microserviços:** payment (8400), notification (8200), deliveryman (8300).
- **OTA server** (`toop-ota`, porta 8500) — server/CLI/wrapper prontos (falta módulo nativo, ver Fase 3).

**O que NÃO roda no compose (legado de referência):**
- `delivery_toop-admin/frontend` (painel **Angular**).
- `delivery_toop-admin/backend/_legacy_src` (backend **JS** legado).
- Apps mobile (4, React Native) — geridos via OTA, não no docker dev.

---

## Passo 1 — Backend: migração das áreas legadas do `_legacy_src`

> Critério: cada área ganha Model + Service + Route + Validators no backend moderno, e o endpoint é consumido pelo painel React.

### 1.1 Finance (módulo financeiro completo)
Adotada abordagem **novo/autônomo** (aprovado pelo usuário): módulo Finance desacoplado do legado, consumindo dados atuais (Orders/Payments/Company), sem depender de Franchise/Braspag. Implementado em `backend/src`: models + `finance.service.ts` + `finance.controller.ts` + `finance.routes.ts` (montado em `routes/index.ts` sob `/finance`).
- [x] Modelos: `CostCenter`, `TypePayment`, `Bank`, `Agency`, `DigitalAccount` (account+extract), `Chargeback` (padrão moderno, `toJSON` sem `__v`, índices).
- [x] Rotas CRUD (autenticadas): `/finance/balances`, `/finance/balances/company/:id`, `/finance/cost-centers`, `/finance/type-payments`, `/finance/banks`, `/finance/agencies`, `/finance/digital-accounts` (+ `/:id/balance`, `/:id/move`), `/finance/chargebacks`.
- [x] Balanço por empresa: agregação real sobre Orders (gross/subtotal/deliveryFees/discounts/paid, `status=delivered`) + `getCompanyBalance` (por método de pagamento + payments received/refunded).
- [ ] Pendente (depende de Fase 1.9 gateways e 1.13 franquia): `Subordinate`/split Braspag, invoices, deliveries finance, saldo por franquia.
- [x] Expor telas Finance no painel React (fazer na Fase 2.3 quando o backend estiver integrado). *(03/09/2026 - página Finance com abas: Balanços, Cost Centers, Type Payments, Bancos, Agências, Contas Digitais, Chargebacks)*

### 1.2 Cashback
- [x] Model `CashbackCampaign` + `CashbackCustomer` (saldo/histórico/total por mês).
- [x] Rotas: `/cashback/campaigns`, `/cashback/used/paginator`, `/cashback/customer/...`.

### 1.3 HelpDesk / FAQ
- [x] Models: `HelpTicket` (protocolo `tickedId`, subject, description, person, company, priority, department, status, name, email, phone, order, images, `deletedAt` soft-delete), `TicketInteraction` (helpTicketsId, description, origin, author), `Faq` (title, caption, description, status). Padrão moderno. `person` referenciado como `User` (model `Person` ainda não migrado — item 1.17).
- [x] Rotas (autenticadas): `/helpdesk/tickets` (list/create), `/helpdesk/tickets/protocol/:protocol`, `/helpdesk/tickets/:id` (get/update/delete), `/helpdesk/tickets/:ticket_id/interactions` (list/create), `/helpdesk/ticketinterations/:id` (update/delete); `/faq` (list/create/get/update/delete).
- [x] Lógica de negócio legada replicada: criar ticket gera interação inicial automaticamente (origin user/company); protocolo busca por `tickedId` + interações.
- [ ] Integrar telas HelpDesk/FAQ no painel React (Fase 2.3).

### 1.4 Email (tipos/templates/variáveis)
- [x] Models `EmailType` (key, name, status, deletedAt), `EmailTemplate` (subject, body, type ref `EmailType`, company ref `Company`, status, deletedAt com `toJSON transform`), `EmailVariable` (name, title, deletedAt).
- [x] Rotas (autenticadas): `/emails/types` (CRUD), `/emails/templates` (CRUD, com `populate` de type), `/emails/variables` (somente leitura como no legado). Fluxo completo validado em runtime: criar type → criar template (type populado) → listar types/templates/variables. `tsc --noEmit` OK.

### 1.5 Marketing / campanhas
- [x] Model `Campaign` (name, disseminationVehicle, initialDate/finalDate, downloadAndroid/downloadIos — corrigidos typos `dowload*` do legado, note, image, soft-delete `deletedAt`).
- [x] Rota `/marketing/campaign` (CRUD autenticado) + `marketing.service/controller/routes`.
- [ ] Integrar telas de campanhas no painel React (Fase 2.3).

### 1.6 Notificações administrativas
- [x] Criar/atualizar/deletar notificação (`POST/PUT/DELETE /notifications/:id`) e **enviar push** por Firebase (`POST /notifications/send` — cria a notificação e envia FCM por tópico; `POST /v2/notification-topic/send`). Envio via `firebase-admin` direto (não proxy p/ 8200). **Degrada com mensagem clara quando não há Service Account** (`FIREBASE_ADMIN_*`).
- [x] Tópicos de push + usuário-por-tópico (`POST /v2/notification-topic` — inscreve/desinscreve token em tópicos `{name}[_{value}]`).

### 1.7 Aplicativos / categorias de app
- [x] Model `AppCategory` (name, type supermarket/restaurant/accessories, showInApp, keyword, segment, status, showHome, order, images, soft-delete `deletedAt`). Desacoplado: `segment` virou string (no legado era ref `CompanySegment`/franchise, inexistente no moderno).
- [x] Rota `/application/category` (CRUD autenticado) + `app.service/controller/routes`.
- [ ] Integrar telas de categorias de app no painel React (Fase 2.3).

### 1.8 Pedidos de supermercado / fluxo shopping completo
- [ ] Models: `Department`, `DepartmentMobile`, `Schedule`, `Invoice`, `PaymentMethod`, `OwnDelivery`.
- [ ] Rotas: `/shopping/*` (departments, cart-item, schedules, invoices, cost-freight, own/online delivery, status/tracking, shipment).
- [ ] Integrar número/supermercado no fluxo do pedido do web-client.

### 1.9 Payment gateways (Braspag / PagarMe / Iugu / PIX)
- [ ] Dependências de SDKs (braspag, pagarme, iugu).
- [ ] Models: `PaymentTransaction`, `Split`, `Chargeback`.
- [ ] Rotas: `/braspag/*`, `/payment/*` (capture, split, PIX charge/verify, card tokenization). Migrar para o microserviço payment (8400) e expor via admin.
- [ ] Subscription/assinatura e repasse (recipient/split).

### 1.10 Cupons / ofertas (hoje só `Promo`)
- [x] Cupons por empresa, por cliente, cupom de destaque/high. *(01/09/2026)*
- [ ] Model `Offer` (oferta por empresa, distinto de Promo). *(ainda pendente — oferta por empresa fica registrada p/ implementação futura)*

### 1.11 Pedido "loose delivery" (delivery avulso)
- [x] Model + rotas `/loose-delivery` (+ address). *(01/09/2026 — fluxo adaptado ao domínio moderno de `Order`/`DeliveryAddress`; sem geocodificação externa nem customer/person fixos do legado)*

### 1.12 Embalagem / Packing e Shopper
- [x] Model `Packing`. *(01/09/2026)*
- [x] Model `Shopper` + atribuição a itens de carrinho. *(01/09/2026 — CRUD completo; atribuição a itens de carrinho fica registrada p/ 1.8 fluxo shopping)*

### 1.13 Franquia / Franchise
- [x] Model `Franchise` + config genérica. *(01/09/2026 — sub-schemas bankData/location/settingsDrive/settingsRace embutidos; state/city refs SettingState/SettingCity mantidos como ObjectId (models 1.19 pendentes); desacopladas integrações Pagar.me split, geocode Google e criação automática de conta digital (registradas p/ 1.9/1.13))*
- [x] Rotas `/franchises` + self-registration público. *(01/09/2026 — CRUD + paginator/search/list-all + `/public` sem auth + `/config/:company`)*

### 1.14 Pré-registro / dinâmico
- [x] Model `PreRegistration` (collection `preRegistration`, campos como ddi/phone/name/email/cpf/nif/status PENDENTE·APPROVED·ANALYZE·DECLINED·RESENT, fotos/documentos, vehicle*, bankData embutido, viewStop/NextRegister, soft-delete deletedAt) + `DynamicPreRegister` (collection `dynamicPreRegister`, schemas embutidos `InputTypeShema`/`UploadDocPhotoShema`). Padrão moderno. Desacoplado: ref `franchise`/`application` não resolvidos (models ainda não migrados).
- [x] Rotas `/pre-register` (PÚBLICAS como no legado: create/list-by-phone/update/delete, `/dynamic`, `/dynamic-record/:id`; `/paginator` autenticado). Fluxo validado em runtime.

### 1.15 Monitor (dashboards tempo real)
- [x] Rotas `/monitor/order`, `/monitor/sales` (pedidos e vendas em tempo real). *(01/09/2026 — ver log)*

### 1.16 Tooling: Popup / Integrações / compressão
- [x] Model `Popup` (collection `popup`: images, name, company ref `Company`, status, startDate/endDate, priorities, vizualizations limite, quantityViews contador, message, textMessageButton, width/height, url, redirectTo enum HOME/URL/ROUTE, startHour/endHour, soft-delete) + `PopupView` (collection `popupView`) + rotas `/tools/popup` (CRUD+updateViews+listPopupApp). Lógica de visibilidade (data/hora/prioridade/limite de views/não-visto) implementada e validada em runtime.
- [x] Model `Integration` (collection `int_integrations`: company ref `Company`, system enum JM_Diamante/RpInfo/Viva_Sistemas, status, soft-delete) + rotas `/tools/integrations` (CRUD+paginator+por-empresa). Validado em runtime.
- [ ] Serviço de compressão de imagem: ADIADO — depende de infraestrutura externa (Bull/Redis, DigitalOcean Spaces/S3, sharp, multer-s3) NÃO presente no backend moderno; e `/food/compress-image` e `/food/change-folder` (legado) dependem de S3. Documentado como pendente de infra.
- [ ] Sync de imagem (`syncImage`): ADIADO — depende de API externa `PRODUCT_IMAGE_API` com tokens hardcoded no legado (segredo). Documentado como pendente.

### 1.17 Cliente / DeliveryAddress / Guest / Person
- [x] Model `DeliveryAddress` + rotas (salvar/editar endereços do cliente). *(01/09/2026 — coleção `customer_delivery_address`; `customer` ref `User` (cliente moderno); `location` 2dsphere obrigatório (lat/lng); `main` único por cliente com propagação automática em create/update; rota `/delivery-address/*` com list por cliente, search (customer/main), find, create/update/delete (soft `isDeleted`). `newTopic` de notificações por cidade desacoplado (item 1.35 pendente))*
- [x] Model `Guest`. *(01/09/2026 — coleção `guest`; rotas públicas `/guest` (POST/PUT upsert por `device`, GET por device) espelhando legado sem auth)*
- [x] Model `Person` (identidade física + indicação) separado de User. *(01/09/2026 — coleção `person` (mesma do legado); refs franchise/company/city(SettingCity); rota `/person/*` com CRUD, paginator (pageIn/pageOut + nome/cpf), listPorNome, search (phone/email/id), avatar; status normalizado (string vazia/null → false); `referralCode` validado no update, criação de `Indication` adiada (model não migrado))*
- [x] Duplicados (`DuplicateRecords`) e normalização. *(01/09/2026 — `/person/register-duplicates?type=person&field=phone|email` via aggregate; branch `customer` não aplicável — modelo `Customer` não migrado (registrado)*

### 1.18 ACL / Permissões / Roles / AccessGroup / AccessFlow
- [x] Models `Role`, `Permission`, `Module`, `AccessGroup`, `AccessFlow`. *(01/09/2026 — coleções `acl_roles`/`acl_permissions`/`settingModule`/`settingController`/`acessGroup`/`accessFlow`; + `Group`)*
- [x] Rotas `/acl/*`, `/accessGroup`, `/report/access-flow`, `/group`. *(01/09/2026 — ver log)*
- [x] Controle por permissão no painel React (aplicar no frontend-react as guards por role). *(01/09/2026 — página `/acl` só aparece para admin/manager; ver log)*

### 1.19 Configurações de domínio (hoje `Settings` é um doc único)
- [x] Models: `SettingState` (settingState: name/uf/country), `SettingCity` (settingCity: name, state ref, deletedAt, lat/lng), `TimeZone` (timeZone: offset/zone/name), `SettingTypesUsers` (settingTypesUsers: name/status/deletedAt), `AppVersion` (appVersion: name/version/platform/status/deletedAt), `SettingBrazilianBanks` (settingBrazilianBanks), `GlobalSettings` (globalSettings: serviceCharge). `Controller`/`Module` já portados na 1.18 (SettingController/SettingModule). *(03/09/2026 — ver log)*
- [x] Rotas `/setting/*`, `/v2/setting/*`. *(03/09/2026 — ver log)*

### 1.20 Catálogo de acessórios (loja não-food) e complementos de produto
- [x] Models `AccessoriesCategory` (accessoriesCategory), `AccessoriesProduct` (accessoriesProduct), `AccessoriesProductComplement` (accessoriesProductComplement), `AccessoriesProductComplementItem` (accessoriesProductComplementItem). *(03/09/2026 — ver log)*
- [x] Rotas `/v2/accessories/*`, `/food/product-complement*` (com auth). *(03/09/2026 — ver log)*
- [x] Ordenação (position + sort) / status (isPaused) / desconto de produto (pricePromotion, percentualDiscount) + list-group por categoria (appVersion). *(03/09/2026 — ver log)*

### 1.21 Prompt de produto / Alerta de produto
- [x] Model `CustomerAlertProduct` + rotas `/v2/customer-alert-product/alert-product` (list/create/update/report). *(03/09/2026 — ver log)*

### 1.22 Chat por pedido (hoje só `Message` por corrida)
- [x] Model `ChatMessage` + rotas `/v1/front/chat/:cartId` (list aggregate + create) e `/chat` (list, no-read, create, read). *(03/09/2026 — ver log)*

### 1.23 Entregador: fila / corridas / histórico / online
- [x] Models `QueueDeliveryMan`, `DeliveryManOnline`, `RaceCanceled`, `RaceHistory` + rotas `/deliveryMan/*`: queue, back-to-queue, race/list, online-last-week, price. *(03/09/2026 — ver log)*
- [x] Aprovação de cadastro de entregador/motorista (`/register-deliveryman`; `/pre-register` já coberto pelo módulo moderno). *(03/09/2026 — ver log)*

### 1.24 Busca
- [x] Rota `/v1/search` (`/company-products`, `/segment/company-products`). *(03/09/2026 — ver log)*

### 1.25 Slider / Tabloid / Tips / site da empresa
- [x] Models `Slider`, `Tabloid`, `Tip`, `TipDeliveryMan`, `CompanySite` (Site). *(03/09/2026 — ver log)*
- [x] Rotas `/slider/*`, `/tabloid/*`, `/Tip/*`, `/TipDeliveryMan/*`, `/company/site/*`. *(03/09/2026 — ver log)*

### 1.26 Log / auditoria
- [x] Model `Log` + rota `/log` (auditoria de ações no painel). *(03/09/2026 — ver log)*

### 1.27 Mobility — lacunas
- [x] Maps (directions/matrix/geocode/autocomplete) — `v1/mobility/maps/*`. *(03/09/2026 — ver log)*
- [x] Services (tipos de corrida) + `service-details`. *(03/09/2026 — ver log)*
- [x] Passengers CRUD, DocumentType, VehicleDocuments, PeakHour, SupportSubject. *(03/09/2026 — ver log)*
- [x] Push/topics de mobilidade, favoritePlaces, chosenDestinations, indication, QR driver, slider mobility, agenda/agendamentos, extract, discount paginator, report adm (races/passenger/driver). *(03/09/2026 — ver log)*

### 1.28 Wallet (admin) — vouchers / recarga
- [x] `/v2/vouchers` (CRUD + validate) + recarga por voucher (hoje só balance/credit/debit manual). *(03/09/2026 — ver log)*

### 1.29 Catálogo ECBR / Image Bank
- [x] `/v2/ecbr-image-bank` (product-bank, generate-code, sync, barcode) e `/imageBank`. *(03/09/2026 — ver log)*

### 1.30 Endereço / Company público
- [x] `/v2/company/register-company`, `/v2/company/location`, `/v2/address`. *(03/09/2026 — ver log)*

### 1.31 Twilio (verificação de número)
- [x] `/twilio` (alerta por chamada + verificação OTP por Verify, sem microserviço). *(03/09/2026 — ver log)*

---

## Passo 2 — Painel Admin React: telas a criar/ativar

> O painel React (`frontend-react`) já tem boa base. Faltam telas e correções.

### 2.1 Ativar telas existentes mas fora da rota
- [x] **Products.jsx** — adicionar rota `/products` + link no sidebar (hoje é código morto não-roteado).
- [x] **Painel.jsx** — adicionar rota/sidebar "Painel de Pedidos" (hoje existe mas inacessível).

### 2.2 Correções de consistência
- [ ] `payments` não é usado — o cálculo client-side de `Payments.jsx` usa dados reais (pedidos entregues + fees do settings); o endpoint `/payments` (model `Payment`) é de transações de pagamento, semântica diferente. A migração do repasse/comissão para server-side fica registrada na **Fase 1.9** (Payment/repasse), não quebrando a tela atual. *(NÃO forçado agora — verificado e documentado.)*
- [x] Unificar serviços: `Categories`, `Banners` e `Products` usavam `api.get/delete` direto — refatorados para `categoryService`, `bannerService`, `productService`.
- [x] Verificado: `promo` singular **está correto** no backend (`/promo` — montado em `routes/index.ts`); `promoService` do frontend já usa `/promo`. Contrato `wallet` confirmado: `GET /wallet/balance` e `/wallet/transactions` aceitam `driverId` via **query param**, igual ao frontend. Nenhum ajuste necessário.
- [x] `Profile.jsx` — "último acesso" real: backend agora grava `lastLogin` no login (`auth.service.ts`) e o Profile busca dados atualizados via `GET /auth/me` (novo `authService.me` + `refreshUser` no AuthContext), mostrando `current.lastLogin` (fallback `—`) em vez de `new Date()`.

### 2.3 Novas telas por módulo migrado (criar quando o backend da fase 1 estiver pronto)
- [x] Finanças: Digital Accounts (bancos/agências/contas), Extrato, Cost Centers, Type Payments, Chargeback. *(03/09/2026 - ver log; Subordinados e Invoices ficam p/ Fase 1.9)*
- [x] Cashback: campanhas, recebidos, histórico. *(03/09/2026 - página Cashback.jsx com abas: Campanhas / Cashback Utilizado)*
- [x] HelpDesk/FAQ: tickets + interações, FAQ. *(03/09/2026 - página HelpDesk.jsx com abas Tickets/FAQ; form com protocolo tickedId, interações por protocolo)*
- [x] Email: tipos, templates, variáveis. *(03/09/2026 - página Email.jsx com abas Tipos/Templates/Variáveis; campos alinhados ao model: key/name e subject/body/type)*
- [x] Marketing: campanhas. *(03/09/2026 - página Marketing.jsx; campos alinhados ao model Campaign: name/disseminationVehicle/initialDate/finalDate/downloads/note/image)*
- [x] Supermercado: tabloid + ECBR Image Bank. *(04/09/2026 - página Supermarket.jsx com abas Tabloid (create/list/delete) e ECBR (list por código/nome, create com file.url, update); schedules/invoices/loose-delivery não possuem CRUDs dedicados no painel - ver log 04/09)*
- [x] Packing, Shopper, Franquias. *(03/09/2026 - páginas Packings.jsx, Shoppers.jsx, Franchises.jsx já ativas nas rotas/sidebar)*
- [x] Pré-registro entrega/motorista + aprovação. *(04/09/2026 - página PreRegister.jsx com abas Pré-registro (paginator por status/busca; Aprovar APPROVED / Recusar DECLINED / Reenviar RESENT via PUT /pre-register/:id) e Cadastro Entregador (paginator + update status)*
- [x] NOC/Monitor (pedidos/vendas tempo real). *(03/09/2026 - página Monitor.jsx aba NOC: GET /monitor/order e /monitor/sales)*
- [x] Tools: Popup, Integrations, compressão de imagem. *(03/09/2026 - página Monitor.jsx aba Tools com painéis Popups e Integrações)*
- [x] Configurações de domínio: cidades, estados, timezones, types-users, app-version, bancos. *(03/09/2026 - página DomainSettings.jsx com abas Estados/Cidades/Tipos de Usuário/Versões App/Bancos/Fusos/Config Global)*
- [x] ACL: roles, permissões, módulos, grupos de acesso, access-flow. *(03/09/2026 - página AccessControl.jsx ativa na rota /acl, visível p/ admin/manager)*
- [x] Acessórios (loja não-food) + complementos de produto. *(04/09/2026 - página Accessories.jsx com seletor de empresa; abas Categorias (CRUD) e Produtos (CRUD com categoria/pessoas/preço/imagens) + modal de Complementos por produto (create)*
- [x] Mobility: services, passengers, schedules, vehicles, documents, peak hours, push, reports, maps. *(03/09/2026 - página Mobility.jsx com tipodoc/peakhour/supportsubject; demais subcategorias p/ log de Mobility)*
- [x] Wallet admin: vouchers, recarga. *(03/09/2026 - página Vouchers.jsx CRUD por franquia; recarga já coberta na tela Wallet existente)*
- [x] Chat por pedido (supervisão). *(03/09/2026 - página Monitor.jsx aba Chat: GET/POST /v1/front/chat/:cartId + não-lidas)*
- [x] Log/auditoria. *(03/09/2026 - página Log.jsx leitura via /log/paginator)*
- [x] Busca de empresa/produtos. *(04/09/2026 - página Search.jsx: form searchText+companyType → GET /v1/search/company-products; exibe empresas com produtos aninhados)*

---

## Passo 3 — Apps Mobile (React Native) + OTA

### 3.1 OTA end-to-end
- [ ] Implementar módulo nativo **`OTAStorage`** (Android + iOS) de `saveBundle`/`loadBundle` (hoje só existe o wrapper JS + server; sem nativo o bundle não é aplicado).
- [ ] Resolver conflito de porta 8500 (OTA vs `deliveryman-microservice/docker-compose.yml`).
- [ ] Publicar um bundle real de teste ponta a ponta (storage só tem arquivos de teste).

### 3.2 Alinhamento de versões/stack
- [ ] Deliveryman app: RN 0.62/React 16 é antigo — avaliar upgrade (client/driver já em RN 0.65; shopper em 0.64).
- [ ] Shopper: remover `routes/shopperStack.routes.tsx` legado e dependência `react-navigation@4` (dead code).
- [ ] Alinhar versões do SDK Firebase entre apps (v11 → v17).

### 3.3 Migrar apps mobile para o backend moderno
- [ ] Confirmar/adicionar endpoints modernos consumidos por cada app (client/deliveryman/driver/shopper) conforme módulos da Fase 1.
- [ ] Migrar login/cadastro mobile para o fluxo `auth` moderno.

---

## Passo 4 — Integrações (ERP / terceiros)

### 4.1 Diretório `delivery_toop-integrations` está vazio
- [ ] Definir/migrar serviços de integração (Pratiko, Viva Sistemas, RPInfo) — hoje só existem como código legado.
- [ ] Rota `/tools/integrations` + sync de imagem + `v1/all/unsynchronized-departments`.

### 4.2 Gateway de integrações
- [ ] Definir porta/rota do gateway de integrações (`apiIntegrationsURL`) e consumo pelos microserviços.

---

## Passo 5 — Produção / Deploy / Segurança

### 5.1 Segurança (pendências do README — validar se já resolvidas)
- [ ] (Confirmado) Chaves `.pem`/`.key` **NÃO** estão no git — manter `.gitignore` bloqueando; auditar de novo antes de qualquer push.
- [ ] Revogar service account Firebase `food-syulnv` no Google Cloud Console (se ainda ativa).
- [ ] Confirmar nenhuma chave Firebase/adminsdk commitada no histórico (limpar com BFG se houver).
- [ ] Gerar `.env` de produção com senhas fortes (existe `.env.production.example`).

### 5.2 CI/CD
- [ ] Revisar `.github/workflows/ci.yml` e `ci-cd.yml` — confirmar que builds/testes rodam para os 4 apps web + backend + painel.
- [ ] Definir pipeline de build/assemble dos apps mobile (Android/iOS) e publicação via OTA.

### 5.3 Testes
- [ ] Rodar e cobrir testes do backend (`delivery_toop-admin/backend/__tests__`).
- [ ] Adicionar testes para módulos migrados da Fase 1.
- [ ] Adicionar testes de UI no painel React (hoje apenas unitários de backend).

### 5.4 Monitoramento
- [ ] Confirmar dashboards Grafana/Prometheus provisionados refletindo os novos módulos.

---

## Registro de progresso

| Data | Itens concluídos |
|------|------------------|
| 28/08/2026 | Documento criado partindo do mapeamento completo. Nenhum item de migração ainda concluído. |
| 28/08/2026 | Fase 2.1 — ativadas as telas do painel React: `Products` (rota `/products`) e `Painel de Pedidos` (rota `/painel`), com links no sidebar. Build vite OK. |
| 28/08/2026 | Fase 2.2 — unificados services em Categories/Banners/Products; "último acesso" real no Profile (backend grava `lastLogin` + `authService.me`/`refreshUser`); verificados contratos `promo` (singular, correto) e `wallet` (query driverId). Item `payments` documentado para Fase 1.9. Builds vite + tsc OK. |
| 28/08/2026 | Fase 1.1 Finance (abordagem novo/autônomo aprovada) — criados models CostCenter/TypePayment/Bank/Agency/DigitalAccount(+extract)/Chargeback, `finance.service`/`controller`/`routes` montados sob `/finance`. Endpoints validados em runtime: `/finance/balances` (agregação com dados reais) e CRUD cost-centers com auth. `tsc --noEmit` OK, admin-api reiniciado. Subordinate/split Braspag/invoices/franquia ficam p/ Fases 1.9/1.13. |
| 28/08/2026 | Fase 1.3 HelpDesk/FAQ — criados models `HelpTicket`/`TicketInteraction`/`Faq` + `helpdesk.service`/`controller`/`routes` (`/helpdesk/*`, `/faq`). Criar ticket gera interação inicial (lógica legada); consulta por protocolo retorna ticket+interações. Fluxo validado em runtime com auth (criar ticket, buscar por protocolo, listar interações). `tsc --noEmit` OK. |
| 28/08/2026 | Fase 1.5 Marketing + 1.7 Aplicativos — criados models `Campaign` e `AppCategory` + `marketing.service`/`app.service`/`controllers`/`routes` (`/marketing/campaign`, `/application/category`), CRUD autenticado com soft-delete `deletedAt`. Desacoplados do legado (`segment` string no lugar de ref `CompanySegment`/franchise; typos `dowload*` corrigidos). Criar+listar validados em runtime. `tsc --noEmit` OK. |
| 28/08/2026 | Fase 1.4 Email — criados models `EmailType`/`EmailTemplate`/`EmailVariable` + `email.service`/`controller`/`routes` (`/emails/types`, `/emails/templates`, `/emails/variables`) montados em `routes/index.ts`. CRUD autenticado; template com `populate` de type; variables somente leitura (comportamento legado). Fluxo validado em runtime: criar type + template + listar. `tsc --noEmit` OK. Registros de teste removidos. |
| 29/08/2026 | Fase 1.14 Pré-registro + 1.16 Tooling — criados models PreRegistration/DynamicPreRegister (com InputTypeShema/UploadDocPhotoShema), Popup/PopupView/Integration + services/controllers/routes (`/pre-register`, `/tools/popup`, `/tools/integrations`) montados em routes/index.ts. Fluxos validados em runtime: pré-registro público (create/list/dynamic/listViews), popup CRUD+visibility (updateViews incrementa quantityViews, listPopupApp retorna popup vigente não-visto) e integrações. Compressão de imagem e syncImage documentados como adiados (dependem de S3/Bull/API externa). `tsc --noEmit` OK. |
| 01/09/2026 | Fase 1.2 Cashback (piloto completo backend + tela painel) — criados models `CashbackCampaign`/`CashbackCustomer`/`CashbackCustomerBalance` (dessacoplados: `companies` ref Company + `allApp`, sem Franquia) + `cashback.service`/`controller`/`routes` montados sob `/cashback` (CRUD campaigns + list/balance/byMonth por customer + `used/paginator` com lookups User/Order/Campaign). CreateDefine `balance=amount`. Fluxo validado em runtime (login admin → criar campanha → listar → paginator). `tsc --noEmit` OK, admin-api reiniciado. Painel React: `cashbackService` + página `Cashbacks.jsx` (abas Campanhas / Cashback Utilizado) + rota `/cashback` + item no sidebar. `vite build` OK, frontend-react reiniciado. |
| 01/09/2026 | Fase 1.10 Cupons Compráveis (backend + tela painel) — criados models `Coupon`/`CompanyCoupon`/`CouponCustomer` (coleções `coupon`/`company_coupon`/`coupon_customer`; distinto do Promo: cupom comprável com `price`/`discountPercentage`/`limit`/`onlyFirstPurchase`/`minPriceDelivery`/datas/`allCompanies`) + `coupon.service`/`controller`/`routes` montados sob `/coupon` (CRUD + display + `highCupon` sort `-price` + `companyCoupons` aggregate com lookup + `couponCustomer`/`couponCustomerPaginator` com lookups). Removido `.populate('couponCompany')` do `highCupon` (virtual inexistente). Fluxo validado em runtime: criar 2 cupons (allCompanies e por empresa) → `paginator`/`highCupon`/`companyCoupons`/`display`/`coupon-customer-paginator` OK. `tsc --noEmit` OK. Painel React: `couponService` + página `Coupons.jsx` (abas Cupons / Utilizados) + rota `/coupons` + item sidebar "Cupons Compráveis". `vite build` OK. Model `Offer` (oferta por empresa) permanece pendente. |
| 01/09/2026 | Fase 1.12 Embalagem/Packing + Shopper (backend + tela painel) — criados models `Packing` (coleção `packing`, name/status, soft-delete) e `Shopper` (coleção `shopper`, isOnline/person ref User/company ref Company/device/token/status/appVersion, soft-delete) + `packing.service`/`shopper.service`/`controllers`/`routes` montados sob `/packing` e `/shopper` (CRUD + listByName/search com filtros). Fluxo validado em runtime: packing create/list/update/delete OK; shopper create (populates company+person)/list/paginator/search/update/delete (soft) OK. `tsc --noEmit` OK. Painel React: `packingService` + `shopperService` + páginas `Packings.jsx` (`/packings`) e `Shoppers.jsx` (`/shoppers`, com selects de empresa e usuário) + items no sidebar. `vite build` OK. Atribuição de shopper a itens de carrinho fica registrada p/ 1.8 fluxo shopping. |
| 01/09/2026 | Fase 1.13 Franquia/Franchise (backend + tela painel) — criado model `Franchise` (coleção `franchise`, sub-schemas embutidos bankData/location/settingsRace/settingsDriver/showPhoneRace/routeSettings; state/city refs SettingState/SettingCity mantidos como ObjectId) + `franchise.service`/`controller`/`routes` montados sob `/franchises` (CRUD + paginator com lookups settingCity/settingState + search + list-all + `POST /public` self-registration sem auth + `GET /config/:company`). Desacopladas integrações externas: split Pagar.me (recipient), geocodificação Google (location aceita do body) e criação automática de conta digital (registradas p/ 1.9/1.13). Fluxo validado em runtime: create (valida email duplicado) → list/paginator/search/update/delete(soft) OK; config e public-register OK; dados de teste soft-deletados. `tsc --noEmit` OK. Painel React: `franchiseService` + página `Franchises.jsx` (`/franchises`, modal-lg novo no CSS) + item no sidebar. `vite build` OK. Subordinado/split Braspag e saldo por franquia permanecem p/ 1.9. |
| 01/09/2026 | Fase 1.17 Cliente/DeliveryAddress/Guest/Person (backend) — criados models `Person` (coleção `person`, refs Franchise/Company/SettingCity; `referralCode` único sparse), `DeliveryAddress` (coleção `customer_delivery_address`, `location` Point 2dsphere, `customer` ref `User`, `main` + `isDeleted`) e `Guest` (coleção `guest`, `device` único). Services/controllers/routes: `/person/*` (CRUD, paginator pageIn/pageOut + nome/cpf, listPorNome, search phone/email/id, avatar, register-duplicates via aggregate, status "" → false, ddi decode, validação de `referralCode` no update — Indication adiada), `/delivery-address/*` (list por cliente, search customer/main, find, create/update exigem lat/lng e propagam `main` único, delete soft `isDeleted`; newTopic de cidade desacoplado), `/guest` público (create/get/update upsert por device, sem auth como no legado). Fluxo validado em runtime p/ os três módulos (criação → listas/update/delete/duplicados/edge-cases) e `tsc --noEmit` OK. Dados de teste removidos (soft-delete + limpeza direta do guest). `Customer` e `Indication` não migrados permanecem pendentes. |
| 01/09/2026 | Fase 1.11 Pedido loose-delivery (backend) — criado módulo `/loose-delivery` (POST create + GET address) sobre o domínio moderno. `create` valida empresa (CompanyModel), latitude/longitude, total e frete; persiste um `DeliveryAddress` (customer = usuário autenticado) e cria um `Order` do tipo "Entrega avulsa" (item genérico, `deliveryAddress` embutido, `paymentStatus: pending`, `typeOfVehicle` opcional validado contra MOTO/CARRO/BICICLETA — campo novo no schema de `Order`). Desacoplado do legado: sem customer/person fixos `60df17...` (usa o usuário autenticado), sem geocodificação Google Maps (`/address` valida coordenadas e explica que geocoding externo não está configurado — rotulado pendente), sem despacho automático por `/order/status` externo (entrega avulsa inicia `pending`; despacho p/ 1.23). Fluxo validado em runtime: validações de campos, criação de endereço+pedido, persistência de `typeOfVehicle`, ordem listável em `/orders`. `tsc --noEmit` OK; dados de teste removidos via mongosh. |
| 01/09/2026 | Fase 1.18 ACL / Permissões / Roles / AccessGroup / AccessFlow (backend + tela painel) — criados models `Role` (acl_roles), `Permission` (acl_permissions, ref Role), `SettingModule` (settingModule), `SettingController` (settingController, ref Module), `AccessGroup` (acessGroup, ref Module), `AccessFlow` (accessFlow) e `Group` (group) + `acl.service`/`access-group.service`/`access-flow.service`/`group.service` + controllers/routes montados em routes/index.ts: `/acl/roles` (CRUD + paginator + listPorNome), `/acl/permissions` (CRUD + paginator + list com populate roles), `/acl/users` (deriva roles/permissions do `role` moderno do User — sem isRoot/franchises/person do legado), `/settings/modules` e `/settings/controllers` (CRUD p/ árvore do AccessGroup), `/access-group` (tree com populate modules + CRUD), `/report/access-flow` (create PÚBLICO com upsert diário por device/customer/person + list agrupado por dia + statistic por `timeInterval`), `/group` (CRUD + paginator + listPorNome, delete soft `deletedAt`). Validações explícitas de campos obrigatórios retornando 400 (não 500). Fluxo validado em runtime: create/update/list/paginator/search de roles, permissions (com populate roles), modules/controllers, access-group tree, access-flow público (create + upsert + list + statistic) e group; validação de campos obrigatórios → 400; dados de teste removidos via mongosh. `tsc --noEmit` OK, admin-api reiniciado. Painel React: `aclService` + `accessGroupService` em `api.js` + página `AccessControl.jsx` (`/acl`) com abas Roles/Permissões/Grupos + item no sidebar `adminOnly` (guard por role: só admin/manager vê), espelhando o controle por permissão do legado. `vite build` OK. |
| 01/09/2026 | Fase 1.15 Monitor (dashboards tempo real, backend) — criado `monitor.service`/`controller`/`routes` montado em `/monitor`: `GET /monitor/order` (lista pedidos em tempo real sobre a coleção `orders` moderna — default filtra status ativos `$nin [delivered,cancelled]`, filtro por `status`, lookup `users` (customer, project name/email) + `companies` (name/images), paginação com lookups preservando nulls, retorna `{list,total}`), `GET /monitor/order/:orderId` (detalhe com populate company/customer/deliveryman→person; valida ObjectId e retorna 400 "Informe um pedido válido"), `GET /monitor/sales` (vendas das últimas 24h agrupadas por hora: total + finished por status, default `delivered`, escopo por empresa via `dataDay`/`status`; 24 buckets). Escopo por empresa do usuário logado (root/admin/manager sem escopo; demais restringe ao `company` do User). Desacoplado do legado: sem `customer`/`payment`/`customerDelivery`/`shopper`/`QueueDeliveryMan` (coleções legadas inexistentes no moderno — `customer` virou lookup `users`, `payment`/endereço são embutidos no `Order`, n/a fila de entregadores p/ 1.23). Fluxo validado em runtime: list default (2 pending, exclui delivered/cancelled), filtro por status, detail com populates, bad id → 400, sales 24 buckets; dados de teste removidos via mongosh. `tsc --noEmit` OK, admin-api reiniciado. |
| 02/09/2026 | Fase 1.6 Notificações + push Firebase (backend + painel; config das credenciais). Config do Firebase Web SDK salvo na raiz (`firebase.txt` — GoJaDelivery, `enduring-honor-419212`, nº `52044825836`). Backend: novas envs em `config/index.ts` (web: `FIREBASE_API_KEY`/`AUTH_DOMAIN`/`PROJECT_ID`/`STORAGE_BUCKET`/`MESSAGING_SENDER_ID`/`APP_ID`/`DATABASE_URL`; admin p/ Service Account: `FIREBASE_ADMIN_ENABLED`/`PROJECT_ID`/`CLIENT_EMAIL`/`PRIVATE_KEY`/`DATABASE_URL`) + `config/firebase.config.ts` (referência tipada da config web + flag `isFirebaseAdminEnabled`) + `services/firebaseAdmin.service.ts` (inicializa o app do Firebase Admin a partir do Service Account, **degradando graciosamente para null quando não há chave**) + `services/notification-topic.service.ts` (create — inscreve/desinscreve token em tópicos `{name}[_{value}]`, e send — push por condição `'{topic}' in topics && application_root && franchise_{}`). `notification.service` ganhou create/update/remove/createAndSend; rotas: `/notifications` (POST create, GET list, GET/:id, PUT/:id, DELETE/:id) + `POST /notifications/send` + `POST /v2/notification-topic` e `POST /v2/notification-topic/send` (autenticadas). Envio direto via `firebase-admin` (não proxy p/ microserviço 8200). Instalado `firebase-admin@11` no backend. Fluxo validado em runtime (sem Service Account): create/update/delete/list de notificações OK; `send`/`createAndSend`/topic degradam com `{success:false, error:"FCM não configurado: adicione um Service Account em FIREBASE_ADMIN_*"}` e `createAndSend` ainda persiste a notificação; dados de teste removidos. Painel React: instalado `firebase@10` web SDK + `src/services/firebase.js` (init a partir de `VITE_FIREBASE_*`, `requestPushToken` e `onForegroundMessage` com degrade) + `.env.example` do frontend documenta a config web + VAPID; `notificationService` estendido (create/update/delete/createAndSend/subscribeTopic/sendToTopic). `vite build` e `tsc --noEmit` OK. Pendente: Service Account (Console Firebase → Project settings → Service accounts) para envio real; VAPID key; tela dedicada de criar/enviar notificação no painel. |
| 03/09/2026 | Fase 1.19 Configurações de domínio (backend) — criados models SettingState (settingState: name/uf/country), SettingCity (settingCity: name, state ref SettingState, deletedAt, lat/lng), TimeZone (timeZone: offset -14..13/zone/name), SettingTypesUsers (settingTypesUsers: name/status/deletedAt), AppVersion (appVersion: name/version/platform ios|android/status/deletedAt), SettingBrazilianBanks (settingBrazilianBanks: compe/ispb/document/long_name/short_name/network/type/pix_type/url), GlobalSettings (globalSettings: serviceCharge) e AddressCity (city_br — catálogo IBGE p/ normalizar lat/lng) + domain-settings.service/controller/outes. Rotas autenticadas montadas em routes/index.ts: /setting/state (list/:id, listPorNome, create, update, delete hard), /setting/city (list por state/name/hasFranchise com populate state + franchise, paginator pageIn/pageOut, normalize-cities a partir do city_br quando disponível, create/update com populate, delete soft deletedAt), /setting/types-users (list + paginator + create/update com normalização de status vazio→false + delete soft), /setting/app-versions (list, create com único ativo por plataforma, check → forceUpdate {forceUpdate,version,platform}), /setting/timezone (list sort offset), /setting/countries (estático Brasil/Portugal/Angola com reordenação por language pt/pt-PT/pt-AO), /setting/app/:franchise (config do app da franquia: serviceDefault/languageDefault/coin/emergencyPhone sobre a coleção moderne ranchise), /v2/setting/brazilian-bank (list com filtro por nome, projeta compe/long_name/short_name) e /global/settings (list do doc único globalSettings). Controller/Module já portados na 1.18. Fluxo validado em runtime: state create/list → city create (populate state)/list/paginator → types-users create (status normalizado)/list → app-version create + check (forceUpdate true) → timezone list, countries (default + pt reordena), brazilian-bank list, app/:franchise com franquia real (delivery/R$/pt-BR/190), global/settings; dados de teste removidos via mongosh. 	sc --noEmit OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.20 Cat�logo de acess�rios + complementos (backend) � criados models AccessoriesCategory (accessoriesCategory: name/company/isPaused/position), AccessoriesProduct (accessoriesProduct: images/name/category ref/description/shortDescription/price/pricePromotion/percentualDiscount/codPdv/isPaused/position/amountPeople ONE|TWO|THREE|FOUR, text index p/ busca), AccessoriesProductComplement (accessoriesProductComplement: name/amountMax/amountMin/isRequired/isQuantified/product ref/isPaused/position/company) e AccessoriesProductComplementItem (accessoriesProductComplementItem: name/codPdv/description/price/isPaused/accessoriesProductComplement ref/company). accessories.service/controller/routes montados em routes/index.ts: /v2/accessories/category (by-company com lookup de produtos, list-by-name, create, update, delete), /v2/accessories/product (list com filtro category+isPaused+text search, list-group por categoria p/ app � compat 1.5.9 products / atual data por header appVersion, get, create com complements+items aninhados e array de imagens via file[].url/url, update com upsert+remo��o de complements/items, sort por position, delete em cascata complement+items, multer/S3 desacoplado), /v2/accessories/product-complement (list por :productId com lookup items, create) e /v2/accessories/product-complement-item (list/create/update/delete) + /food/product-complement/:productId (list p/ app). Tratamento de status vazio?false (isPaused) e valida��es de campos obrigat�rios ? 400 (AppError) em vez de 500. Fluxo validado em runtime: by-company (categoria+produto), list-group (keys title/key/products e com appVersion title/key/data), list-by-name, get/create/update de produto (upsert de complemento e remo��o do antigo), complement-list (com items), item list, food product-complement, sort, delete em cascata product?complement+items e category; company inv�lida ? 400; dados de teste removidos. 	sc --noEmit OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.21 Alerta de produto / prompt (backend) — criado model CustomerAlertProduct (coleção customer_alert_product: customer/franchise ref, listIds/identificationDate/period alertData+list, status) + customer-alert-product.service/controller/routes montado em /v2/customer-alert-product/alert-product: list (com lookup customer/franchise e filtro por lista/status/paginação), create (com listIds p/ envio), update, report (contagens por tipo/lista). Desacoplado do legado (sem warning/legacy prisma). Validação runtime em 03/09 (lista vazia 200, LIST_COUNT com dados → listagem). `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.22 Chat por pedido (backend) — criado model ChatMessage (coleção chatMessage: message, type text|audio|image|text_alert, dataType, urlFile, shoppingCart ref Cart, person/personSend customer|shopper|deliveryMan, personId/personSendId, flag, read, readSend, order_number; timestamps + índice composto) + chat-message.service/controller/routes: /chat (list admin com $or por person/personSend, create, no-read count?personId, updateRead) e /v1/front/chat/:cartId (list aggregate $project customer/shopper/deliveryMan + $lookup users, preservando nulls) + POST (create) p/ front. Fluxo validado em runtime: list vazio → create → list com mensagem → noRead total 1 → read; registrado em routes/index.ts com authenticate. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.23 Entregador: fila / corridas / online / cadastro (backend) — criados models QueueDeliveryMan (queueDeliveryMan; status WAIT|PROCESS|FINISH|NOT_FOUND_DELIVERYMAN; arrays tipados ObjectId), DeliveryManOnline (deliveryManOnline), RaceCanceled (raceCanceled), RaceHistory (raceHistory; status ACCEPTED|REFUSED|CANCELED) e RegisterDeliveryMan (registerDeliveryMan; status PENDING|ANALYZE|DECLINED|APPROVED; vehicleType CAR|MOTORCYCLE|BIKE) + delivery-dispatch.service/controller (montado em /deliveryMan e /delivery-man) e register-deliveryman.routes (montado em /register-deliveryman). Rotas: queue (list/statusOne/updateStatus/update/updateReceived/haveActive/back-to-queue — reseta Order p/ pending e desvincula deliveryman), online (create/offline com total em minutos/online-last-week aggregate mediaTime), race (canceled/list/history com lookup Order p/ companyName/paymentPriceDelivery), price (devolve {payment:{priceDelivery}} do Order), register (create upsert por cpf/list/paginator/updateStatus). Fluxo validado em runtime: queue list vazio, online-last-week {list,totalMedia}, register create por cpf (persiste), list vazio; /pre-register já coberto pelo módulo moderno. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.24 Busca /v1/search (backend) — criado search.service/controller/routes montado em /v1/search com authenticate: GET /company-products (valida searchText e companyType; filtro Company por active + $or name regex/tags/category SEM $text — $text+regex no mesmo $or causava Mongo "No query solutions"/291; attachProduct em Product+AccessoriesProduct) e GET /segment/company-products (sem companyType; limite 100 companies). Fluxo validado em runtime: company-products e segment retornam 200 (vazio sem dados correspondentes); 500 do $text corrigido. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.25 Slider / Tabloid / Tips / site da empresa (backend) — criados models Slider (slider; type slider|banner|driver, segment, category delivery|service, vizualizations, priorities, images, deletedAt, status/companyClick bool), Tabloid (tabloid), Tip (tip), TipDeliveryMan (tipDeliveryMan) e CompanySite (site; company/status/name/domain/URLlogo/email/about/slider/deletedAt) + site-content.service/controller/routes montados em routes/index.ts com authenticate: /slider (create com extractImages file/url + coerceBool status/companyClick; list/paginator com companyScope(userId); update; remove soft delete), /tabloid (create/list/update/remove hard delete via findByIdAndDelete), /Tip (create/list/search/remove), /TipDeliveryMan (create/list/remove), /company/site (create/list/paginator com companyScope; update/remove soft delete). Fluxo validado em runtime: Tip create, register-deliveryman create, site create e todos os lists 200; dados de teste removidos via mongosh e coleções confirmadas vazias. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.26 Log / auditoria (backend) — criado model Log (log: typeSystem MOBILE|WEB|BACKEND|UNDEFINED; typeLog WARN|ERROR|ALERT|SUCCESS|UNDEFINED; description Mixed; category; originError; path; error Mixed; method; type error; level; origin backend|frontend; request Mixed; timestamps + índices createdAt/typeLog/category), log.service (create/list por id/list-all/paginator com pageIn/pageOut e errorLog helper) + log.controller/routes, montado em `/log` com `authenticate`. Validação em runtime: `/log/create` → 201; `/log/paginator?pageIn=0&pageOut=10` → 200; `/log/:id` → 200; `/log/:id` com id inválido → 400. Dados de teste removidos e `tsc --noEmit` OK (admin-api reiniciado). |
| 03/09/2026 | Fase 1.27 Mobility — lacunas (backend) — criados ~23 models: DocumentType (documentType), PeakHour (peakHour), SupportSubject (supportSubject), VehicleDocuments (vehicleDocuments), Service (service), Passenger (passenger), FavoritePlaces (favoritePlaces), FavoriteDrivers (favoriteDrivers), ChosenDestinations (driverChosenDestinations), QrCodeDriver (qrCodeDriver), Indication (indication), MobilityNotification (mobilityNotification), PushNotification (pushNotification), ChatRace (chatRace), VoucherDiscount (voucherDiscount), TypePaymentService (typePaymentService), PriceCalculation (priceCalculation, model-only), MobilitySlider (mob_slider), Evaluation (evaluation), TravelBookingInfo (travelBookingInfo), + services/controllers. Rotas consolidadas + maps montadas em routes/index.ts com `authenticate` sob `/v1/mobility`: exports em mobility-crud (documenttypes/peakhours/supportsubjects/vehicle-documents), mobility-service-passenger (services + service-details, passengers), mobility-app (favorite-place, driver/favorite, chosen-destinations, qrcode, indication), mobility-extras (notifications, push-notification, topic, message, extract, type-payment-service, discount, slider), mobility-report-extra (evaluation, booking/schedule, report adm races/passenger/driver + active monitoring) e `/v1/mobility/maps` (maps.service com Google Maps via env GOOGLE_MAPS + fallback haversine; sem chave degrada com AppError 400). Config adicionou `GOOGLE_MAPS` (default ''). Validação em runtime: create+list/paginator de documentType, peakHour, supportSubject, service (+list-front/available), passenger; favorite-place, chosen-destinations, slider, type-payment, discount, indication (exige personReceive → 400 sem, 200 com), message, evaluation, report adm (3) + active monitoring (200), maps → 400 (degradação esperada sem GOOGLE_MAPS). Dados de teste removidos via mongosh e coleções confirmadas vazias. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.28 Wallet (admin) — vouchers / recarga (backend) — criados models `Voucher` (voucher: name/status/value/code/limit/used[] Mixed/dateInit/dateFinish/franchise ref/deletedAt, timestamps + toJSON; igual ao legado Finance/DigitalAccounts/VoucherModel) e `PassengerWallet` (passenger_wallet: holder ref Passenger, balance, transactions[] credit-ledger — ledger próprio escolhido pelo usuário para a recarga, sem tocar no DigitalAccount do lado delivery/finanças nem no Wallet de motorista). `voucher.service` (create valida code/value/limit/datas + código duplicado por franquia; paginator escopo por franquia com filtro deletedAt e pageIn/pageOut; update com normalização de status vazio→false + upsert; remove soft delete findByIdAndUpdate deletedAt; validate valida passageiro+franquia → busca voucher ativo por code+franquia → janela vigência dateInit/dateFinish → limite used.length>=limit → já-usado → credita o PassengerWallet credit e registra passenger/usedAt em used[]) + `passenger-wallet.service` (getOrCreate/credit/balance). Controller + rotas montadas em routes/index.ts com `authenticate`: `/v2/vouchers` (POST /, GET /paginator, PUT /:id, DELETE /:id) e `/v2/wallet` (GET /balance?passenger, POST /add-balance-voucher). Desacoplado do legado: sem middlewares checkFranchises (franchise vai no body/query, validado como ObjectId) e sem o subsystem legacy DigitalAccounts/BankTransactions/AccountBalance (a recarga usa o PassengerWallet com transação credit + reference=Voucher). Validação em runtime: create 201 + duplicado 400 + validações de campo 400; paginator 200 (escopo franquia, após delete total 0); add-balance-voucher 201 (credita balance 25) + duplicado "Você já usou" 400 + código inválido 400 + passageiro inválido 400; balance 200 (balance 25 + transação credit reference Voucher); update 200 (valor 30); delete soft 200 e voucher deixa de aparecer no paginator e é rejeitado no validate (400 Código inválido). Dados de teste (franchise/person/passenger/voucher/passenger_wallet) removidos via mongosh e coleções confirmadas vazias. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.29 Catálogo ECBR / Image Bank (backend) — criados models EcbrProductDepartment (coleção ecbr_ProductDepartment: name/base/ref_reference_ECBR barcode unique, keywords, images, departments ObjectId[], weight, description, copyright, status, audited, companyAdded) e ImageBank (coleção imageBank: barcode, productName/productAccent, keywords, description String[], packing ref, packingAmount, images, category String[], brand). ecbr-image-bank.service (list com filtros page/limit/barcode/name/isCopyright/isImages/isDepartments/status/audited/company + lookup department com projeção name, retorna {response,pagination,total}; generateCode ECBR00000001+leftPad; listByBarcode 404 se vazio; create valida nome/barcode/descrição/keywords/departments + monta images de file[].url/url + departments ObjectId + barcode duplicado -> 400 + propaga para products bruto se audited; update com normalização de status + upsert + propaga se audited; sync itera ecbr_ProductDepartment e updateMany em products bruto) + image-bank.service (create/list por barcode regex com populate packing, listPorNome productAccent lowercase+insensitive, listPorCategory regex, create/register, update, remove findByIdAndDelete). Controller + rotas montadas em routes/index.ts: /v2/ecbr-image-bank com uthenticate (GET /, GET /generate/code/ecbr, GET /barcode/:barcode, GET /sync, POST /, PUT /update/:id) e /imageBank público (GET /list/:barcode/:pageIn/:size, GET /listPorNome/:nome/:pageIn/:size, GET /listPorCategory/:category/:pageIn/:size, POST /create, POST /register, PUT /update/:id, DELETE /delete/:id). Validação em runtime: ecbr generate-code ECBR00000001/02, list vazio, create sem nome 400, create válido 200, barcode duplicado 400, list por barcode/name (sem acento case-insensitive), barcode inexistente 404, sync 200, update 200; imageBank list 200 (barcode/nome/categoria case-insensitive), register 200, update 200, delete 200 (id inválido 400). Upload legacy S3 desacoplado (imagens via url no body). Dados de teste removidos via mongosh e coleções confirmadas vazias. 	sc --noEmit OK, admin-api reiniciado. |

| 03/09/2026 | Fase 1.30 Endereço / Company público (backend) — criados models StateBr (coleção state_br: codigo_uf/uf/nome/latitude/longitude, índice nome) e CompanyDelivery (coleção company_delivery: isOpen/isManual/company ref/typePayments/mdr/fee/distance sub-schema/max_distance/max_distance_withdraw/min_purchase/max_amount_items/time_to_call_delivery/own_delivery/online_delivery/has_split/withdrawMarket/mediaRating/totalRating/deletedAt). address.service (listState → StateBr.sort({nome:1}); listCity → AddressCityModel/city_br com filtro codigo_uf Number + $text search nome, sort {nome:1}) + public-company.service (registerCompany: validações franchise ObjectId/file/lat/lng/name → AppError 400, extractImages de file[].url/url, normalizeCategory NFD+lowercase, cria Group moderno, insere company bruto na coleção companies com franchise/name/approved:false/status:false/groups/images/category/location GeoJSON, cria CompanyDelivery fee 15/min_purchase 20/max_amount_items 20/distance [{0,10000,10,25}] e atualiza company.companyDelivery; listLocation: aggregate $match status:true + lookup company_delivery + $unwind preserveNullAndEmptyArrays + filtro opcional companyDelivery.isOpen). Controller + rotas públicas (sem auth, como no legado) montadas em routes/index.ts: /v2/address (GET /state, GET /city) e /v2/company (POST /register-company, GET /location). Company moderno (strict) não alterado — fluxo público usa a coleção bruta companies. getAddress (geocode Google/LooseDelivery) fora do escopo da 1.30. Validação em runtime: /v2/address state/city 200 (vazios); register-company sem franchise 400 "Informe a Franquia", franchise inválido 400, sem file 400 "Imagens inválidas", sem lat/lng 400, válido 200 (cria Group + company bruta + company_delivery com fee 15); location 200 count=1 (após status:true), ?isOpen=true count=0 (delivery isOpen:false), ?isOpen=false count=1. Dados de teste removidos via mongosh (companies/group/company_delivery 0). `tsc --noEmit` OK, admin-api reiniciado. |

| 03/09/2026 | Fase 1.31 Twilio — alerta por chamada + verificação OTP (backend) — instalado `twilio` no backend + envs em config/index.ts `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_SERVICE_SID` (todas `default('')`, degradação graciosa). Criado model Twilio (coleção twilio: name/email/phone/note/active, timestamps — igual ao legado `_legacy_src/models/twilio.js`) + twilio.service com getClient() que degrada com AppError 400 quando sem TWILIO_ACCOUNT_SID/AUTH_TOKEN e getServiceSid() quando sem TWILIO_SERVICE_SID; create (persiste o alerta, como o legado, validando name/email/phone → 400), sendVerificationCode(phone) via client.verify.services(serviceSid).verifications.create (channel sms → {message}) e checkVerificationCode(phone, code) via verificationChecks.create → {valid: status==='approved', status}. Controller + rotas montadas em routes/index.ts em /twilio: POST / (create, com authenticate, como no legado), POST /send-code e POST /verify-code (públicos para verificação de número do mobile). Desacoplado do legado: sem o client criado no import da rota nem o número hardcoded +13028658482 do fluxo de chamadas de novo pedido (registra-se o alerta; a chamada TwiML fica para o fluxo de ordem, fora do escopo). Validação em runtime: send-code sem phone 400 "Informe o telefone", com phone 400 degradado "Twilio não configurado", verify-code sem code 400 "Informe o código de verificação", com code 400 degradado; create sem auth 401, sem name/email/phone 400, válido 201 (persiste). Dados de teste removidos via mongosh (coleção twilio vazia). `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 2.3 Finance — tela no painel React — corrigido o proxy do vite dev (porta 4202): antes o `proxyRoutes` era uma lista fixa de ~22 prefixos antigos que NÃO cobria os módulos migrados (404 em `/franchises`, `/cashback`, `/finance` — telas existentes quebradas em dev); reescrito `frontend-react/vite.config.js` como proxy catch-all `'/'` com `bypass` (retorna o index.html para navegação SPA e deixa o vite servir assets `/src|node_modules|@vite|@id|.vite` + extensões estáticas) e target `http://admin-api:8100` (hostname do backend na rede docker — env `VITE_API_PROXY` do container chegava vazio). Validado via 4202: `/health` 200, `/v2/company/location` 200 (público), `/finance/balances` 401 sem auth e 200 com token (JSON real). Criado `financeService` em `src/services/api.js` (listBalances/getCompanyBalance, CRUD cost-centers/type-payments/banks/agencies/digital-accounts/chargebacks + getDigitalAccountBalance/moveDigitalAccount) e página `src/pages/Finance.jsx` com abas: Balanços (cards resumo + tabela por empresa), Centros de Custo, Tipos de Pagamento, Bancos, Agências (select de banco), Contas Digitais (select agência/empresa) e Chargebacks (status). Rota `/finance` em `App.jsx` + item "Financeiro" no `Sidebar.jsx`. `vite build` OK (1530 módulos). Validação runtime via proxy: GET de todas as abas 200; CREATE cost-center/tipo-pagamento/banco/agência/conta digital/move (crédito 150, saldo 150) OK. Dados de teste removidos via mongosh (costcenters/typepayments/banks/agencies/digitalaccounts/chargebacks = 0). Subordinados e Invoices ficam p/ Fase 1.9. |
| 03/09/2026 | Fase 2.3 Telas restantes do painel React (10 telas) — adicionados em `src/services/api.js`: `domainService` (states/cities/types-users/app-versions/timezones/brazilian-banks/global-settings), `helpdeskService` (tickets CRUD/byProtocol/interactions CRUD), `faqService`, `emailService` (types/templates/variables), `marketingService` (campaign CRUD), `logService` (paginator), `voucherService` (paginator pageIn/pageOut+franchise, create/update/remove/validate), `mobilityService` (documenttypes/peakhours/supportsubjects CRUD), `monitorService` (order/order-detail/sales), `toolsService` (popup/integrations) e `chatService` (list-by-cart/create/no-read/set-read). Criadas páginas: `DomainSettings.jsx` (abas Estados/Cidades/Tipos de Usuário/Versões App/Bancos/Fusos/Config Global), `HelpDesk.jsx` (abas Tickets com protocolo tickedId + interações por protocolo / FAQ), `Email.jsx` (abas Tipos/Templates/Variáveis, campos alinhados ao model: key+name e subject/body/type), `Marketing.jsx` (campaign com nome/veículo/datas/downloads/note/images), `Log.jsx` (leitura), `Vouchers.jsx` (CRUD por franquia com código/valor/limite/vigência), `Mobility.jsx` (Tipos de Documento/Horários de Pico/Assuntos de Suporte) e `Monitor.jsx` (abas NOC pedidos+vendas tempo real, Chat por cartId com não-lidas, Tools popups+integrações). Rotas registradas em `App.jsx` e itens no `Sidebar.jsx` (Config. Domínio, HelpDesk/FAQ, E-mails, Marketing, Vouchers, Mobility, Monitor/Tools, Log/Auditoria). `DataTable.jsx` ganhou suporte a `onView` (botão visualizar). CRUDs alinhados aos models reais: HelpTicket (tickedId), EmailType (key/name), Campaign (name/disseminationVehicle/initialDate/...), Voucher (code/value/limit/dateInit/dateFinish/franchise). `vite build` OK (1538 módulos). Validação runtime via proxy 4202: GET de todos os endpoints 200; CREATE state/faq/email-type/campaign (após ajuste de campos)/document-type/ticket validados 201-200; dados de teste removidos via mongosh (states/faqs/emailtypes/campaigns/helptickets/documenttypes = 0). Supermercado, Pré-registro, Acessórios/complementos e Busca ficam para Fase 2.3/2.4 seguintes. |
| 04/09/2026 | Fase 2.3 Telas restantes do painel React (4 telas) - adicionados em `src/services/api.js`: `supermarketService` (tabloid list/create/delete + ecbr list/create/update/generate-code/sync), `preRegisterService` (pre-register paginator/update + register-deliveryman paginator/updateStatus), `accessoriesService` (category by-company/list/create/update/delete; product list/get/create/update/delete; complements by-product + create) e `searchService` (company-products com searchText+companyType + segment). Criadas paginas: `Supermarket.jsx` (abas Tabloid - listar/criar/excluir; ECBR Image Bank - listar por codigo/nome, criar (file.url obrigatorio p/ create, departments ObjectId[], keywords, audited propaga), editar), `PreRegister.jsx` (abas Pre-registro - paginator por status/busca + Aprovar APPROVED/Recusar DECLINED/Reenviar RESENT via PUT /pre-register/:id; Cadastro Entregador - paginator + update status via /register-deliveryman/status/:id), `Accessories.jsx` (seletor de empresa via companyService.getCompanies; abas Categorias CRUD e Produtos CRUD com categoria/pessoas/preco/imagens + modal de Complementos por produto create) e `Search.jsx` (form searchText+companyType -> GET /v1/search/company-products; exibe empresas com produtos aninhados). Rotas registradas em `App.jsx` (/supermarket, /pre-register, /accessories, /search) + itens no `Sidebar.jsx`. `vite build` OK (1542 modulos). Validacao runtime via proxy 4202 com token: GET de todos os endpoints 200 (tabloid/list, ecbr paginator, pre-register paginator, register-deliveryman paginator, accessories category by-company); CREATE tabloid, pre-register + approve (PUT status APPROVED), accessories category, product e complement, ecbr (apos ajuste p/ `file:{url}` - antes 400 Imagens invalidas) e search - todos 200/201; dados de teste removidos via mongosh (tabloid/ecbr/preregistration/accessoryCategory/accessoryProduct/accessoryComplement = 0). Schedules/invoices/loose-delivery nao possuem CRUDs dedicados no painel (anotado). |
