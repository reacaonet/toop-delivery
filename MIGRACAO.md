# GoJá Delivery — Plano de Migração

> Documento de controle do que **ainda não foi migrado** do sistema legado para o backend/painel moderno (TypeScript + React). Cada item tem etapas e um checkbox `[ ]` para marcarmos `[x]` conforme for realizado.

**Última atualização:** 10/09/2026

---

## Contexto atual (já migrado / rodando)

Backend moderno (`delivery_toop-admin/backend/src`) e painel React (`delivery_toop-admin/frontend-react`) já cobrem:

- **CORE delivery/e-commerce:** auth (login/cadastro store), empresas, categorias, produtos, pedidos, carrinho, banners, review, filiais, upload.
- **Estoque:** StockItem / StockBatch / StockMovement.
- **Mobility (subset):** drivers, bookings (aceitar/rejeitar/contra-oferta/iniciar/completar/cancelar/avaliar/QR), wallet do motorista, mensagens por corrida, promo.
- **Painel React** com: Dashboard, Empresas, Categorias, Banners, Pedidos, Usuários, Entregadores, Motoristas, Corridas, Cupons, Wallet, Pagamentos, Configurações, Relatórios, Perfil.
- **4 apps web:** web-client (cliente+rides), store (loja+estoque), deliveryman-app, landpage.
- **3 microserviços:** payment (8400), notification (8200), deliveryman (8300).
- **OTA server** (`toop-ota`, porta 8500) — server/CLI/wrapper prontos (falta módulo nativo, ver Fase 3).

**O que NÃO roda no compose (legado de referência):**
- `delivery_toop-admin/frontend` (painel **Angular**).
- `delivery_toop-admin/backend/_legacy_src` (backend **JS** legado).
- Apps mobile (4, React Native) — geridos via OTA, não no docker dev.

---

## Passo 1 — Backend: migração das áreas legadas do `_legacy_src`

> Critério: cada área ganha Model + Service + Route + Validators no backend moderno, e o endpoint é consumido pelo painel React.

### 1.1 Finance (módulo financeiro completo)
Adotada abordagem **novo/autônomo** (aprovado pelo usuário): módulo Finance desacoplado do legado, consumindo dados atuais (Orders/Payments/Company), sem depender de Franchise/Braspag. Implementado em `backend/src`: models + `finance.service.ts` + `finance.controller.ts` + `finance.routes.ts` (montado em `routes/index.ts` sob `/finance`).
- [x] Modelos: `CostCenter`, `TypePayment`, `Bank`, `Agency`, `DigitalAccount` (account+extract), `Chargeback` (padrão moderno, `toJSON` sem `__v`, índices).
- [x] Rotas CRUD (autenticadas): `/finance/balances`, `/finance/balances/company/:id`, `/finance/cost-centers`, `/finance/type-payments`, `/finance/banks`, `/finance/agencies`, `/finance/digital-accounts` (+ `/:id/balance`, `/:id/move`), `/finance/chargebacks`.
- [x] Balanço por empresa: agregação real sobre Orders (gross/subtotal/deliveryFees/discounts/paid, `status=delivered`) + `getCompanyBalance` (por método de pagamento + payments received/refunded).
- [x] Pendente (depende de Fase 1.9 gateways e 1.13 franquia): `Subordinate`/split Braspag, invoices, deliveries finance, saldo por franquia. *(09/09/2026 — coberto: repasse server-side em `/payments/repasse*` com `Split`/`PaymentTransaction` (Fase 1.9/2.2) e invoice/recipient/split via proxy ao microserviço 8400; saldo por franquia permanece pendente de dados/definição)*
- [x] Expor telas Finance no painel React (fazer na Fase 2.3 quando o backend estiver integrado). *(03/09/2026 - página Finance com abas: Balanços, Cost Centers, Type Payments, Bancos, Agências, Contas Digitais, Chargebacks)*

### 1.2 Cashback
- [x] Model `CashbackCampaign` + `CashbackCustomer` (saldo/histórico/total por mês).
- [x] Rotas: `/cashback/campaigns`, `/cashback/used/paginator`, `/cashback/customer/...`.

### 1.3 HelpDesk / FAQ
- [x] Models: `HelpTicket` (protocolo `tickedId`, subject, description, person, company, priority, department, status, name, email, phone, order, images, `deletedAt` soft-delete), `TicketInteraction` (helpTicketsId, description, origin, author), `Faq` (title, caption, description, status). Padrão moderno. `person` referenciado como `User` (model `Person` ainda não migrado — item 1.17).
- [x] Rotas (autenticadas): `/helpdesk/tickets` (list/create), `/helpdesk/tickets/protocol/:protocol`, `/helpdesk/tickets/:id` (get/update/delete), `/helpdesk/tickets/:ticket_id/interactions` (list/create), `/helpdesk/ticketinterations/:id` (update/delete); `/faq` (list/create/get/update/delete).
- [x] Lógica de negócio legada replicada: criar ticket gera interação inicial automaticamente (origin user/company); protocolo busca por `tickedId` + interações.
- [x] Integrar telas HelpDesk/FAQ no painel React (Fase 2.3). *(08/09/2026 - `HelpDesk.jsx` na rota /helpdesk + Sidebar "HelpDesk / FAQ": abas Tickets (CRUD + detalhe por protocolo com timeline de interações + nova interação origin company) e FAQ (CRUD title/caption/description/status). `helpdeskService` + `faqService` em api.js. Validação runtime: create ticket → interação inicial auto (origin user), list, protocol com interações, create interaction, update status SOLVED, delete soft; faq create/list/update/delete. Dados de teste removidos (helptickets/ticketinteractions/faqs = 0). Ver log.)*

### 1.4 Email (tipos/templates/variáveis)
- [x] Models `EmailType` (key, name, status, deletedAt), `EmailTemplate` (subject, body, type ref `EmailType`, company ref `Company`, status, deletedAt com `toJSON transform`), `EmailVariable` (name, title, deletedAt).
- [x] Rotas (autenticadas): `/emails/types` (CRUD), `/emails/templates` (CRUD, com `populate` de type), `/emails/variables` (somente leitura como no legado). Fluxo completo validado em runtime: criar type → criar template (type populado) → listar types/templates/variables. `tsc --noEmit` OK.

### 1.5 Marketing / campanhas
- [x] Model `Campaign` (name, disseminationVehicle, initialDate/finalDate, downloadAndroid/downloadIos — corrigidos typos `dowload*` do legado, note, image, soft-delete `deletedAt`).
- [x] Rota `/marketing/campaign` (CRUD autenticado) + `marketing.service/controller/routes`.
- [x] Integrar telas de campanhas no painel React (Fase 2.3). *(08/09/2026 - `Marketing.jsx` (rota /marketing + Sidebar "Marketing"): CRUD de campanhas de marketing (name, disseminationVehicle app/sms/email/social, datas, downloads Android/iOS, nota, imagem por URL) via `marketingService`; `Cashback.jsx` (rota /cashback + Sidebar "Cashback"): abas Campanhas (CRUD name/percent/amount/balanço balance=amount/datas/allApp/status toggle) e Cashback Utilizado (`/cashback/used/paginator`). Validação runtime: marketing campaign create/list(total=1)/update(veiculo sms, downloads 20)/delete; cashback campaign create(balance=1000)/list/update(status false, percent 15, balance 1500)/delete soft + used paginator {list:[],total:0}. Leftover pré-existente "Cashback Teste 10%" da Fase 1.2 (01/09) removido. Dados de teste: campaign/cashback_campaign = 0. Ver log.)*

### 1.6 Notificações administrativas
- [x] Criar/atualizar/deletar notificação (`POST/PUT/DELETE /notifications/:id`) e **enviar push** por Firebase (`POST /notifications/send` — cria a notificação e envia FCM por tópico; `POST /v2/notification-topic/send`). Envio via `firebase-admin` direto (não proxy p/ 8200). **Degrada com mensagem clara quando não há Service Account** (`FIREBASE_ADMIN_*`).
- [x] Tópicos de push + usuário-por-tópico (`POST /v2/notification-topic` — inscreve/desinscreve token em tópicos `{name}[_{value}]`).

### 1.7 Aplicativos / categorias de app
- [x] Model `AppCategory` (name, type supermarket/restaurant/accessories, showInApp, keyword, segment, status, showHome, order, images, soft-delete `deletedAt`). Desacoplado: `segment` virou string (no legado era ref `CompanySegment`/franchise, inexistente no moderno).
- [x] Rota `/application/category` (CRUD autenticado) + `app.service/controller/routes`.
- [x] Integrar telas de categorias de app no painel React (Fase 2.3). *(08/09/2026 - página AppCategories.jsx na rota /app-categories com CRUD e filtros - ver log)*

### 1.8 Pedidos de supermercado / fluxo shopping completo
- [x] Models migrados: `ShoppingDepartment` (coleção `department`), `ShoppingDepartmentMobile` (`departmentmobile`), `ShoppingSchedule` (`schedule`, dayWeek/startHour/endHour/type BOTH|DELIVERY|WITHDRAWAL), `ShoppingPaymentMethod` (`shoppingPaymentMethod`, flags/vencimento/token por gateway BRASPAG|PAGARME|IUGU) e `SortDepartment` (`sortDepartment`, ordenação por empresa). *(04/09/2026 — ver log)*
- [x] Rotas `/shopping/department`, `/shopping/departmentmobile`, `/shopping/schedule`, `/shopping/payment-method` (autenticadas) — list/paginator/create/update/delete(soft); schedule: `/all` (agrupado por empresa com `$lookup companies`), `/have/:company`, `/:company?type=`, `/type` (backfill BOTH), `/:id`. Regates de primeira-letra/`sortDepartment` no paginator; sugestões por regex; lookup ECBR por barcode. `moment` substituído por Date nativo UTC-3.
- [x] Fluxo shopping completo de carrinho/pagamento (09/09/2026) — criados models `ShoppingCart` (coleção `shoppingCart`, status pending/inProgress/purchaded/deleted/canceled, subtotal/discount/deliveryFee/total, tip/schedule/pixTxid/pixDate/deliveryAddress embutido) e `ShoppingCartItem` (coleção `shoppingCartItem`, product ref, amount/price/total, check/radio, type supermarket|restaurant|accessories, shopper ref, isPizza/size/pieces/flavors/notes), `OrderStatus` (coleção `orderStatus`, 14 estados legados WAIT_COMPANY→CANCELED, orderNumber, payment/typePayment/typeSchedule/deliveryman/shopper/franchise) e `OrderTracking` (coleção `orderTracking`, location 2dsphere) + model `PixGenerate` (coleção `pixGenerate`, txid/qrcode/status pending|paid|expired|failed). `Order` ganhou campos opcionais aditivos: `shoppingCart`, `deliveryMode` (own|online), `schedule`, `tip`, `pixTxid`. Rotas em `/shopping/cart` (list, current, get, create, add-item, update-item, remove-item, update, delete soft, reorder, checkout) e `/shopping/order` (list, get com statusHistory, cost-freight GET/PUT, own/online-delivery GET/PUT, start-schedule-order). Checkout valida carrinho não-finalizado/vazio, gera `orderNumber`, cria `Order` (paymentStatus paid quando MONEY) e registra `OrderStatus` WAIT_COMPANY. `/pix` (generate-charge → proxy 8400 com persistência de `PixGenerate`; verify/:txid → leitura local com tip de webhook). Validação runtime: cart create → add item (subtotal 21) → get → checkout (total 29 com deliveryFee 8, paymentMethod MONEY) → order/:id com statusHistory 1. `tsc --noEmit` OK.
- [x] Criação de método de pagamento com tokenização (09/09/2026) — `POST /shopping/payment-method/:customer` deixa de retornar 501: tokeniza via proxy ao microserviço 8400 (`/payment/card`, payload Braspag `{CustomerName,CardNumber,Holder,ExpirationDate,Brand,SecurityCode}`), extrai `cardToken` da resposta, persiste `shoppingPaymentMethod` com cartão mascarado (últimos 4) e `isMain` único. Sem `PAYMENT_APP_TOKEN`/`PAYMENT_APP_SECRET` ou microserviço fora → **degrade gracioso 400** com mensagem clara. List/update/delete seguem funcionais.
- [x] Web-client: supermercado como categoria + pagamento compartilhado (métodos/gateway do sistema) — DECISÃO (09/09/2026): supermercado = **só mais um tipo de empresa dentro do fluxo de delivery já existente** (reusa HomePage/CompanyPage/carrinho single-vendor/checkout — sem sistema paralelo). IMPLEMENTADO (09/09/2026): (1) **Débito server-side na criação do pedido** — `order.service.create` agora valida aritmética (subtotal=∑item.total, total=subtotal+frete−desconto; mismatch → 400), e para `credit_card`/`debit_card` exige `paymentMethodId` (cartão salvo do cliente, 404 se não pertence), cobra no gateway via `paymentGatewayService.pagarmeTransaction` com o payload real do legado `payloadPayment.js` (amount em centavos, card_id=cardToken, card_cvv=verifierCode, customer.documents tipo CPF, billing do endereço de entrega, postback `/pagar-me/driver/status`) e grava `paymentStatus=paid`; para `pix` gera `pixCharge({amount})` e persiste `pixTxid`/`pixQrcode` (novo campo aditivo) com `paymentStatus=pending`; ambos registram `PaymentTransaction` (`operation` charge/pix_charge). Falha/degrade de gateway → 400 claro e o pedido **não é criado**. Novos campos aditivos no `Order`: `paymentMethodId`, `pixQrcode` (+ `discount`/`tip` no validator). (2) **Meus Cartões no web-client** — página `PaymentMethodsPage` (rota /payment-methods, menu "Meus Cartões"): lista/novo (POST `/shopping/payment-method/:customer`, tokeniza+persiste mascarado)/principal (PUT isMain)/remover (DELETE soft); `shopping-payment-method.service.create` agora aceita `valid` como MM/AA (normaliza para o gateway e converte para Date fim-do-mês no storage). (3) **Checkout** — para cartão carrega os cartões salvos (radio, principal por padrão) e envia `paymentMethodId`; erros do gateway aparecem em toast; após pedido PIX confirmado, `OrderDetail` mostra o código copiável (banner). `tsc` OK, jest 67/67 (incl. 5 testes novos de order.service), web-client build OK. Pendente: wire do pagamento compartilhado na **mobilidade** (bookings) — mesma camada/cartões do sistema.

### 1.9 Payment gateways (Braspag / PagarMe / Iugu / PIX)
- [x] Dependências de SDKs (braspag, pagarme, iugu) — NÃO duplicadas no backend moderno: optou-se por **proxy ao microserviço payment (8400)** via `services/payment-gateway.service.ts` (axios, token de app cacheado 55min com `PAYMENT_APP_TOKEN`/`PAYMENT_APP_SECRET`, prefixo `LTS` default `v1`; degrada 400 claro quando microserviço/credenciais ausentes), espelhando os endpoints reais do 8400 (`/payment/card`, `/payment/card/:token`, `/payment/binCard/:bin`, `/sales`, `/pagar-me/{cards,pix,recipient,transactions}`, `/invoice*`, `/cancellation*`, `/split/after/:id`, `/payment/information/:id`, `/transaction`). *(09/09/2026 — ver log)*
- [x] Models: `PaymentTransaction` (coleção `paymenttransaction` via model name `PaymentTransaction`), `Split` (coleção por model `Split`), `Chargeback` (criado na Fase 1.1) — `PaymentTransaction`: gateway GATEWAYS (BRASPAG/PAGARME/IUGU/PIX/MONEY), operações TX_OPERATIONS (charge/repasse/pix_*/card_tokenize/invoice/...), status pending|processing|succeeded|failed|refunded, refs order/booking/shoppingCart/customer/company + gatewayId/gatewayResponse. *(09/09/2026 — ver log)*
- [x] Rotas: `/payments/*` (estendido — ordem específica antes de `/:id`): `GET /repasse`, `GET /repasse/company/:company`, `POST /card`, `GET /card/:tokenCard`, `POST /charge`, `POST /pix/charge`, `POST /cancellation/:paymentId`, `PUT /cancellation-partial/:paymentId`, `GET /invoice`, `GET /invoice/:id`, `POST /invoice`, `GET /invoice/receivable`, `POST /recipient`, `PUT /recipient/:recipientId`, `POST /split/after/:paymentId`, `GET /transaction/:paymentId`, `GET /transactions` + legados `GET /` e `GET /:id`. `/braspag/*` montado (transactions, transaction+merchantId, capture — caminhos legados apontando ao 8400; receive/notification com 400 claro). Registro de transações persistido via `paymentGatewayService.record()`. *(09/09/2026 — ver log)*
- [x] Subscription/assinatura e repasse (recipient/split): recipient CRUD via 8400; **repasse server-side** em `services/repasse.service.ts` — `recordRepasse(order)` chamado no `order.service` no bloco `delivered` (após `creditDeliverymanWallet`): guard anti-duplicidade (Split existente por order), feePct de `SettingsModel.deliverymanFeePercentage` (default 2), persiste `PaymentTransaction` (operation `repasse`) + 3 Splits (company 100% do líquido, deliveryman taxa de entrega menos taxa, platform taxa de gestão), `summaryByCompany` agrega pedidos delivered na faixa de `deliveredAt` (gross/deliveryFees/platformFee/deliverymanShare/net) e `listSplits` paginado. *(09/09/2026 — ver log)*

### 1.10 Cupons / ofertas (hoje só `Promo`)
- [x] Cupons por empresa, por cliente, cupom de destaque/high. *(01/09/2026)*
- [x] Model `Offer` (oferta, distinto de Promo). *(06/09/2026 — coleção `offer` (mesma do legado); `name`/`groupCompany`/`cost`/`status`/`description` requeridos + `images`; ver log)*
- [x] Rotas `/offer/*` (autenticadas, paths legados): `GET /list`, `POST /register`, `POST /create` (exige `file[].url/url` → 400 "Imagens inválidas", normaliza status vazio→false, extrai imagens), `PUT /update/:id`, `DELETE /delete/:id` (hard delete). *(06/09/2026 — ver log)*

### 1.11 Pedido "loose delivery" (delivery avulso)
- [x] Model + rotas `/loose-delivery` (+ address). *(01/09/2026 — fluxo adaptado ao domínio moderno de `Order`/`DeliveryAddress`; sem geocodificação externa nem customer/person fixos do legado)*

### 1.12 Embalagem / Packing e Shopper
- [x] Model `Packing`. *(01/09/2026)*
- [x] Model `Shopper` + atribuição a itens de carrinho. *(01/09/2026 — CRUD completo; atribuição a itens de carrinho fica registrada p/ 1.8 fluxo shopping)*

### 1.13 Franquia / Franchise
- [x] Model `Franchise` + config genérica. *(01/09/2026 — sub-schemas bankData/location/settingsDrive/settingsRace embutidos; state/city refs SettingState/SettingCity mantidos como ObjectId (models 1.19 pendentes); desacopladas integrações Pagar.me split, geocode Google e criação automática de conta digital (registradas p/ 1.9/1.13))*
- [x] Rotas `/franchises` + self-registration público. *(01/09/2026 — CRUD + paginator/search/list-all + `/public` sem auth + `/config/:company`)*

### 1.14 Pré-registro / dinâmico
- [x] Model `PreRegistration` (collection `preRegistration`, campos como ddi/phone/name/email/cpf/nif/status PENDENTE·APPROVED·ANALYZE·DECLINED·RESENT, fotos/documentos, vehicle*, bankData embutido, viewStop/NextRegister, soft-delete deletedAt) + `DynamicPreRegister` (collection `dynamicPreRegister`, schemas embutidos `InputTypeShema`/`UploadDocPhotoShema`). Padrão moderno. Desacoplado: ref `franchise`/`application` não resolvidos (models ainda não migrados).
- [x] Rotas `/pre-register` (PÚBLICAS como no legado: create/list-by-phone/update/delete, `/dynamic`, `/dynamic-record/:id`; `/paginator` autenticado). Fluxo validado em runtime.

### 1.15 Monitor (dashboards tempo real)
- [x] Rotas `/monitor/order`, `/monitor/sales` (pedidos e vendas em tempo real). *(01/09/2026 — ver log)*

### 1.16 Tooling: Popup / Integrações / compressão
- [x] Model `Popup` (collection `popup`: images, name, company ref `Company`, status, startDate/endDate, priorities, vizualizations limite, quantityViews contador, message, textMessageButton, width/height, url, redirectTo enum HOME/URL/ROUTE, startHour/endHour, soft-delete) + `PopupView` (collection `popupView`) + rotas `/tools/popup` (CRUD+updateViews+listPopupApp). Lógica de visibilidade (data/hora/prioridade/limite de views/não-visto) implementada e validada em runtime.
- [x] Model `Integration` (collection `int_integrations`: company ref `Company`, system enum JM_Diamante/RpInfo/Viva_Sistemas, status, soft-delete) + rotas `/tools/integrations` (CRUD+paginator+por-empresa). Validado em runtime.
- [x] Serviço de compressão de imagem (09/09/2026) — `POST /tools/compress` (auth, `uploadSingle('file')`): comprime via `sharp` (quality default 75, redimensiona opcional por query width/height, converte para WebP). `sharp` declarada no `package.json` mas carregada com import dinâmico + **fallback gracioso**: sem o pacote instalado no ambiente, retorna o arquivo original com `degraded:true` e aviso `npm install` (validado em runtime no dev). Multer/S3 legacy não replicados no endpoint moderno (upload local).
- [x] Sync de imagem (`syncImage`) (09/09/2026) — `POST /tools/sync-image` (auth): baixa a imagem de `PRODUCT_IMAGE_API` (config, sem tokens hardcoded), grava em `uploads/` e retorna metadata; **degrade 400 claro** quando `PRODUCT_IMAGE_API` não configurada (validado em runtime).

### 1.17 Cliente / DeliveryAddress / Guest / Person
- [x] Model `DeliveryAddress` + rotas (salvar/editar endereços do cliente). *(01/09/2026 — coleção `customer_delivery_address`; `customer` ref `User` (cliente moderno); `location` 2dsphere obrigatório (lat/lng); `main` único por cliente com propagação automática em create/update; rota `/delivery-address/*` com list por cliente, search (customer/main), find, create/update/delete (soft `isDeleted`). `newTopic` de notificações por cidade desacoplado (item 1.35 pendente))*
- [x] Model `Guest`. *(01/09/2026 — coleção `guest`; rotas públicas `/guest` (POST/PUT upsert por `device`, GET por device) espelhando legado sem auth)*
- [x] Model `Person` (identidade física + indicação) separado de User. *(01/09/2026 — coleção `person` (mesma do legado); refs franchise/company/city(SettingCity); rota `/person/*` com CRUD, paginator (pageIn/pageOut + nome/cpf), listPorNome, search (phone/email/id), avatar; status normalizado (string vazia/null → false); `referralCode` validado no update com **criação de `Indication`** (06/09/2026 — ver log))*
- [x] Model `Customer`. *(06/09/2026 — coleção `customer` (mesma do legado); ref person, iugu_id/email/ddi/phone/device/instanceIdToken/token/favoriteRestaurants/favoriteSupermarkets/termsNotAccepted default true/sku/appVersion/rating{stars,comment,dateRating}/deletedAt; índices phone/email/person/token; ver log)*
- [x] Rotas `/customer/*` (paths legados). *(06/09/2026 — paginator (page/limit + `$lookup` customer_delivery_address main+person, escopo geo opcional por franquia), list/list/:id (populate person), listPorNome (agrega por nome da person — corrige bug legado que buscava campo `name` inexistente em customer), create (sku único gerado 18-char+timestamp; sem mailer/notificação Google — externos desacoplados), update (ddi decode, sem upsert, gera sku se ausente), delete hard, search PÚBLICO (phone/email/person; backfill ddi), search-customer (email/phone regex), search-person-customer (person status:true por nome/email/telefone com `$toString` — sem crash do `$toLong` legado; + lookup customer com endereço principal); ver log)*
- [x] Duplicados (`DuplicateRecords`) e normalização. *(01/09/2026 — `/person/register-duplicates?type=person&field=phone|email` via aggregate; branch `customer` adicionado em 06/09/2026 com o model Customer migrado)*

### 1.18 ACL / Permissões / Roles / AccessGroup / AccessFlow
- [x] Models `Role`, `Permission`, `Module`, `AccessGroup`, `AccessFlow`. *(01/09/2026 — coleções `acl_roles`/`acl_permissions`/`settingModule`/`settingController`/`acessGroup`/`accessFlow`; + `Group`)*
- [x] Rotas `/acl/*`, `/accessGroup`, `/report/access-flow`, `/group`. *(01/09/2026 — ver log)*
- [x] Controle por permissão no painel React (aplicar no frontend-react as guards por role). *(01/09/2026 — página `/acl` só aparece para admin/manager; ver log)*

### 1.19 Configurações de domínio (hoje `Settings` é um doc único)
- [x] Models: `SettingState` (settingState: name/uf/country), `SettingCity` (settingCity: name, state ref, deletedAt, lat/lng), `TimeZone` (timeZone: offset/zone/name), `SettingTypesUsers` (settingTypesUsers: name/status/deletedAt), `AppVersion` (appVersion: name/version/platform/status/deletedAt), `SettingBrazilianBanks` (settingBrazilianBanks), `GlobalSettings` (globalSettings: serviceCharge). `Controller`/`Module` já portados na 1.18 (SettingController/SettingModule). *(03/09/2026 — ver log)*
- [x] Rotas `/setting/*`, `/v2/setting/*`. *(03/09/2026 — ver log)*

### 1.20 Catálogo de acessórios (loja não-food) e complementos de produto
- [x] Models `AccessoriesCategory` (accessoriesCategory), `AccessoriesProduct` (accessoriesProduct), `AccessoriesProductComplement` (accessoriesProductComplement), `AccessoriesProductComplementItem` (accessoriesProductComplementItem). *(03/09/2026 — ver log)*
- [x] Rotas `/v2/accessories/*`, `/food/product-complement*` (com auth). *(03/09/2026 — ver log)*
- [x] Ordenação (position + sort) / status (isPaused) / desconto de produto (pricePromotion, percentualDiscount) + list-group por categoria (appVersion). *(03/09/2026 — ver log)*

### 1.21 Prompt de produto / Alerta de produto
- [x] Model `CustomerAlertProduct` + rotas `/v2/customer-alert-product/alert-product` (list/create/update/report). *(03/09/2026 — ver log)*

### 1.22 Chat por pedido (hoje só `Message` por corrida)
- [x] Model `ChatMessage` + rotas `/v1/front/chat/:cartId` (list aggregate + create) e `/chat` (list, no-read, create, read). *(03/09/2026 — ver log)*

### 1.23 Entregador: fila / corridas / histórico / online
- [x] Models `QueueDeliveryMan`, `DeliveryManOnline`, `RaceCanceled`, `RaceHistory` + rotas `/deliveryMan/*`: queue, back-to-queue, race/list, online-last-week, price. *(03/09/2026 — ver log)*
- [x] Aprovação de cadastro de entregador/motorista (`/register-deliveryman`; `/pre-register` já coberto pelo módulo moderno). *(03/09/2026 — ver log)*

### 1.24 Busca
- [x] Rota `/v1/search` (`/company-products`, `/segment/company-products`). *(03/09/2026 — ver log)*

### 1.25 Slider / Tabloid / Tips / site da empresa
- [x] Models `Slider`, `Tabloid`, `Tip`, `TipDeliveryMan`, `CompanySite` (Site). *(03/09/2026 — ver log)*
- [x] Rotas `/slider/*`, `/tabloid/*`, `/Tip/*`, `/TipDeliveryMan/*`, `/company/site/*`. *(03/09/2026 — ver log)*

### 1.26 Log / auditoria
- [x] Model `Log` + rota `/log` (auditoria de ações no painel). *(03/09/2026 — ver log)*

### 1.27 Mobility — lacunas
- [x] Maps (directions/matrix/geocode/autocomplete) — `v1/mobility/maps/*`. *(03/09/2026 — ver log)*
- [x] Services (tipos de corrida) + `service-details`. *(03/09/2026 — ver log)*
- [x] Passengers CRUD, DocumentType, VehicleDocuments, PeakHour, SupportSubject. *(03/09/2026 — ver log)*
- [x] Push/topics de mobilidade, favoritePlaces, chosenDestinations, indication, QR driver, slider mobility, agenda/agendamentos, extract, discount paginator, report adm (races/passenger/driver). *(03/09/2026 — ver log)*

### 1.28 Wallet (admin) — vouchers / recarga
- [x] `/v2/vouchers` (CRUD + validate) + recarga por voucher (hoje só balance/credit/debit manual). *(03/09/2026 — ver log)*

### 1.29 Catálogo ECBR / Image Bank
- [x] `/v2/ecbr-image-bank` (product-bank, generate-code, sync, barcode) e `/imageBank`. *(03/09/2026 — ver log)*

### 1.30 Endereço / Company público
- [x] `/v2/company/register-company`, `/v2/company/location`, `/v2/address`. *(03/09/2026 — ver log)*

### 1.31 Twilio (verificação de número)
- [x] `/twilio` (alerta por chamada + verificação OTP por Verify, sem microserviço). *(03/09/2026 — ver log)*

---

## Passo 2 — Painel Admin React: telas a criar/ativar

> O painel React (`frontend-react`) já tem boa base. Faltam telas e correções.

### 2.1 Ativar telas existentes mas fora da rota
- [x] **Products.jsx** — adicionar rota `/products` + link no sidebar (hoje é código morto não-roteado).
- [x] **Painel.jsx** — adicionar rota/sidebar "Painel de Pedidos" (hoje existe mas inacessível).

### 2.2 Correções de consistência
- [x] `payments` não era usado só client-side: migração do **repasse/comissão para server-side concluída** (Fase 1.9/2.2) — `repasse.service` + `PaymentTransaction`/`Split` + rotas `/payments/repasse*`; chamado automático no fluxo `delivered` do `order.service`. A tela `Payments.jsx` (cálculo client-side sobre Orders) segue válida, agora com contraparte server-side auditável. *(09/09/2026 — ver log 1.9/2.2)*
- [x] Unificar serviços: `Categories`, `Banners` e `Products` usavam `api.get/delete` direto — refatorados para `categoryService`, `bannerService`, `productService`.
- [x] Verificado: `promo` singular **está correto** no backend (`/promo` — montado em `routes/index.ts`); `promoService` do frontend já usa `/promo`. Contrato `wallet` confirmado: `GET /wallet/balance` e `/wallet/transactions` aceitam `driverId` via **query param**, igual ao frontend. Nenhum ajuste necessário.
- [x] `Profile.jsx` — "último acesso" real: backend agora grava `lastLogin` no login (`auth.service.ts`) e o Profile busca dados atualizados via `GET /auth/me` (novo `authService.me` + `refreshUser` no AuthContext), mostrando `current.lastLogin` (fallback `—`) em vez de `new Date()`.

### 2.3 Novas telas por módulo migrado (criar quando o backend da fase 1 estiver pronto)
- [x] Finanças: Digital Accounts (bancos/agências/contas), Extrato, Cost Centers, Type Payments, Chargeback. *(03/09/2026 - ver log; Subordinados e Invoices ficam p/ Fase 1.9)*
- [x] Cashback: campanhas, recebidos, histórico. *(03/09/2026 - página Cashback.jsx com abas: Campanhas / Cashback Utilizado)*
- [x] HelpDesk/FAQ: tickets + interações, FAQ. *(03/09/2026 - página HelpDesk.jsx com abas Tickets/FAQ; form com protocolo tickedId, interações por protocolo)*
- [x] Email: tipos, templates, variáveis. *(03/09/2026 - página Email.jsx com abas Tipos/Templates/Variáveis; campos alinhados ao model: key/name e subject/body/type)*
- [x] Marketing: campanhas. *(03/09/2026 - página Marketing.jsx; campos alinhados ao model Campaign: name/disseminationVehicle/initialDate/finalDate/downloads/note/image)*
- [x] Supermercado: tabloid + ECBR Image Bank. *(04/09/2026 - página Supermarket.jsx com abas Tabloid (create/list/delete) e ECBR (list por código/nome, create com file.url, update); schedules/invoices/loose-delivery não possuem CRUDs dedicados no painel - ver log 04/09)*
- [x] Ofertas (Offer). *(06/09/2026 - página Offers.jsx na rota /offers com create/list/edit/delete; custo, status, grupo de empresa, imagens por URL; create via /offer/register com status booleano, edit via /offer/update/:id com file - ver log 06/09)*
- [x] Clientes (Customer/person). *(06/09/2026 - página Customers.jsx na rota /customers com aba Clientes (paginator página/limite + detalhe via list/:id + exclusão) e aba Busca (por nome da pessoa via search-person-customer e por e-mail/telefone via search-customer) - ver log 06/09)*
- [x] Indicações (Indication). *(06/09/2026 - página Indications.jsx na rota /indications com paginator geral (Indicado/Recebedor/Código/Valor/Resgatado/Ativo/Data) + paginator novo no backend em /v1/mobility/indication/paginator - ver log 06/09)*
- [x] Pessoas (Person). *(06/09/2026 - página Persons.jsx na rota /persons com abas Pessoas (paginator/busca por nome/detalhe/referralCode/exclusão soft) e Duplicados (register-duplicates person por telefone/e-mail) - ver log 06/09)*
- [x] Departamentos (Shopping Department). *(06/09/2026 - página ShoppingDepartments.jsx na rota /shopping-departments, CRUD via /shopping/department (paginator com term/busca/employee, create/update/soft delete) - ver log 06/09)*
- [x] Sliders (SiteContent). *(06/09/2026 - página Sliders.jsx na rota /sliders, CRUD via /slider (paginator, create exige file.url+company, update coerce bool, delete soft) - ver log 06/09)*
- [x] Alertas de Produto (AlertProduct). *(06/09/2026 - página AlertProducts.jsx na rota /alert-products com abas Alertas (list/create, edição desativa - PUT sem body) e Relatório (/alert-product/report paginado) - ver log 06/09)*
- [x] Gorjetas (Tip/TipDeliveryMan). *(06/09/2026 - página Tips.jsx na rota /tips com abas Gorjetas (list/create/delete/search por status+type) e Gorjetas Entregador (list/create/delete) - ver log 06/09)*
- [x] Sites de Empresa (Site). *(06/09/2026 - página Sites.jsx na rota /company-sites, CRUD via /company/site (paginator page/limit 0-based, create/update/delete soft) - ver log 06/09)*
- [x] Horários (Shopping Schedule). *(06/09/2026 - página Schedules.jsx na rota /shopping-schedules com abas Por Empresa (agrupado /all; excluir) e Cadastrar (dayWeek/startHour/endHour HHMM/type); collection `schedule` - ver log 06/09)*
- [x] Mobility Passageiros (Passenger). *(06/09/2026 - página MobilityPassengers.jsx na rota /mobility/passengers, CRUD via /v1/mobility/passengers (paginator pageIn/pageOut, search/filter por nome, create/update/remove soft) - ver log 06/09)*
- [x] Mobility Serviços (Service). *(06/09/2026 - página MobilityServices.jsx na rota /mobility/services, CRUD via /v1/mobility/services (paginator pageIn/pageOut + busca por nome, create com type/capacity/franchise) - ver log 06/09)*
- [x] Mobility Sliders (MobilitySlider). *(06/09/2026 - página MobilitySliders.jsx na rota /mobility/sliders, CRUD via /v1/mobility/slider (paginator pageIn/pageOut + target, create exige file:[{url}]+franchise) - ver log 06/09)*
- [x] Mobility Push / Fatos (PushNotification / VoucherDiscount). *(06/09/2026 - página MobilityNotifications.jsx na rota /mobility/notifications com abas Push Notifications (paginator + create exige topic ou user) e Cupom Desconto (paginator/create/update; sem DELETE no backend) - ver log 06/09)*
- [x] Mobility Avaliações (Evaluation). *(06/09/2026 - página MobilityEvaluations.jsx na rota /mobility/evaluations com abas Passageiro→Motorista (list) e Motorista→Passageiro (driver/paginator) + média por rated - ver log 06/09)*
- [x] Mobility Relatórios (Report adm driver/passenger/races). *(06/09/2026 - página MobilityReports.jsx na rota /mobility/reports com abas Motoristas/Passageiros/Corridas: balanços + tabelas paginadas com filtros de data/status - ver log 06/09)*
- [x] Mobility Monitor (map/monitoring + active/monitoring). *(06/09/2026 - página MobilityMonitor.jsx na rota /mobility/monitoring, tabela do monitoramento no mapa (monitoring) com pageIn 1-indexado + card do monitoramento ativo (stub no backend) - ver log 06/09)*
- [x] Mobility Extrato por driver. *(06/09/2026 - página MobilityExtract.jsx na rota /mobility/extract, seletor de motorista + extrato agrupado por mês (GET /v1/mobility/extract/:driver) com cards de resumo - ver log 06/09)*
- [x] Mobility Mensagens/Conversas. *(06/09/2026 - página MobilityMessages.jsx na rota /mobility/messages, conversas por motorista/passageiro + lista por booking + enviar mensagem - ver log 06/09)*
- [x] Mobility Tópicos (FCM topics). *(06/09/2026 - página MobilityTopics.jsx na rota /mobility/topics: inscrever token em tópicos, enviar push por tópico e vincular usuários a tópicos (link-user-topics); operações degradam 400 sem Service Account FIREBASE_ADMIN_* - ver log 06/09)*
- [x] Mobility Documentos de Veículo / Formas de Pagamento / Viagem. *(06/09/2026 - página MobilityDocuments.jsx na rota /mobility/documents com abas Documentos (paginator/searchDriver + create/update, status=true propaga dados no Driver), Formas de Pagamento (readonly) e Travel Info (busca por booking, 404 quando não concluído) - ver log 06/09)*
- [x] Mobility Notificações App (config). *(06/09/2026 - página MobilityNotificationsApp.jsx na rota /mobility/app-notifications: configurar/exibir notificações do app (paginator/gráfico/create/update/remove soft via deletedAt) - ver log 06/09)*
- [x] Grupos de Empresa (Group). *(06/09/2026 - página Groups.jsx na rota /groups: CRUD (paginator pageIn/pageOut + listPorNome/franchise, soft delete) - ver log 06/09)*
- [x] Métodos de Pagamento Supermercado Online. *(06/09/2026 - página ShoppingPaymentMethods.jsx na rota /shopping-payment-methods: list por cliente (User role customer), toggle isMain, editar nome/flag, soft delete; create bloqueado 501 (gateway, Passo 1.9) - ver log 06/09)*
- [x] Config. Empresa (franchise por company). *(06/09/2026 - página CompanyConfig.jsx na rota /company-config: seletor de empresa → GET /franchises/config/:company → activateTip; distingue "sem franquia" via company.franchise; + franchiseService.listAll/getConfig - ver log 06/09)*
- [x] Access Flow (relatório). *(06/09/2026 - página AccessFlow.jsx na rota /access-flow: /report/access-flow/list (30 dias agrupados) + /statistic?timeInterval=N → count - ver log 06/09)*
- [x] Mobility Tipos de Documento (DocumentType). *(06/09/2026 - página MobilityDocumentTypes.jsx na rota /mobility/document-types: paginator/search/listAll/get/CRUD soft (name/type VEHICLE|DRIVER/status) - ver log 06/09)*
- [x] Mobility Pico de Horário (PeakHour). *(06/09/2026 - página MobilityPeakHours.jsx na rota /mobility/peak-hours: usa mobilityService existente; paginator/listAll/CRUD soft (franchise/start/end strings "800"/"1800"/status) - ver log 06/09)*
- [x] Mobility Assuntos de Suporte (SupportSubject). *(06/09/2026 - página MobilitySupportSubjects.jsx na rota /mobility/support-subjects: paginator (status all)/listAll/graphic/search/CRUD soft (franchise/subject/type PASSENGER|DRIVER/target CANCEL|SUPPORT/status) - ver log 06/09)*
- [x] Packing, Shopper, Franquias. *(03/09/2026 - páginas Packings.jsx, Shoppers.jsx, Franchises.jsx já ativas nas rotas/sidebar)*
- [x] Pré-registro entrega/motorista + aprovação. *(04/09/2026 - página PreRegister.jsx com abas Pré-registro (paginator por status/busca; Aprovar APPROVED / Recusar DECLINED / Reenviar RESENT via PUT /pre-register/:id) e Cadastro Entregador (paginator + update status)*
- [x] NOC/Monitor (pedidos/vendas tempo real). *(03/09/2026 - página Monitor.jsx aba NOC: GET /monitor/order e /monitor/sales)*
- [x] Tools: Popup, Integrations, compressão de imagem. *(03/09/2026 - página Monitor.jsx aba Tools com painéis Popups e Integrações)*
- [x] Configurações de domínio: cidades, estados, timezones, types-users, app-version, bancos. *(03/09/2026 - página DomainSettings.jsx com abas Estados/Cidades/Tipos de Usuário/Versões App/Bancos/Fusos/Config Global)*
- [x] ACL: roles, permissões, módulos, grupos de acesso, access-flow. *(03/09/2026 - página AccessControl.jsx ativa na rota /acl, visível p/ admin/manager)*
- [x] Acessórios (loja não-food) + complementos de produto. *(04/09/2026 - página Accessories.jsx com seletor de empresa; abas Categorias (CRUD) e Produtos (CRUD com categoria/pessoas/preço/imagens) + modal de Complementos por produto (create)*
- [x] Mobility: services, passengers, schedules, vehicles, documents, peak hours, push, reports, maps. *(03/09/2026 - página Mobility.jsx com tipodoc/peakhour/supportsubject; demais subcategorias p/ log de Mobility)*
- [x] Wallet admin: vouchers, recarga. *(03/09/2026 - página Vouchers.jsx CRUD por franquia; recarga já coberta na tela Wallet existente)*
- [x] Chat por pedido (supervisão). *(03/09/2026 - página Monitor.jsx aba Chat: GET/POST /v1/front/chat/:cartId + não-lidas)*
- [x] Log/auditoria. *(03/09/2026 - página Log.jsx leitura via /log/paginator)*
- [x] Busca de empresa/produtos. *(04/09/2026 - página Search.jsx: form searchText+companyType → GET /v1/search/company-products; exibe empresas com produtos aninhados)*

---

## Passo 3 — Aplicações Web (cliente / entregador / loja / landpage)

> **Forma real de trabalho (04/09/2026):** o produto roda como **aplicações web** servidas por Vite na stack dev (`docker-compose.dev.yml`):
> - **Cliente** — `delivery_toop-web-client` → `web-client` → **http://localhost:4200** (`toop-web-client-dev`)
> - **Entregador** — `delivery_toop-deliveryman` → `deliveryman-app` → **http://localhost:4204** (`toop-deliveryman-app-dev`)
> - **Loja** — `delivery_toop-store` → `store` → **http://localhost:4203** (`toop-store-dev`)
> - **Landpage** — `delivery_toop-landpage` → `landpage` → **http://localhost:4205** (`toop-landpage-dev`)
> - **Painel admin** — `frontend-react` → **http://localhost:4202**
>
> **Não** há APK/React Native no caminho ativo: os diretórios `mobile/*` (RN) são código paralelo/legado, **fora** do fluxo de entrega web. O servidor OTA (`delivery_toop-ota`, 8500) e o `OTAProvider`/`OTAStorage` só se aplicam **se**, no fim, for gerado um wrapper nativo (WebView) — até lá ficam como infra opcional, sem trabalho ativo.

### 3.1 Aplicações web — alinhamento ao backend moderno (Fase 1)
- [x] Confirmar/migrar os endpoints consumidos pelas apps web (cliente/entregador/loja/landpage) para os módulos modernos da Fase 1 (auth, pedidos, catálogo, pré-registro, etc.). *(04/09/2026 — confirmado: as 3 apps com API (cliente 4200, loja 4203, entregador 4204) já consomem o backend moderno `admin-api:8100` via proxy vite `/api` (que faz rewrite removendo o prefixo `/api`). Todas as famílias de endpoints usadas pelas apps — `/auth`, `/cart`, `/orders`, `/companies`, `/products`, `/categories`, `/banners`, `/reviews`, `/bookings`, `/promo`, `/users`, `/messages`, `/deliverymen`, `/drivers`, `/wallet`, `/settings`, `/upload`, `/branches`, `/stock-items`, `/stock-batches`, `/stock-movements` — existem montadas em `backend/src/routes/index.ts` com os mesmos sub-caminhos que as apps chamam. Validação em runtime: login e cadastro reais via proxy das 3 apps (cliente/loja/entregador) retornam token; `GET /orders?customer=<ObjectId válido>` 200 (o 500 só ocorre com ObjectId inválido); socket.io está no mesmo `httpServer:8100` (server.ts) cobrindo os eventos `booking:*` das apps. Landpage 4205 é 100% visual, sem API. Foi criado/removido um cliente de teste para a verificação.)*
- [x] Migrar login/cadastro das apps web para o fluxo `auth` moderno (`POST /auth`, token JWT). *(04/09/2026 — confirmado já alinhado: as apps web já chamam `POST /auth` (login), `/auth/register` (cliente), `/auth/register-store` (loja), `/auth/register-deliveryman` (entregador), `/auth/refresh` e `/auth/me` — todos implementados no backend moderno (`backend/src/routes/auth.routes.ts`) com contratos idênticos `{success,data:{user,token,refreshToken}}` (login/register) e `{token}` (refresh). Interceptores das apps guardam o JWT em `localStorage` e reenviam via `Authorization: Bearer`. Token admin validado via proxy das 3 apps.)*

### 3.2 OTA (infra opcional — só se houver wrapper nativo no fim)
- [ ] Implementar módulo nativo **`OTAStorage`** (Android + iOS) de `saveBundle`/`loadBundle` — apenas se for gerado APK wrapper (WebView). Hoje só existe o wrapper JS + server. *(09/09/2026 — avaliado: referências `NativeModules.OTAStorage.saveBundle/loadBundle` no código RN não têm módulo nativo correspondente; sem SDK Android/iOS no ambiente não é buildável aqui. Permanece condicionado à decisão de produto de gerar APK wrapper.)*
- [ ] Publicar um bundle real de teste ponta a ponta (storage só tem arquivos de teste) — igualmente condicionado ao wrapper nativo.
- [x] Esclarecer conflito de porta 8500. *(04/09/2026 - no stack dev em execução o serviço `deliveryman-microservice` usa a porta 8300 (docker-compose.dev.yml) e o `ota-server` usa 8500 — NÃO há conflito no stack ativo. O `8500:8500` só existe no compose órfão/standalone `delivery_toop-microservice_deliveryman/docker-compose.yml` (find_delivery, node:10), que não roda no stack dev.)*

### 3.3 Código RN legado (`mobile/*`) — fora do caminho web
- [x] Manter `mobile/*` (client/driver/shopper RN) apenas como referência; **não** investir em upgrade/stack deles enquanto o fluxo é web. *(09/09/2026 — avaliado e registrado: as 4 apps RN são código paralelo/legado fora do caminho de entrega web; referências a módulos nativos inexistentes (`NativeModules.OTAStorage`) reforçam que qualquer wrapper depende de decisão de produto. Decisão: manter como referência; remoção futura se confirmado dead code.)*

---

## Passo 4 — Integrações (ERP / terceiros)

### 4.1 Diretório `delivery_toop-integrations` está vazio
- [x] Rota `/tools/integrations` — **já implementada no backend moderno** (Fase 1.14/1.16). *(Confirmado 04/09/2026: `backend/src/models/Integration.ts` (sistemas `JM_Diamante`, `RpInfo`, `Viva_Sistemas`, company/status/soft-delete) + `tools.routes.ts` montado em `/tools/integrations`: paginator, `company/:company`, list, create, update, delete — tudo `authenticate`. É o mesmo contrato que o painel legado Angular consumia.)*
- [ ] Definir/migrar os **conectores/tipadores** de integração (Pratiko, Viva Sistemas, RPInfo, JM_Diamante) — **ADIADO por depender de ERP externo**. O código legado (`_legacy_src/services/runProcessIntegration/{runProcess.js, pratiko/newOrder.js}` e `frontend/.../services/integration/{RpInfo, Viva_Sistemas}/*`) conecta-se a um **gateway externo** (`apiIntegrationsURL`) com endpoints `v1/viva-sistemas/products/barcode/:barcode/company/:company?synchronize=true` e similares. Requer credenciais/endpoints reais + decisão: reutilizar o gateway legado ou criar módulo no backend moderno. *(04/09/2026 — pendente; sem trabalho ativo até ter acesso ao ambiente do provider.)*
- [ ] Sync de imagem / `v1/all/unsynchronized-departments` — **ADIADO por external**. O script `backend/scripts/syncDepartment/*` é um import pontual de planilha com IDs pratiko e URL DigitalOcean Spaces hardcoded, referenciando models legados — não é um serviço reutilizável. A rota `v1/all/unsynchronized-departments` (Sankhya) depende do gateway/credenciais. *(04/09/2026 — pendente.)*

### 4.2 Gateway de integrações
- [x] Scaffold de serviço dedicado criado em `delivery_toop-integrations` (09/09/2026 — package.json/tsconfig/servidor Express 8501 com `/health` + `/v1/token` (501 claro até `INTEGRATION_APP_TOKEN`/`INTEGRATION_APP_SECRET`) + `/integrations/connectors` (degrade 400 "conectores ainda não contratados"). Integrações de dados (CRUD) já vivem no backend moderno em `/tools/integrations`). Conexão com ERPs reais continua **ADIADO por external** (credenciais/contrato do provider).

---

## Passo 5 — Produção / Deploy / Segurança

### 5.1 Segurança (pendências do README — validar se já resolvidas)
- [x] Chaves `.pem`/`.key` **NÃO** estão no git — `.gitignore` bloqueia `*.pem`/`*.key`/`*.crt`/`*.p12`/`*.keystore`/`firebase-adminsdk*.json`; `git ls-files` não lista nenhum desses. *(Auditado 04/09/2026.)*
- [x] **CORREÇÃO (04/09/2026)** — o item acima estava **incorreto**: existiam **duas chaves RSA privadas reais de JWT hardcoded** em `docker-compose.dev.yml` (`JWT_PRIVATE_KEY` do microserviço deliveryman e do payment, com material base64 entre `-----BEGIN/END PRIVATE KEY-----`). Removidas do arquivo commitado e substituídas por referências de env `${JWT_PRIVATE_KEY_DELIVERYMAN_MICRO}` / `${JWT_PRIVATE_KEY_PAYMENT_MICRO}` (padrão já usado no mesmo arquivo para `CLOUD_FIREBASE_private_key`). Documentadas em `.env.example`. **AÇÃO OBRIGATÓRIA do usuário:** como as chaves já estiveram no histórico git, **rotacionar** os pares de chaves JWT dos microserviços (gerar novos com `openssl genrsa -out jwt_private.pem 2048`, colocar nos `.env` locais / gerenciador de segredos, e reatribuir/re-emitir tokens JWT afetados) e considerar purgar o histórico com BFG.
- [ ] Revogar service account Firebase `food-syulnv` no Google Cloud Console (se ainda ativa).
- [x] Nenhuma chave **Firebase/Service Account (`adminsdk`)* commitada no histórico. *(Auditado 04/09/2026: `git grep -I -E "BEGIN (RSA )?PRIVATE KEY" <todas as revs>` só retorna placeholders — `.env.example` usa `Your-Private-Key`, o compose (após a correção acima) não tem mais chave real, e `firebase.txt` contém apenas a **config web** do Firebase (apiKey/appId do Web SDK — públicos por design, não um segredo). Nenhum `firebase-adminsdk-*.json` com `private_key` real esteve no histórico.)*
- [x] Gerar `.env` de produção com senhas fortes (existe `.env.production.example`). *(09/09/2026 — `.env.production.example` atualizado com os novos itens da Fase 1/4/5: `PAYMENT_URL`/`NOTIFICATION_URL`/`DELIVERYMAN_URL` ativas, `INTEGRATION_URL`, `PRODUCT_IMAGE_API`, `LTS`, `PAYMENT_APP_TOKEN`/`PAYMENT_APP_SECRET` e `GRAFANA_USER`/`GRAFANA_PASSWORD` — placeholder `CHANGE_ME_*`. As envs dos microserviços já usavam referência `${...}` sem chaves commitadas.)*

### 5.2 CI/CD
- [x] Revisar builds/testes dos 4 apps web + backend + painel. *(09/09/2026 — o `.github/workflows/ci.yml` existente estava obsoleto e impraticável: triggers em `main`/`develop` (o repo usa `master`), `npm ci` no backend e no painel sem `package-lock.json` commitado (falharia) e job docker apontando para `delivery_toop-microservice_*`. Substituído por `.github/workflows/ci.yml` funcional (GitHub Actions, Node 20, `push`/PR em `master`, concurrency por ref): job `backend` (working-directory `delivery_toop-admin/backend`, env de segredo do jest + `npm install` + `tsc` + jest — 7 suites/63 testes), job `admin-panel` (frontend-react, sem lock commitado → `npm install` + `vite build` + `vitest run` — 7 testes) e job `web-apps` em matrix x4 (web-client/store/deliveryman/landpage: setup-node com `cache: npm`/`cache-dependency-path`, `npm ci` — lockfiles confirmados em sincronia via `npm ci --dry-run` nos 4 containers — + `npm run build` = `tsc && vite build`). Para o CI ficar verde foi corrigido o build do web-client (4 erros TS pré-existentes): `getCompanyId` aceita `string | { _id; name?; logo? }` (HomePage.tsx — antes `Company | Order`, e `order.company` era `string | objeto`), `phone?` no `User` do AuthContext (ProfilePage.tsx lia `user.phone`), setter não usado `setPickupComplement` removido do destructuring (RideRequestPage.tsx, mantendo o value usado no payload) e guard `if (id)` no `openChat` de RideTrackingPage.tsx antes de `messageService.markAsRead`. web-client build OK (`tsc && vite build`; só warning de chunk > 500 kB). YAML do workflow validado via `js-yaml` (dep do jest) no container admin-api. O build/cache de imagens docker e o CD (`ci-cd.yml`) e o pipeline mobile/OTA continuam pendentes de definição de deploy (ver item abaixo).*
- [ ] Definir pipeline de build/assemble dos apps mobile (Android/iOS) e publicação via OTA — **condicionado ao wrapper nativo** (Passo 3.2).

### 5.3 Testes
- [x] Rodar e cobrir testes do backend (`delivery_toop-admin/backend/__tests__`).
- [x] Adicionar testes para módulos migrados da Fase 1.
- [x] Adicionar testes de UI no painel React (hoje apenas unitários de backend).

### 5.4 Monitoramento
- [x] Provisionar Prometheus + Grafana refletindo os novos módulos — o backend já expõe métricas no formato Prometheus (`middleware/metrics.ts`: `GET /metrics` com `http_requests_total` por status/método + `http_request_duration_seconds_avg/p95`; validado em runtime 8100 → 200, 7168 reqs acumuladas). Produção já tinha prometheus/grafana provisionados (`config/prometheus/prometheus.yml` scorando `admin-api:8100`, `payment-microservice:8400`, `notification-microservice:8200`, `deliveryman-microservice:8300`, redis, mongodb, postgres + `config/grafana` com dashboard overview). *(09/09/2026 — adicionados os serviços `prometheus` (9090, retenção 15d) e `grafana` (3000) ao `docker-compose.dev.yml` reutilizando o mesmo `prometheus.yml`/`provisioning` — job names do dev idênticos aos service names do stack (`admin-api`, `payment-microservice`, etc.), portanto scrape válido sem edição. YAML validado via `js-yaml`: 16 services + prometheus/grafana.)*

---

## Registro de progresso

| Data | Itens concluídos |
|------|------------------|
| 28/08/2026 | Documento criado partindo do mapeamento completo. Nenhum item de migração ainda concluído. |
| 28/08/2026 | Fase 2.1 — ativadas as telas do painel React: `Products` (rota `/products`) e `Painel de Pedidos` (rota `/painel`), com links no sidebar. Build vite OK. |
| 28/08/2026 | Fase 2.2 — unificados services em Categories/Banners/Products; "último acesso" real no Profile (backend grava `lastLogin` + `authService.me`/`refreshUser`); verificados contratos `promo` (singular, correto) e `wallet` (query driverId). Item `payments` documentado para Fase 1.9. Builds vite + tsc OK. |
| 28/08/2026 | Fase 1.1 Finance (abordagem novo/autônomo aprovada) — criados models CostCenter/TypePayment/Bank/Agency/DigitalAccount(+extract)/Chargeback, `finance.service`/`controller`/`routes` montados sob `/finance`. Endpoints validados em runtime: `/finance/balances` (agregação com dados reais) e CRUD cost-centers com auth. `tsc --noEmit` OK, admin-api reiniciado. Subordinate/split Braspag/invoices/franquia ficam p/ Fases 1.9/1.13. |
| 28/08/2026 | Fase 1.3 HelpDesk/FAQ — criados models `HelpTicket`/`TicketInteraction`/`Faq` + `helpdesk.service`/`controller`/`routes` (`/helpdesk/*`, `/faq`). Criar ticket gera interação inicial (lógica legada); consulta por protocolo retorna ticket+interações. Fluxo validado em runtime com auth (criar ticket, buscar por protocolo, listar interações). `tsc --noEmit` OK. |
| 28/08/2026 | Fase 1.5 Marketing + 1.7 Aplicativos — criados models `Campaign` e `AppCategory` + `marketing.service`/`app.service`/`controllers`/`routes` (`/marketing/campaign`, `/application/category`), CRUD autenticado com soft-delete `deletedAt`. Desacoplados do legado (`segment` string no lugar de ref `CompanySegment`/franchise; typos `dowload*` corrigidos). Criar+listar validados em runtime. `tsc --noEmit` OK. |
| 28/08/2026 | Fase 1.4 Email — criados models `EmailType`/`EmailTemplate`/`EmailVariable` + `email.service`/`controller`/`routes` (`/emails/types`, `/emails/templates`, `/emails/variables`) montados em `routes/index.ts`. CRUD autenticado; template com `populate` de type; variables somente leitura (comportamento legado). Fluxo validado em runtime: criar type + template + listar. `tsc --noEmit` OK. Registros de teste removidos. |
| 29/08/2026 | Fase 1.14 Pré-registro + 1.16 Tooling — criados models PreRegistration/DynamicPreRegister (com InputTypeShema/UploadDocPhotoShema), Popup/PopupView/Integration + services/controllers/routes (`/pre-register`, `/tools/popup`, `/tools/integrations`) montados em routes/index.ts. Fluxos validados em runtime: pré-registro público (create/list/dynamic/listViews), popup CRUD+visibility (updateViews incrementa quantityViews, listPopupApp retorna popup vigente não-visto) e integrações. Compressão de imagem e syncImage documentados como adiados (dependem de S3/Bull/API externa). `tsc --noEmit` OK. |
| 01/09/2026 | Fase 1.2 Cashback (piloto completo backend + tela painel) — criados models `CashbackCampaign`/`CashbackCustomer`/`CashbackCustomerBalance` (dessacoplados: `companies` ref Company + `allApp`, sem Franquia) + `cashback.service`/`controller`/`routes` montados sob `/cashback` (CRUD campaigns + list/balance/byMonth por customer + `used/paginator` com lookups User/Order/Campaign). CreateDefine `balance=amount`. Fluxo validado em runtime (login admin → criar campanha → listar → paginator). `tsc --noEmit` OK, admin-api reiniciado. Painel React: `cashbackService` + página `Cashbacks.jsx` (abas Campanhas / Cashback Utilizado) + rota `/cashback` + item no sidebar. `vite build` OK, frontend-react reiniciado. |
| 01/09/2026 | Fase 1.10 Cupons Compráveis (backend + tela painel) — criados models `Coupon`/`CompanyCoupon`/`CouponCustomer` (coleções `coupon`/`company_coupon`/`coupon_customer`; distinto do Promo: cupom comprável com `price`/`discountPercentage`/`limit`/`onlyFirstPurchase`/`minPriceDelivery`/datas/`allCompanies`) + `coupon.service`/`controller`/`routes` montados sob `/coupon` (CRUD + display + `highCupon` sort `-price` + `companyCoupons` aggregate com lookup + `couponCustomer`/`couponCustomerPaginator` com lookups). Removido `.populate('couponCompany')` do `highCupon` (virtual inexistente). Fluxo validado em runtime: criar 2 cupons (allCompanies e por empresa) → `paginator`/`highCupon`/`companyCoupons`/`display`/`coupon-customer-paginator` OK. `tsc --noEmit` OK. Painel React: `couponService` + página `Coupons.jsx` (abas Cupons / Utilizados) + rota `/coupons` + item sidebar "Cupons Compráveis". `vite build` OK. Model `Offer` (oferta por empresa) permanece pendente. |
| 01/09/2026 | Fase 1.12 Embalagem/Packing + Shopper (backend + tela painel) — criados models `Packing` (coleção `packing`, name/status, soft-delete) e `Shopper` (coleção `shopper`, isOnline/person ref User/company ref Company/device/token/status/appVersion, soft-delete) + `packing.service`/`shopper.service`/`controllers`/`routes` montados sob `/packing` e `/shopper` (CRUD + listByName/search com filtros). Fluxo validado em runtime: packing create/list/update/delete OK; shopper create (populates company+person)/list/paginator/search/update/delete (soft) OK. `tsc --noEmit` OK. Painel React: `packingService` + `shopperService` + páginas `Packings.jsx` (`/packings`) e `Shoppers.jsx` (`/shoppers`, com selects de empresa e usuário) + items no sidebar. `vite build` OK. Atribuição de shopper a itens de carrinho fica registrada p/ 1.8 fluxo shopping. |
| 01/09/2026 | Fase 1.13 Franquia/Franchise (backend + tela painel) — criado model `Franchise` (coleção `franchise`, sub-schemas embutidos bankData/location/settingsRace/settingsDriver/showPhoneRace/routeSettings; state/city refs SettingState/SettingCity mantidos como ObjectId) + `franchise.service`/`controller`/`routes` montados sob `/franchises` (CRUD + paginator com lookups settingCity/settingState + search + list-all + `POST /public` self-registration sem auth + `GET /config/:company`). Desacopladas integrações externas: split Pagar.me (recipient), geocodificação Google (location aceita do body) e criação automática de conta digital (registradas p/ 1.9/1.13). Fluxo validado em runtime: create (valida email duplicado) → list/paginator/search/update/delete(soft) OK; config e public-register OK; dados de teste soft-deletados. `tsc --noEmit` OK. Painel React: `franchiseService` + página `Franchises.jsx` (`/franchises`, modal-lg novo no CSS) + item no sidebar. `vite build` OK. Subordinado/split Braspag e saldo por franquia permanecem p/ 1.9. |
| 01/09/2026 | Fase 1.17 Cliente/DeliveryAddress/Guest/Person (backend) — criados models `Person` (coleção `person`, refs Franchise/Company/SettingCity; `referralCode` único sparse), `DeliveryAddress` (coleção `customer_delivery_address`, `location` Point 2dsphere, `customer` ref `User`, `main` + `isDeleted`) e `Guest` (coleção `guest`, `device` único). Services/controllers/routes: `/person/*` (CRUD, paginator pageIn/pageOut + nome/cpf, listPorNome, search phone/email/id, avatar, register-duplicates via aggregate, status "" → false, ddi decode, validação de `referralCode` no update — Indication adiada), `/delivery-address/*` (list por cliente, search customer/main, find, create/update exigem lat/lng e propagam `main` único, delete soft `isDeleted`; newTopic de cidade desacoplado), `/guest` público (create/get/update upsert por device, sem auth como no legado). Fluxo validado em runtime p/ os três módulos (criação → listas/update/delete/duplicados/edge-cases) e `tsc --noEmit` OK. Dados de teste removidos (soft-delete + limpeza direta do guest). `Customer` e `Indication` não migrados permanecem pendentes. |
| 01/09/2026 | Fase 1.11 Pedido loose-delivery (backend) — criado módulo `/loose-delivery` (POST create + GET address) sobre o domínio moderno. `create` valida empresa (CompanyModel), latitude/longitude, total e frete; persiste um `DeliveryAddress` (customer = usuário autenticado) e cria um `Order` do tipo "Entrega avulsa" (item genérico, `deliveryAddress` embutido, `paymentStatus: pending`, `typeOfVehicle` opcional validado contra MOTO/CARRO/BICICLETA — campo novo no schema de `Order`). Desacoplado do legado: sem customer/person fixos `60df17...` (usa o usuário autenticado), sem geocodificação Google Maps (`/address` valida coordenadas e explica que geocoding externo não está configurado — rotulado pendente), sem despacho automático por `/order/status` externo (entrega avulsa inicia `pending`; despacho p/ 1.23). Fluxo validado em runtime: validações de campos, criação de endereço+pedido, persistência de `typeOfVehicle`, ordem listável em `/orders`. `tsc --noEmit` OK; dados de teste removidos via mongosh. |
| 01/09/2026 | Fase 1.18 ACL / Permissões / Roles / AccessGroup / AccessFlow (backend + tela painel) — criados models `Role` (acl_roles), `Permission` (acl_permissions, ref Role), `SettingModule` (settingModule), `SettingController` (settingController, ref Module), `AccessGroup` (acessGroup, ref Module), `AccessFlow` (accessFlow) e `Group` (group) + `acl.service`/`access-group.service`/`access-flow.service`/`group.service` + controllers/routes montados em routes/index.ts: `/acl/roles` (CRUD + paginator + listPorNome), `/acl/permissions` (CRUD + paginator + list com populate roles), `/acl/users` (deriva roles/permissions do `role` moderno do User — sem isRoot/franchises/person do legado), `/settings/modules` e `/settings/controllers` (CRUD p/ árvore do AccessGroup), `/access-group` (tree com populate modules + CRUD), `/report/access-flow` (create PÚBLICO com upsert diário por device/customer/person + list agrupado por dia + statistic por `timeInterval`), `/group` (CRUD + paginator + listPorNome, delete soft `deletedAt`). Validações explícitas de campos obrigatórios retornando 400 (não 500). Fluxo validado em runtime: create/update/list/paginator/search de roles, permissions (com populate roles), modules/controllers, access-group tree, access-flow público (create + upsert + list + statistic) e group; validação de campos obrigatórios → 400; dados de teste removidos via mongosh. `tsc --noEmit` OK, admin-api reiniciado. Painel React: `aclService` + `accessGroupService` em `api.js` + página `AccessControl.jsx` (`/acl`) com abas Roles/Permissões/Grupos + item no sidebar `adminOnly` (guard por role: só admin/manager vê), espelhando o controle por permissão do legado. `vite build` OK. |
| 01/09/2026 | Fase 1.15 Monitor (dashboards tempo real, backend) — criado `monitor.service`/`controller`/`routes` montado em `/monitor`: `GET /monitor/order` (lista pedidos em tempo real sobre a coleção `orders` moderna — default filtra status ativos `$nin [delivered,cancelled]`, filtro por `status`, lookup `users` (customer, project name/email) + `companies` (name/images), paginação com lookups preservando nulls, retorna `{list,total}`), `GET /monitor/order/:orderId` (detalhe com populate company/customer/deliveryman→person; valida ObjectId e retorna 400 "Informe um pedido válido"), `GET /monitor/sales` (vendas das últimas 24h agrupadas por hora: total + finished por status, default `delivered`, escopo por empresa via `dataDay`/`status`; 24 buckets). Escopo por empresa do usuário logado (root/admin/manager sem escopo; demais restringe ao `company` do User). Desacoplado do legado: sem `customer`/`payment`/`customerDelivery`/`shopper`/`QueueDeliveryMan` (coleções legadas inexistentes no moderno — `customer` virou lookup `users`, `payment`/endereço são embutidos no `Order`, n/a fila de entregadores p/ 1.23). Fluxo validado em runtime: list default (2 pending, exclui delivered/cancelled), filtro por status, detail com populates, bad id → 400, sales 24 buckets; dados de teste removidos via mongosh. `tsc --noEmit` OK, admin-api reiniciado. |
| 02/09/2026 | Fase 1.6 Notificações + push Firebase (backend + painel; config das credenciais). Config do Firebase Web SDK salvo na raiz (`firebase.txt` — GoJaDelivery, `enduring-honor-419212`, nº `52044825836`). Backend: novas envs em `config/index.ts` (web: `FIREBASE_API_KEY`/`AUTH_DOMAIN`/`PROJECT_ID`/`STORAGE_BUCKET`/`MESSAGING_SENDER_ID`/`APP_ID`/`DATABASE_URL`; admin p/ Service Account: `FIREBASE_ADMIN_ENABLED`/`PROJECT_ID`/`CLIENT_EMAIL`/`PRIVATE_KEY`/`DATABASE_URL`) + `config/firebase.config.ts` (referência tipada da config web + flag `isFirebaseAdminEnabled`) + `services/firebaseAdmin.service.ts` (inicializa o app do Firebase Admin a partir do Service Account, **degradando graciosamente para null quando não há chave**) + `services/notification-topic.service.ts` (create — inscreve/desinscreve token em tópicos `{name}[_{value}]`, e send — push por condição `'{topic}' in topics && application_root && franchise_{}`). `notification.service` ganhou create/update/remove/createAndSend; rotas: `/notifications` (POST create, GET list, GET/:id, PUT/:id, DELETE/:id) + `POST /notifications/send` + `POST /v2/notification-topic` e `POST /v2/notification-topic/send` (autenticadas). Envio direto via `firebase-admin` (não proxy p/ microserviço 8200). Instalado `firebase-admin@11` no backend. Fluxo validado em runtime (sem Service Account): create/update/delete/list de notificações OK; `send`/`createAndSend`/topic degradam com `{success:false, error:"FCM não configurado: adicione um Service Account em FIREBASE_ADMIN_*"}` e `createAndSend` ainda persiste a notificação; dados de teste removidos. Painel React: instalado `firebase@10` web SDK + `src/services/firebase.js` (init a partir de `VITE_FIREBASE_*`, `requestPushToken` e `onForegroundMessage` com degrade) + `.env.example` do frontend documenta a config web + VAPID; `notificationService` estendido (create/update/delete/createAndSend/subscribeTopic/sendToTopic). `vite build` e `tsc --noEmit` OK. Pendente: Service Account (Console Firebase → Project settings → Service accounts) para envio real; VAPID key; tela dedicada de criar/enviar notificação no painel. |
| 03/09/2026 | Fase 1.19 Configurações de domínio (backend) — criados models SettingState (settingState: name/uf/country), SettingCity (settingCity: name, state ref SettingState, deletedAt, lat/lng), TimeZone (timeZone: offset -14..13/zone/name), SettingTypesUsers (settingTypesUsers: name/status/deletedAt), AppVersion (appVersion: name/version/platform ios|android/status/deletedAt), SettingBrazilianBanks (settingBrazilianBanks: compe/ispb/document/long_name/short_name/network/type/pix_type/url), GlobalSettings (globalSettings: serviceCharge) e AddressCity (city_br — catálogo IBGE p/ normalizar lat/lng) + domain-settings.service/controller/outes. Rotas autenticadas montadas em routes/index.ts: /setting/state (list/:id, listPorNome, create, update, delete hard), /setting/city (list por state/name/hasFranchise com populate state + franchise, paginator pageIn/pageOut, normalize-cities a partir do city_br quando disponível, create/update com populate, delete soft deletedAt), /setting/types-users (list + paginator + create/update com normalização de status vazio→false + delete soft), /setting/app-versions (list, create com único ativo por plataforma, check → forceUpdate {forceUpdate,version,platform}), /setting/timezone (list sort offset), /setting/countries (estático Brasil/Portugal/Angola com reordenação por language pt/pt-PT/pt-AO), /setting/app/:franchise (config do app da franquia: serviceDefault/languageDefault/coin/emergencyPhone sobre a coleção moderne ranchise), /v2/setting/brazilian-bank (list com filtro por nome, projeta compe/long_name/short_name) e /global/settings (list do doc único globalSettings). Controller/Module já portados na 1.18. Fluxo validado em runtime: state create/list → city create (populate state)/list/paginator → types-users create (status normalizado)/list → app-version create + check (forceUpdate true) → timezone list, countries (default + pt reordena), brazilian-bank list, app/:franchise com franquia real (delivery/R$/pt-BR/190), global/settings; dados de teste removidos via mongosh. 	sc --noEmit OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.20 Cat�logo de acess�rios + complementos (backend) � criados models AccessoriesCategory (accessoriesCategory: name/company/isPaused/position), AccessoriesProduct (accessoriesProduct: images/name/category ref/description/shortDescription/price/pricePromotion/percentualDiscount/codPdv/isPaused/position/amountPeople ONE|TWO|THREE|FOUR, text index p/ busca), AccessoriesProductComplement (accessoriesProductComplement: name/amountMax/amountMin/isRequired/isQuantified/product ref/isPaused/position/company) e AccessoriesProductComplementItem (accessoriesProductComplementItem: name/codPdv/description/price/isPaused/accessoriesProductComplement ref/company). accessories.service/controller/routes montados em routes/index.ts: /v2/accessories/category (by-company com lookup de produtos, list-by-name, create, update, delete), /v2/accessories/product (list com filtro category+isPaused+text search, list-group por categoria p/ app � compat 1.5.9 products / atual data por header appVersion, get, create com complements+items aninhados e array de imagens via file[].url/url, update com upsert+remo��o de complements/items, sort por position, delete em cascata complement+items, multer/S3 desacoplado), /v2/accessories/product-complement (list por :productId com lookup items, create) e /v2/accessories/product-complement-item (list/create/update/delete) + /food/product-complement/:productId (list p/ app). Tratamento de status vazio?false (isPaused) e valida��es de campos obrigat�rios ? 400 (AppError) em vez de 500. Fluxo validado em runtime: by-company (categoria+produto), list-group (keys title/key/products e com appVersion title/key/data), list-by-name, get/create/update de produto (upsert de complemento e remo��o do antigo), complement-list (com items), item list, food product-complement, sort, delete em cascata product?complement+items e category; company inv�lida ? 400; dados de teste removidos. 	sc --noEmit OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.21 Alerta de produto / prompt (backend) — criado model CustomerAlertProduct (coleção customer_alert_product: customer/franchise ref, listIds/identificationDate/period alertData+list, status) + customer-alert-product.service/controller/routes montado em /v2/customer-alert-product/alert-product: list (com lookup customer/franchise e filtro por lista/status/paginação), create (com listIds p/ envio), update, report (contagens por tipo/lista). Desacoplado do legado (sem warning/legacy prisma). Validação runtime em 03/09 (lista vazia 200, LIST_COUNT com dados → listagem). `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.22 Chat por pedido (backend) — criado model ChatMessage (coleção chatMessage: message, type text|audio|image|text_alert, dataType, urlFile, shoppingCart ref Cart, person/personSend customer|shopper|deliveryMan, personId/personSendId, flag, read, readSend, order_number; timestamps + índice composto) + chat-message.service/controller/routes: /chat (list admin com $or por person/personSend, create, no-read count?personId, updateRead) e /v1/front/chat/:cartId (list aggregate $project customer/shopper/deliveryMan + $lookup users, preservando nulls) + POST (create) p/ front. Fluxo validado em runtime: list vazio → create → list com mensagem → noRead total 1 → read; registrado em routes/index.ts com authenticate. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.23 Entregador: fila / corridas / online / cadastro (backend) — criados models QueueDeliveryMan (queueDeliveryMan; status WAIT|PROCESS|FINISH|NOT_FOUND_DELIVERYMAN; arrays tipados ObjectId), DeliveryManOnline (deliveryManOnline), RaceCanceled (raceCanceled), RaceHistory (raceHistory; status ACCEPTED|REFUSED|CANCELED) e RegisterDeliveryMan (registerDeliveryMan; status PENDING|ANALYZE|DECLINED|APPROVED; vehicleType CAR|MOTORCYCLE|BIKE) + delivery-dispatch.service/controller (montado em /deliveryMan e /delivery-man) e register-deliveryman.routes (montado em /register-deliveryman). Rotas: queue (list/statusOne/updateStatus/update/updateReceived/haveActive/back-to-queue — reseta Order p/ pending e desvincula deliveryman), online (create/offline com total em minutos/online-last-week aggregate mediaTime), race (canceled/list/history com lookup Order p/ companyName/paymentPriceDelivery), price (devolve {payment:{priceDelivery}} do Order), register (create upsert por cpf/list/paginator/updateStatus). Fluxo validado em runtime: queue list vazio, online-last-week {list,totalMedia}, register create por cpf (persiste), list vazio; /pre-register já coberto pelo módulo moderno. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.24 Busca /v1/search (backend) — criado search.service/controller/routes montado em /v1/search com authenticate: GET /company-products (valida searchText e companyType; filtro Company por active + $or name regex/tags/category SEM $text — $text+regex no mesmo $or causava Mongo "No query solutions"/291; attachProduct em Product+AccessoriesProduct) e GET /segment/company-products (sem companyType; limite 100 companies). Fluxo validado em runtime: company-products e segment retornam 200 (vazio sem dados correspondentes); 500 do $text corrigido. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.25 Slider / Tabloid / Tips / site da empresa (backend) — criados models Slider (slider; type slider|banner|driver, segment, category delivery|service, vizualizations, priorities, images, deletedAt, status/companyClick bool), Tabloid (tabloid), Tip (tip), TipDeliveryMan (tipDeliveryMan) e CompanySite (site; company/status/name/domain/URLlogo/email/about/slider/deletedAt) + site-content.service/controller/routes montados em routes/index.ts com authenticate: /slider (create com extractImages file/url + coerceBool status/companyClick; list/paginator com companyScope(userId); update; remove soft delete), /tabloid (create/list/update/remove hard delete via findByIdAndDelete), /Tip (create/list/search/remove), /TipDeliveryMan (create/list/remove), /company/site (create/list/paginator com companyScope; update/remove soft delete). Fluxo validado em runtime: Tip create, register-deliveryman create, site create e todos os lists 200; dados de teste removidos via mongosh e coleções confirmadas vazias. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.26 Log / auditoria (backend) — criado model Log (log: typeSystem MOBILE|WEB|BACKEND|UNDEFINED; typeLog WARN|ERROR|ALERT|SUCCESS|UNDEFINED; description Mixed; category; originError; path; error Mixed; method; type error; level; origin backend|frontend; request Mixed; timestamps + índices createdAt/typeLog/category), log.service (create/list por id/list-all/paginator com pageIn/pageOut e errorLog helper) + log.controller/routes, montado em `/log` com `authenticate`. Validação em runtime: `/log/create` → 201; `/log/paginator?pageIn=0&pageOut=10` → 200; `/log/:id` → 200; `/log/:id` com id inválido → 400. Dados de teste removidos e `tsc --noEmit` OK (admin-api reiniciado). |
| 03/09/2026 | Fase 1.27 Mobility — lacunas (backend) — criados ~23 models: DocumentType (documentType), PeakHour (peakHour), SupportSubject (supportSubject), VehicleDocuments (vehicleDocuments), Service (service), Passenger (passenger), FavoritePlaces (favoritePlaces), FavoriteDrivers (favoriteDrivers), ChosenDestinations (driverChosenDestinations), QrCodeDriver (qrCodeDriver), Indication (indication), MobilityNotification (mobilityNotification), PushNotification (pushNotification), ChatRace (chatRace), VoucherDiscount (voucherDiscount), TypePaymentService (typePaymentService), PriceCalculation (priceCalculation, model-only), MobilitySlider (mob_slider), Evaluation (evaluation), TravelBookingInfo (travelBookingInfo), + services/controllers. Rotas consolidadas + maps montadas em routes/index.ts com `authenticate` sob `/v1/mobility`: exports em mobility-crud (documenttypes/peakhours/supportsubjects/vehicle-documents), mobility-service-passenger (services + service-details, passengers), mobility-app (favorite-place, driver/favorite, chosen-destinations, qrcode, indication), mobility-extras (notifications, push-notification, topic, message, extract, type-payment-service, discount, slider), mobility-report-extra (evaluation, booking/schedule, report adm races/passenger/driver + active monitoring) e `/v1/mobility/maps` (maps.service com Google Maps via env GOOGLE_MAPS + fallback haversine; sem chave degrada com AppError 400). Config adicionou `GOOGLE_MAPS` (default ''). Validação em runtime: create+list/paginator de documentType, peakHour, supportSubject, service (+list-front/available), passenger; favorite-place, chosen-destinations, slider, type-payment, discount, indication (exige personReceive → 400 sem, 200 com), message, evaluation, report adm (3) + active monitoring (200), maps → 400 (degradação esperada sem GOOGLE_MAPS). Dados de teste removidos via mongosh e coleções confirmadas vazias. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.28 Wallet (admin) — vouchers / recarga (backend) — criados models `Voucher` (voucher: name/status/value/code/limit/used[] Mixed/dateInit/dateFinish/franchise ref/deletedAt, timestamps + toJSON; igual ao legado Finance/DigitalAccounts/VoucherModel) e `PassengerWallet` (passenger_wallet: holder ref Passenger, balance, transactions[] credit-ledger — ledger próprio escolhido pelo usuário para a recarga, sem tocar no DigitalAccount do lado delivery/finanças nem no Wallet de motorista). `voucher.service` (create valida code/value/limit/datas + código duplicado por franquia; paginator escopo por franquia com filtro deletedAt e pageIn/pageOut; update com normalização de status vazio→false + upsert; remove soft delete findByIdAndUpdate deletedAt; validate valida passageiro+franquia → busca voucher ativo por code+franquia → janela vigência dateInit/dateFinish → limite used.length>=limit → já-usado → credita o PassengerWallet credit e registra passenger/usedAt em used[]) + `passenger-wallet.service` (getOrCreate/credit/balance). Controller + rotas montadas em routes/index.ts com `authenticate`: `/v2/vouchers` (POST /, GET /paginator, PUT /:id, DELETE /:id) e `/v2/wallet` (GET /balance?passenger, POST /add-balance-voucher). Desacoplado do legado: sem middlewares checkFranchises (franchise vai no body/query, validado como ObjectId) e sem o subsystem legacy DigitalAccounts/BankTransactions/AccountBalance (a recarga usa o PassengerWallet com transação credit + reference=Voucher). Validação em runtime: create 201 + duplicado 400 + validações de campo 400; paginator 200 (escopo franquia, após delete total 0); add-balance-voucher 201 (credita balance 25) + duplicado "Você já usou" 400 + código inválido 400 + passageiro inválido 400; balance 200 (balance 25 + transação credit reference Voucher); update 200 (valor 30); delete soft 200 e voucher deixa de aparecer no paginator e é rejeitado no validate (400 Código inválido). Dados de teste (franchise/person/passenger/voucher/passenger_wallet) removidos via mongosh e coleções confirmadas vazias. `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 1.29 Catálogo ECBR / Image Bank (backend) — criados models EcbrProductDepartment (coleção ecbr_ProductDepartment: name/base/ref_reference_ECBR barcode unique, keywords, images, departments ObjectId[], weight, description, copyright, status, audited, companyAdded) e ImageBank (coleção imageBank: barcode, productName/productAccent, keywords, description String[], packing ref, packingAmount, images, category String[], brand). ecbr-image-bank.service (list com filtros page/limit/barcode/name/isCopyright/isImages/isDepartments/status/audited/company + lookup department com projeção name, retorna {response,pagination,total}; generateCode ECBR00000001+leftPad; listByBarcode 404 se vazio; create valida nome/barcode/descrição/keywords/departments + monta images de file[].url/url + departments ObjectId + barcode duplicado -> 400 + propaga para products bruto se audited; update com normalização de status + upsert + propaga se audited; sync itera ecbr_ProductDepartment e updateMany em products bruto) + image-bank.service (create/list por barcode regex com populate packing, listPorNome productAccent lowercase+insensitive, listPorCategory regex, create/register, update, remove findByIdAndDelete). Controller + rotas montadas em routes/index.ts: /v2/ecbr-image-bank com uthenticate (GET /, GET /generate/code/ecbr, GET /barcode/:barcode, GET /sync, POST /, PUT /update/:id) e /imageBank público (GET /list/:barcode/:pageIn/:size, GET /listPorNome/:nome/:pageIn/:size, GET /listPorCategory/:category/:pageIn/:size, POST /create, POST /register, PUT /update/:id, DELETE /delete/:id). Validação em runtime: ecbr generate-code ECBR00000001/02, list vazio, create sem nome 400, create válido 200, barcode duplicado 400, list por barcode/name (sem acento case-insensitive), barcode inexistente 404, sync 200, update 200; imageBank list 200 (barcode/nome/categoria case-insensitive), register 200, update 200, delete 200 (id inválido 400). Upload legacy S3 desacoplado (imagens via url no body). Dados de teste removidos via mongosh e coleções confirmadas vazias. 	sc --noEmit OK, admin-api reiniciado. |

| 03/09/2026 | Fase 1.30 Endereço / Company público (backend) — criados models StateBr (coleção state_br: codigo_uf/uf/nome/latitude/longitude, índice nome) e CompanyDelivery (coleção company_delivery: isOpen/isManual/company ref/typePayments/mdr/fee/distance sub-schema/max_distance/max_distance_withdraw/min_purchase/max_amount_items/time_to_call_delivery/own_delivery/online_delivery/has_split/withdrawMarket/mediaRating/totalRating/deletedAt). address.service (listState → StateBr.sort({nome:1}); listCity → AddressCityModel/city_br com filtro codigo_uf Number + $text search nome, sort {nome:1}) + public-company.service (registerCompany: validações franchise ObjectId/file/lat/lng/name → AppError 400, extractImages de file[].url/url, normalizeCategory NFD+lowercase, cria Group moderno, insere company bruto na coleção companies com franchise/name/approved:false/status:false/groups/images/category/location GeoJSON, cria CompanyDelivery fee 15/min_purchase 20/max_amount_items 20/distance [{0,10000,10,25}] e atualiza company.companyDelivery; listLocation: aggregate $match status:true + lookup company_delivery + $unwind preserveNullAndEmptyArrays + filtro opcional companyDelivery.isOpen). Controller + rotas públicas (sem auth, como no legado) montadas em routes/index.ts: /v2/address (GET /state, GET /city) e /v2/company (POST /register-company, GET /location). Company moderno (strict) não alterado — fluxo público usa a coleção bruta companies. getAddress (geocode Google/LooseDelivery) fora do escopo da 1.30. Validação em runtime: /v2/address state/city 200 (vazios); register-company sem franchise 400 "Informe a Franquia", franchise inválido 400, sem file 400 "Imagens inválidas", sem lat/lng 400, válido 200 (cria Group + company bruta + company_delivery com fee 15); location 200 count=1 (após status:true), ?isOpen=true count=0 (delivery isOpen:false), ?isOpen=false count=1. Dados de teste removidos via mongosh (companies/group/company_delivery 0). `tsc --noEmit` OK, admin-api reiniciado. |

| 03/09/2026 | Fase 1.31 Twilio — alerta por chamada + verificação OTP (backend) — instalado `twilio` no backend + envs em config/index.ts `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_SERVICE_SID` (todas `default('')`, degradação graciosa). Criado model Twilio (coleção twilio: name/email/phone/note/active, timestamps — igual ao legado `_legacy_src/models/twilio.js`) + twilio.service com getClient() que degrada com AppError 400 quando sem TWILIO_ACCOUNT_SID/AUTH_TOKEN e getServiceSid() quando sem TWILIO_SERVICE_SID; create (persiste o alerta, como o legado, validando name/email/phone → 400), sendVerificationCode(phone) via client.verify.services(serviceSid).verifications.create (channel sms → {message}) e checkVerificationCode(phone, code) via verificationChecks.create → {valid: status==='approved', status}. Controller + rotas montadas em routes/index.ts em /twilio: POST / (create, com authenticate, como no legado), POST /send-code e POST /verify-code (públicos para verificação de número do mobile). Desacoplado do legado: sem o client criado no import da rota nem o número hardcoded +13028658482 do fluxo de chamadas de novo pedido (registra-se o alerta; a chamada TwiML fica para o fluxo de ordem, fora do escopo). Validação em runtime: send-code sem phone 400 "Informe o telefone", com phone 400 degradado "Twilio não configurado", verify-code sem code 400 "Informe o código de verificação", com code 400 degradado; create sem auth 401, sem name/email/phone 400, válido 201 (persiste). Dados de teste removidos via mongosh (coleção twilio vazia). `tsc --noEmit` OK, admin-api reiniciado. |
| 03/09/2026 | Fase 2.3 Finance — tela no painel React — corrigido o proxy do vite dev (porta 4202): antes o `proxyRoutes` era uma lista fixa de ~22 prefixos antigos que NÃO cobria os módulos migrados (404 em `/franchises`, `/cashback`, `/finance` — telas existentes quebradas em dev); reescrito `frontend-react/vite.config.js` como proxy catch-all `'/'` com `bypass` (retorna o index.html para navegação SPA e deixa o vite servir assets `/src|node_modules|@vite|@id|.vite` + extensões estáticas) e target `http://admin-api:8100` (hostname do backend na rede docker — env `VITE_API_PROXY` do container chegava vazio). Validado via 4202: `/health` 200, `/v2/company/location` 200 (público), `/finance/balances` 401 sem auth e 200 com token (JSON real). Criado `financeService` em `src/services/api.js` (listBalances/getCompanyBalance, CRUD cost-centers/type-payments/banks/agencies/digital-accounts/chargebacks + getDigitalAccountBalance/moveDigitalAccount) e página `src/pages/Finance.jsx` com abas: Balanços (cards resumo + tabela por empresa), Centros de Custo, Tipos de Pagamento, Bancos, Agências (select de banco), Contas Digitais (select agência/empresa) e Chargebacks (status). Rota `/finance` em `App.jsx` + item "Financeiro" no `Sidebar.jsx`. `vite build` OK (1530 módulos). Validação runtime via proxy: GET de todas as abas 200; CREATE cost-center/tipo-pagamento/banco/agência/conta digital/move (crédito 150, saldo 150) OK. Dados de teste removidos via mongosh (costcenters/typepayments/banks/agencies/digitalaccounts/chargebacks = 0). Subordinados e Invoices ficam p/ Fase 1.9. |
| 03/09/2026 | Fase 2.3 Telas restantes do painel React (10 telas) — adicionados em `src/services/api.js`: `domainService` (states/cities/types-users/app-versions/timezones/brazilian-banks/global-settings), `helpdeskService` (tickets CRUD/byProtocol/interactions CRUD), `faqService`, `emailService` (types/templates/variables), `marketingService` (campaign CRUD), `logService` (paginator), `voucherService` (paginator pageIn/pageOut+franchise, create/update/remove/validate), `mobilityService` (documenttypes/peakhours/supportsubjects CRUD), `monitorService` (order/order-detail/sales), `toolsService` (popup/integrations) e `chatService` (list-by-cart/create/no-read/set-read). Criadas páginas: `DomainSettings.jsx` (abas Estados/Cidades/Tipos de Usuário/Versões App/Bancos/Fusos/Config Global), `HelpDesk.jsx` (abas Tickets com protocolo tickedId + interações por protocolo / FAQ), `Email.jsx` (abas Tipos/Templates/Variáveis, campos alinhados ao model: key+name e subject/body/type), `Marketing.jsx` (campaign com nome/veículo/datas/downloads/note/images), `Log.jsx` (leitura), `Vouchers.jsx` (CRUD por franquia com código/valor/limite/vigência), `Mobility.jsx` (Tipos de Documento/Horários de Pico/Assuntos de Suporte) e `Monitor.jsx` (abas NOC pedidos+vendas tempo real, Chat por cartId com não-lidas, Tools popups+integrações). Rotas registradas em `App.jsx` e itens no `Sidebar.jsx` (Config. Domínio, HelpDesk/FAQ, E-mails, Marketing, Vouchers, Mobility, Monitor/Tools, Log/Auditoria). `DataTable.jsx` ganhou suporte a `onView` (botão visualizar). CRUDs alinhados aos models reais: HelpTicket (tickedId), EmailType (key/name), Campaign (name/disseminationVehicle/initialDate/...), Voucher (code/value/limit/dateInit/dateFinish/franchise). `vite build` OK (1538 módulos). Validação runtime via proxy 4202: GET de todos os endpoints 200; CREATE state/faq/email-type/campaign (após ajuste de campos)/document-type/ticket validados 201-200; dados de teste removidos via mongosh (states/faqs/emailtypes/campaigns/helptickets/documenttypes = 0). Supermercado, Pré-registro, Acessórios/complementos e Busca ficam para Fase 2.3/2.4 seguintes. |
| 04/09/2026 | Fase 2.3 Telas restantes do painel React (4 telas) - adicionados em `src/services/api.js`: `supermarketService` (tabloid list/create/delete + ecbr list/create/update/generate-code/sync), `preRegisterService` (pre-register paginator/update + register-deliveryman paginator/updateStatus), `accessoriesService` (category by-company/list/create/update/delete; product list/get/create/update/delete; complements by-product + create) e `searchService` (company-products com searchText+companyType + segment). Criadas paginas: `Supermarket.jsx` (abas Tabloid - listar/criar/excluir; ECBR Image Bank - listar por codigo/nome, criar (file.url obrigatorio p/ create, departments ObjectId[], keywords, audited propaga), editar), `PreRegister.jsx` (abas Pre-registro - paginator por status/busca + Aprovar APPROVED/Recusar DECLINED/Reenviar RESENT via PUT /pre-register/:id; Cadastro Entregador - paginator + update status via /register-deliveryman/status/:id), `Accessories.jsx` (seletor de empresa via companyService.getCompanies; abas Categorias CRUD e Produtos CRUD com categoria/pessoas/preco/imagens + modal de Complementos por produto create) e `Search.jsx` (form searchText+companyType -> GET /v1/search/company-products; exibe empresas com produtos aninhados). Rotas registradas em `App.jsx` (/supermarket, /pre-register, /accessories, /search) + itens no `Sidebar.jsx`. `vite build` OK (1542 modulos). Validacao runtime via proxy 4202 com token: GET de todos os endpoints 200 (tabloid/list, ecbr paginator, pre-register paginator, register-deliveryman paginator, accessories category by-company); CREATE tabloid, pre-register + approve (PUT status APPROVED), accessories category, product e complement, ecbr (apos ajuste p/ `file:{url}` - antes 400 Imagens invalidas) e search - todos 200/201; dados de teste removidos via mongosh (tabloid/ecbr/preregistration/accessoryCategory/accessoryProduct/accessoryComplement = 0). Schedules/invoices/loose-delivery nao possuem CRUDs dedicados no painel (anotado). |
| 04/09/2026 | Forma real de trabalho dos apps (Passo 3): o produto roda como aplicacoes web servidas por Vite na stack dev - Cliente delivery_toop-web-client em http://localhost:4200 (toop-web-client-dev), Entregador delivery_toop-deliveryman em http://localhost:4204 (toop-deliveryman-app-dev), Loja delivery_toop-store em http://localhost:4203 (toop-store-dev), Landpage delivery_toop-landpage em http://localhost:4205, Painel admin frontend-react em http://localhost:4202 - todos mapeados no docker-compose.dev.yml (web-client 4200, deliveryman-app 4204). NAO ha APK/React Native no caminho ativo: os diretorios mobile/* (RN) sao codigo paralelo/legado fora do fluxo web. Por isso o modulo nativo OTAStorage (saveBundle/loadBundle) e o bundle de teste NAO foram implementados agora - ficam como infra opcional (Passo 3.2), condicionados a um eventual wrapper nativo (WebView) no fim. Esclarecido o auxilio de porta 8500: no stack dev o deliveryman-microservice usa 8300 (docker-compose.dev.yml) e o ota-server usa 8500 - sem conflito no stack ativo; o 8500:8500 existe apenas no compose orfao/standalone delivery_toop-microservice_deliveryman/docker-compose.yml (find_delivery, node:10). Validei no container toop-deliveryman-microservice-dev (porta 8300). Comitado. |
| 04/09/2026 | Passo 3.1 confirmado concluido (apps web ja alinhadas ao backend moderno da Fase 1). Confirmado em runtime que as 3 apps com API (cliente 4200, loja 4203, entregador 4204) consomem o admin-api:8100 via proxy vite /api (rewrite remove o prefixo /api): login e cadastro reais (POST /auth, /auth/register, /auth/register-store, /auth/register-deliveryman, /auth/refresh, /auth/me) retornando {user,token,refreshToken} identicos ao backend moderno; GET /orders?customer=<ObjectId valido> 200 (500 so com ObjectId invalido); socket.io no mesmo httpServer:8100 (server.ts) cobre os eventos booking:*; todas as familias de endpoints usadas pelas apps (cart/orders/companies/products/categories/banners/reviews/bookings/promo/users/messages/deliverymen/drivers/wallet/settings/upload/branches/stock-*) existem montadas em backend/src/routes/index.ts com os mesmos sub-caminhos. Landpage 4205 e visual sem API. Fix do HMR tambem comitado (vite.config.js frontend-react adiciona /@react-refresh a isAsset). Cliente de teste registrado e removido via mongosh apos a validacao. Comitado. |
| 04/09/2026 | Passo 4 documentado + Passo 5.1 auditoria de seguranca. Passo 4: a rota /tools/integrations + model Integration (JM_Diamante/RpInfo/Viva_Sistemas) JA existem no backend moderno (Fase 1.14/1.16) - marcado [x]; os conectores ERP (Pratiko/newOrder, RpInfo, Viva) e o gateway apiIntegrationsURL ficam ADIADOS por dependerem de credenciais/endpoints externos (codigo legado em _legacy_src/services/runProcessIntegration e frontend/.../services/integration; gateway nao existe no stack moderno; delivery_toop-integrations vazio). Sync de imagem / v1/all/unsynchronized-departments tambem adiado (script syncDepartment e import pontual de planilha com IDs/URLs hardcoded). Passo 5.1: auditado .gitignore (bloqueia *.pem/*.key/*.crt/*.p12/*.keystore/firebase-adminsdk*.json; git ls-files nao lista sensiveis). CORRECAO IMPORTANTE: o item "(Confirmado) chaves nao estao no git" estava errado - haviam DUAS chaves RSA de JWT reais hardcoded em docker-compose.dev.yml (microservicos deliveryman e payment). Removidas e trocadas por env vars ${JWT_PRIVATE_KEY_DELIVERYMAN_MICRO}/${JWT_PRIVATE_KEY_PAYMENT_MICRO} (documentadas no .env.example). ACAO PENDENTE do usuario: ROTACIONAR as chaves JWT (ja estiveram no historico). Nenhum firebase service account/private_key real no historico (git grep em todas as revs so acha placeholders). firebase.txt so tem config web publica. Comitado. |
| 04/09/2026 | Fase 1.8 Fluxo shopping/supermercado - parte autossuficiente (backend). Criados models `ShoppingDepartment` (colecao department: franchise/company/name unique/suggesteds/showInApp/status/deletedAt - sem SuggestedsModel, string SGBD taxon/order), `ShoppingDepartmentMobile` (departmentmobile, sem suggesteds), `ShoppingSchedule` (schedule: dayWeek SUNDAY..SATURDAY, startHour/endHour Number, type BOTH|DELIVERY|WITHDRAWAL) e `ShoppingPaymentMethod` (shoppingPaymentMethod: customer/isMain/flag/cartNumber/valid/gateway BRASPAG|PAGARME|IUGU/cardToken/isDeleted) + `SortDepartment` (sortDepartment: department/company/order). Services (padrao moderno: AppError + ok/next; sem moment - Date nativo UTC-3 no Schedule TODAY; sem escrever LogModel; despacho legado `updateIntegration` removido; corrigido bug legado de `company` indefinido no create mobile e update com `upsert:true` que recriava doc): shopping-department.service (list por franchise/company+tambem sem franchise, barcode via lookup na EcbrProductDepartment, paginator com aggregate + $lookup sortDepartment p/ popular sort em cada dept, create/update/softDelete), shopping-department-mobile.service, shopping-schedule.service (all: agregacao por empresa com $lookup companies e agrupamento por dia; have; list com meta TODAY UTC-3; create/update/updateType/softDelete) e shopping-payment-method.service (list/update p/ isMain unico/softDelete; create = stub 501 "depende do gateway de pagamentos (Braspag/PagarMe/Iugu) - Passo 1.9"). Controllers + rotas montadas em routes/index.ts com authenticate: /shopping/department, /shopping/departmentmobile, /shopping/schedule, /shopping/payment-method - paths legados preservados (/paginator, /all, /have/:company, /:company?type=, /type, /:id). Typecheck `tsc --noEmit` OK (fix TS2339 `franchise` em Company com cast). admin-api reiniciado (ts-node-dev nao pega arquivos novos). Validacao runtime 8100: department create/list-all/paginator(com sort)/update/delete, departmentmobile create(company do body)/list/paginator/update/delete, schedule create/all(company name via $lookup)/have/update/updateType/delete, payment-method list (0) / create 501 (mensagem 1.9) / update 404 / delete 404 - todos OK. Dados de teste removidos via mongosh (department/departmentmobile/schedule = 0). Invoice + fluxo cart/order/frete + criacao de metodo de pagamento com tokenizacao ficam ADIADOS p/ 1.9 (gateways externos). Comitado. |
| 06/09/2026 | Fase 1.10 Model Offer (backend) - criado model `Offer` (colecao `offer`, mesma do legado: name/groupCompany/cost/status/description requeridos + images; timestamps; toJSON sem __v) + offer.service/controller/routes montados em routes/index.ts com authenticate: /offer/list, /offer/register, /offer/create, /offer/update/:id, /offer/delete/:id (paths legados). Create exige file[].url/url (senão AppError 400 "Imagens inválidas"), normaliza status vazio/null→false e extrai `images` do body (S3 desacoplado, como no restante da Fase 1); register persiste body cru (comportamento legado); update normaliza status, exige file e faz findOneAndUpdate (sem o upsert legado que criava doc novo com id inexistente); delete hard via findOneAndDelete. Validado em runtime: create (status ""→false, images preenchidas), register, list (2), update (status "true"→true, images substituídas), delete, list final 0; invalid id → 400 "Informe uma oferta válida", create sem file → 400 "Imagens inválidas", sem auth → 401. `tsc --noEmit` OK, admin-api reiniciado. Dados de teste removidos via API (count 0). Observacao: o legado nao tem ref company no Offer (usa `groupCompany` boolean distinto de Promo/Coupon); integracao de exibicao por empresa no app fica a cargo do fluxo de catalogo (nao e o foco desta fase). Comitado. |
| 06/09/2026 | Fase 2.3 Tela Clientes (painel React) - `customerService` em `src/services/api.js` (paginator page/limit, list/:id, listPorNome, search-customer, search-person-customer, create, update, remove). Criada `src/pages/Customers.jsx` (rota /customers + item "Clientes" no Sidebar com ícone UserSquare), abas: (1) Clientes - paginator page 0-based/limit 15 com total, paginação Prev/Próximo, colunas Nome (person.name via $lookup)/Telefone (ddi+phone)/E-mail/App/Criado, Ações Ver (modal detalhe via list/:id: person, sku, appVersion, device, instanceIdToken, termos, favoritos, rating, datas) e Excluir (confirm + hard delete); (2) Busca - por nome da pessoa (search-person-customer, mín. 2 chars, mostra "Cadastrado/Sem cadastro" + Ver quando possui customer) e por e-mail/telefone (search-customer). `vite build` OK (1544 módulos). Validado em runtime: person status:true + customer create (sku único), paginator total 1 com person.name, list/:id populado, search-customer por email (1), search-person-customer por nome (1 com customerId), delete customer + person, total 0. Observacao: search-person-customer filtra person status:true + deletedAt ausente (condição do legado) - person criado via API sem status fica fora da busca; no fluxo real o signup do app grava status true. Dados de teste removidos (person Cliente Painel x3 e customers via mongosh). Comitado. |
| 06/09/2026 | Fase 2.3 Tela Indicações (painel React) + paginator no backend - adicionado ao `indication.service` o método `paginator(query)` (page default 0, limit default 20; aggregate com $lookup em person (name/email/image) para `person` e `personReceive`, $sort createdAt desc, $skip/$limit; total via estimatedDocumentCount; retorna {list,total}) + controller `paginator` + rota GET /v1/mobility/indication/paginator em indication.routes.ts (authenticate; mantido GET / com filtro personReceive do app). `tsc --noEmit` OK, admin-api reiniciado. Validado em runtime: paginator 200 com 0 docs; 401 sem token; após criar person A (referralCode REFUI1) + B e atualizar B com code REFUI1 → paginator total 1 (person=Indicado B, personReceive=Indicador A, code REFUI1, total 20) e list por personReceive count 1. Dados de teste removidos via mongosh (indication 1, persons 2). No painel: `indicationService` em `src/services/api.js` (paginator + listByPerson) e página `Indications.jsx` (rota /indications + item "Indicações" no Sidebar com ícone Gift): tabela com Indicado/Recebedor/Código/Valor R$/Resgatado/Ativo/Data + paginação Prev/Próximo com total + Atualizar. `vite build` OK (1544 módulos). Comitado. |
| 06/09/2026 | Fase 2.3 Tela Pessoas (painel React) - `personService` em `src/services/api.js` (paginator pageIn/pageOut/name, get/:id, update, remove soft, registerDuplicates type/field). Criada `src/pages/Persons.jsx` (rota /persons + item "Pessoas" no Sidebar com ícone UserRound), abas: (1) Pessoas - paginator page 0-based/limit 20 com busca por nome (regex case-insensitive) + paginação Prev/Próximo/Total + colunas Nome/Telefone (ddi+phone)/E-mail/Código de indicação (referralCode)/Status/Criado + Ver (modal com todos os campos da person: cidade/company/franchise, devices, birthdate, timeZone, status, referralCode, timestamps + botão Excluir soft) ; (2) Duplicados - select Telefone/E-mail chamando /person/register-duplicates?type=person&field=... (agregação $group do legado; grupos de `_id` nulo são filtrados na tela, ex.: telefone ausente). `vite build` OK. Validado em runtime: 2 persons criadas com mesmo telefone → paginator total 2, busca por nome total 2, register-duplicates phone (grupo 11990001111 com nmPhone 4) e email (grupos por dominio de teste), delete soft → busca final 0; dados de teste removidos via mongosh (leftover 0). Sem mudanças no backend (rotas /person/* já existiam da Fase 1.17). Comitado. |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Departamentos, Sliders, Alertas de Produto. (1) `ShoppingDepartments.jsx` (rota /shopping-departments, Sidebar "Departamentos" LayoutGrid): CRUD via `/shopping/department` (paginator pageIn/pageOut/term busca por nome, create/update/soft delete), colunas Nome/Empresa (populate)/App (showInApp)/Status/Criado em, modal com name/suggesteds (vírgula)/company (select companyService.getCompanies)/flags. (2) `Sliders.jsx` (rota /sliders, Sidebar "Sliders" Image): CRUD via `/slider` (paginator pageIn/pageOut; create exige `file:{url}` + `company` required no model; update coerce status/companyClick a bool e troca images; delete soft via deletedAt), colunas Nome/Empresa/Tipo/Categoria/Prioridade/Status/Cliq. Empresa. (3) `AlertProducts.jsx` (rota /alert-products, Sidebar "Alertas de Produto" BellRing): abas Alertas (/v2/customer-alert-product/alert-product/notification list/create; edição desativa via PUT sem body) e Relatório (/alert-product/report com page/limit). Services novos em api.js: `shoppingDepartmentService`, `sliderService`, `alertProductService`. `vite build` OK (1549 módulos — corrigido ícone `Images` inexistente no lucide-react para `Image`). Validado em runtime: departamento create/update/soft-delete (paginator 200); slider create com company real (6a95eb533a016b0799194766) status/"false"→false + companyClick→true + imagens trocadas + delete (list 0); alertas list 0 e report total 0. Dados de teste removidos via mongosh (2 departments + 1 slider). Observações: collection de empresas do backend moderno é `companies` (CompanyModel), não `company_delivery`; /offer/register e slider generate sem company dão erro legado (a tela exige seleção). Comitado. |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Gorjetas, Sites de Empresa, Horários. (1) `Tips.jsx` (rota /tips, Sidebar "Gorjetas" Coins): abas Gorjetas (/Tip list/create/delete/:id + /Tip/search por status+type; model: value R$, type system/user, status) e Gorjetas Entregador (/TipDeliveryMan list/create/delete/:id; model: value, deliveryMan, orderStatus, tip ref). (2) `Sites.jsx` (rota /company-sites, Sidebar "Sites de Empresa" Globe2): CRUD via `/company/site` (paginator page/limit 0-based skip=page*limit → {list,total}; list/create/update/:id/delete/:id soft; montado com authenticate em routes/index.ts:154), colunas Nome/Empresa (populate)/Domínio/Status/Criado, modal com company (select)/name/domain/URLlogo/email/about/slider/status, paginação Prev/Próximo page 0-based. (3) `Schedules.jsx` (rota /shopping-schedules, Sidebar "Horários" Clock): abas Por Empresa (GET /shopping/schedule/all → [{_id, company:{name}, count, hours:{DOM..SAB:[{id,start,end}]}}] com agrupamento + excluir por id) e Cadastrar (POST /shopping/schedule/:company com empresa select + dayWeek (DOM..SAB) + startHour/endHour numérico HHMM ex.: 800=08:00 + type DELIVERY/WITHDRAWAL/BOTH; update/updateType disponíveis no service). Services novos em api.js: `tipService`, `siteService`, `scheduleService`. `vite build` OK (1552 módulos — corrigido ícone `HandCoins` inexistente no lucide-react para `Coins`; Globe2/Clock existem). Validado em runtime: tip create/search (status true + type user)/delete (list 0); site create/T-Site-2 update (status false)/delete soft (list 0); schedule create FRIDAY 800-1800 DELIVERY + delete soft; observado que `/shopping/schedule/all` não filtrava deletedAt (única lacuna) → adicionado `$match: { deletedAt: { $exists: false } }` no início do aggregate em shopping-schedule.service.ts e admin-api reiniciado; após isso /all 0. Dados de teste removidos via mongosh (site T-Site-2 + schedule soft-deletado da company 6a95eb533a016b0799194766; collection de schedules é `schedule`). Observações: `schedule` é a collection real (não shoppingschedules); collection de empresas do backend moderno é `companies` (CompanyModel), não `company_delivery`. Comitado. |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Mobility: Passageiros, Serviços, Sliders. (1) `MobilityPassengers.jsx` (rota /mobility/passengers, Sidebar "Mobility Passageiros" UserRound): CRUD via `/v1/mobility/passengers` (paginator pageIn/pageOut → {list,total} com person+franchise populados; busca por nome via /filter que retorna records Person-shaped com passenger aninhado — DELETE/PUT resolvem o id via `passenger._id` aninhado), colunas Cliente/Telefone/E-mail/Franquia/Estrelas/Indicação/Status/Criado, modal com select de person (personService.paginator) + franchise (franchiseService.getFranchises) + status; gotcha: `franchise: ''` causa CastError → tela envia `undefined`; `/link-frachise` (typo do backend) é ação geo do app, não usada no admin. (2) `MobilityServices.jsx` (rota /mobility/services, Sidebar "Mobility Serviços" Car): CRUD via `/v1/mobility/services` (paginator pageIn/pageOut + name regex + franchiseId; create força status true e aceita file/maker p/ images/makers), colunas Nome/Tipo (enum bike|motorcycle|car|microbus|bus|truck|package)/Capacidade/Preço Base/Tarifa Mínima/Valor Fixo/Status, modal com type/capacity/franchise (texto)/preços/raio/checkboxes (onlyForWomen, requireConfirmationCode, showArrivalTime, useDynamicsRace). (3) `MobilitySliders.jsx` (rota /mobility/sliders, Sidebar "Mobility Sliders" Image): CRUD via `/v1/mobility/slider` (paginator pageIn/pageOut + target regex; create exige `file:[{url}]` + franchise válida, senão 400 "Informe uma franquia válida"; update só troca image se `file` for array — nunca enviar objeto; status string coerced a bool), colunas Imagem (thumbnail)/Nome/Franquia/Impressões/URL Destino/Target/Status. Services novos em api.js: `passengerService`, `serviceService`, `mobilitySliderService` (nome `serviceService` mantido do agente — não colide). `vite build` OK (1555 módulos). Validado em runtime: paginators de passengers/services/slider 200 com {list,total} 0; 401 sem token; POSTs de validação — service create sem franchise dava 500 (CastError) e foi corrigido no backend (guard `Informe uma franquia válida` em mobility-service.service.create) com admin-api reiniciado → 400 limpo; passenger/slider sem persona/franquia → 400. Coleções de seed vazias (franchises 0, persons 0) — sem dados de teste criados, leftover 0. Comitado. |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Mobility: Monitor, Extrato, Mensagens. (1) `MobilityMonitor.jsx` (rota /mobility/monitoring, Sidebar "Mobility Monitor" Map): `GET /v1/mobility/report/map/monitoring` — **pageIn 1-indexado** (`(from-1)*size`), default pageOut 10, retorna array puro (sem total), campos _id/client/driver/pickup/dropoff/serviceCategory/vehicleType/estimatedPrice/proposedPrice/distance/duration/createdAt/status/paymentMethod/scheduledAt/statusTxt; "Next" habilitado só com página cheia; `GET /v1/mobility/report/active/monitoring` → **stub** `{key,value}` (card informativo). Maps POST (/maps/direction|matrix|geo|complete) mapeados mas NÃO wireados (não-tabulares). (2) `MobilityExtract.jsx` (rota /mobility/extract, Sidebar "Mobility Extrato" Wallet): `GET /v1/mobility/extract/:driver` (query timezone default America/Sao_Paulo) → **objeto chaveado por "YYYY-MM"** de entradas {_id, price, createdAt, year, month, day, monthTxt} (somente bookings completed); seletor de motorista via `driverService.getDrivers()`; flattens em DataTable com cards de resumo. (3) `MobilityMessages.jsx` (rota /mobility/messages, Sidebar "Mobility Mensagens" MessageCircle): `GET /message/conversations` — exige filtro `passenger` XOR `driver` (senão `[]`), só hidrata um lado (driver ou passenger.person); `GET /message?booking=<id>` → docs ChatRace {message,type,booking,sent,receive,driver,passenger,createdAt}; `POST /message` — service valida só message+booking, schema exige sent/receive/driver/passenger (form preenche do contêiner selecionado, driver/passenger unwrapped via _id). Services: `mobilityMonitorService`, `mobilityExtractService`, `mobilityMessageService`. `vite build` OK (1561 módulos; primeiro build falhou porque `mobilityMonitorService` não havia sido inserido — corrigido). Validado em runtime: map/active monitoring 200, extract dummy ObjectId 200 `{}`, message list/conversations 200 `[]` (com e sem filtro), message create {} → 400 `Informe mensagem e booking`, noauth 401. Nenhum dado criado (leftover 0). Comitado. |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Mobility: Tópicos, Documentos, Notificações App (encerra o módulo Mobility no painel). (1) `MobilityTopics.jsx` (rota /mobility/topics, Sidebar "Mobility Tópicos" Radio): `POST /v1/mobility/topic` inscreve token em tópicos (body {token, topics:[{name,value?}], unsubscribeTopic?}; key=name ou name_value; retorna {topics:[keys]}); `POST /topic/send` (body {topic,title,subject,franchise?,priority? default max}; FCM condition `'topic' in topics && 'application_root' in topics` + franchise_<id>); `GET /topic/link-user-topics` sincroniza drivers/passengers com token → {driverErr,passengerErr}. Sem Service Account (FIREBASE_ADMIN_*) tudo degrada 400 "FCM não configurado" — validado em runtime (page trata como alert esperado). (2) `MobilityDocuments.jsx` (rota /mobility/documents, Sidebar "Mobility Documentos" FileText): abas Documentos de Veículo (`/v1/mobility/vehicle-documents`: paginator pageIn/pageOut/searchDriver → {list,total} com $lookup driver; GET /:driver?approved&status; POST exige driver+5 campos de veículo → 400; PUT /:id aceita status/approved boolean; status=true propaga dados no Driver + desativa outros docs do motorista), Formas de Pagamento (`/v1/mobility/type-payment-service` → listActive status:true; readonly; model TypePaymentService: name/type MONEY|CARD|BRASPAG|PAGARME|PIX|ALL/genre/status) e Viagem (`/v1/mobility/booking/travel-info/:booking` → TravelBookingInfo status concluded; 404 se não concluída; times/distancias formatados server-side "45 minutos"/"12.30 KM"). (3) `MobilityNotificationsApp.jsx` (rota /mobility/app-notifications, Sidebar "Mobility Notificações App" Megaphone — SEM conflito, já importado no Sidebar): `/v1/mobility/notifications` listAll/graphic (agregados diários cumulativos reversos)/paginator (type+franchise $lookup)/search/POST (service seta status=true+images=[], mapeia file:[{url}] ou url → images[], remove file/url)/PUT/ DELETE soft (deletedAt). Services: `mobilityTopicService`, `mobilityDocumentService`, `mobilityNotificationAppService`. `vite build` OK (1564 módulos; 1º build falhou: Megaphone duplicado no import do Sidebar — corrigido). Validado em runtime (admin): link_topics/topic_sub/topic_send → 400 FCM não configurado (esperado), vehdocs paginator 200 total=0 + by-driver 200 [] + create {} → 400 "Id do motorista é inválido", type_payment 200 [], travel-info id válido → 404 "Informações de viagem não encontradas", notifications listAll/graphic [] e paginator total=0, noauth 401 em vehicle-documents. Nenhum dado criado (leftover 0). **Módulo Mobility completo no painel React (12 telas: Passageiros, Serviços, Sliders, Push/Fatos, Avaliações, Relatórios, Monitor, Extrato, Mensagens, Tópicos, Documentos, Notificações App).** Comitado. |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Grupos + Métodos de Pagamento Supermercado. (1) `Groups.jsx` (rota /groups, Sidebar "Grupos" LayoutGrid): `/group/paginator` pageIn/pageOut + listPorNome (regex name) + franchise → {list,total} (Group: name*/description*/status default false/franchise ref/images[]/deletedAt soft); `/group/list` → {data,total,page,pages}; POST/PUT/DELETE/:id; página DataTable com modal de criação/edição (status toggle, franchise via `franchiseService.getFranchises()`). (2) `ShoppingPaymentMethods.jsx` (rota /shopping-payment-methods, Sidebar "Met. Pagamento Super" CreditCard): `/shopping/payment-method/:customer` GET (filtros isMain/isDeleted default false/flag) → array; PUT /:id (isMain=true desmarca demais cartões do cliente; edit envía só nameOnCard/flag); DELETE /:id soft (isDeleted:true); **POST /:customer → 501 "depende do gateway (Braspag/PagarMe/Iugu) - Passo 1.9"** — página é list+edit+soft-delete; seletor de cliente via `userService.getUsers()` filtrado role=='customer' (model ref Users — pessoas/customers de outras collections são inválidos); campos exibidos mascarados (cartNumber ****last4, document ***XX, verifierCode/cardToken NUNCA enviados/expostos); update roda com `runValidators:true` (put parcial só com campos conhecidos). Services: `groupService`, `shoppingPaymentMethodService`. `vite build` OK (1566 módulos). Validado em runtime (admin): group paginator/list 200 total=0, create {} → 400 "Informe um nome válido", payment-method list 200 [], PUT/DELETE id dummy → 404 "Método de pagamento não encontrado", POST → 501 (esperado, Passo 1.9), noauth 401. Nenhum dado criado (leftover 0). Sem alteração de backend (sem restart). **Não commitado (aguardando usuário).** |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Config de Empresa + Access Flow. (1) `CompanyConfig.jsx` (rota /company-config, Sidebar "Config. Empresa" Settings): seletor de empresa (`companyService.getCompanies()`), `GET /franchises/config/:company` → `{activateTip}` (400 "Informe uma empresa válida" p/ ObjectId inválido; company→franchise→activateTip, default false) + `GET /franchises/list-all` para exibir nome da franquia (match `_id === company.franchise`); distinção "sem franquia" vs "gorjeta desativada" via presença de `company.franchise` (backend não diferencia). Completa `franchiseService` (getConfig/listAll). (2) `AccessFlow.jsx` (rota /access-flow, Sidebar "Access Flow" Activity): `GET /report/access-flow/list` → [{_id:{year,month,day}, accessInfo:[docs]}], limit 30 desc — card por dia + DataTable com os campos reais do model (device, customer/person/franchise ObjectIds, version, history, createdAt/updatedAt); `GET /report/access-flow/statistic?timeInterval=N` → {count} (N dias, default 0=hoy). Services: `accessFlowService` (+ franchiseService.getConfig/listAll). `vite build` OK (1568 módulos). Validado em runtime (admin): franchise config company real → 200 `{activateTip:false}`; id inválido → 400; list-all → 200 (2 franquias de seed — **seed de franquias existe agora**, activateTip true, soft-deleted); access-flow list [] e statistic 200; noauth 401. Nenhum dado criado (leftover 0). Sem alteração de backend. **Não commitado (aguardando usuário).** |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Mobility CRUDs de configuração (documenttypes, peakhours, supportsubjects). (1) `MobilityDocumentTypes.jsx` (rota /mobility/document-types, Sidebar "Mobility Tipos Doc." FileText): via `mobilityDocumentTypeService` novo — `/v1/mobility/documenttypes` paginator (pageIn/pageOut + name/type/status), search ({name,type}), listAll, `/:id?`, POST (create força status=true; body name/type VEHICLE|DRIVER/status), PUT, DELETE soft (deletedAt). (2) `MobilityPeakHours.jsx` (rota /mobility/peak-hours, Sidebar "Mobility Pico Horário" Clock): REUSA `mobilityService` existente (listPeakHours paginator pageIn/pageOut+franchise/start/end/status, listAll, create força status=true body {franchise,start,end} strings vazio "800"/"1800", update, delete soft). (3) `MobilitySupportSubjects.jsx` (rota /mobility/support-subjects, Sidebar "Mobility Assuntos" Headphones): via `mobilitySupportSubjectService` novo — `/v1/mobility/supportsubjects` paginator (pageIn/pageOut/status→'all' para listar inativos/type/target/subject/franchiseId, lista com $lookup franchise) + listAll/graphic/search/list/CRUD soft; model: franchise req, subject req, status req, type PASSENGER|DRIVER, target CANCEL|SUPPORT. `vite build` OK (1571 módulos). Validado em runtime (admin): documenttypes paginator total=1 (1 seed existente)/listAll [1], create {} → 400 "Informe o nome do tipo de documento"; peakhours listAll []/paginator total=0, create {} → 400 "Id da Franquia é inválido"; supportsubjects paginator (status=all) total=0/graphic [], create {} → 400 "Informe o assunto". Nenhum dado criado (leftover 0). Sem alteração de backend. **Não commitado (aguardando usuário).** |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Estoque, Avaliações (moderação), Despacho. (1) `Stock.jsx` (rota /stock, Sidebar "Estoque" Package): `stockService` — `/stock-items` (company/:companyId com q search, CRUD; model StockItem: name/company/category?/unit enum kg|g|un|L|ml|cx|pct/minimumStock/description?/active — DELETE é soft), `/stock-batches` (company/:companyId, branch/:branchId?stockItemId=, alerts/:companyId → [{stockItem,branch,currentQuantity,minimumStock}], `/:id`, POST/PUT; model StockBatch: stockItem/branch/quantity/initialQuantity/unitCost/batchNumber/supplier?/expiryDate?/entryDate/status active|expired|consumed — **sem DELETE**), `/stock-movements` (summary/:branchId, company/:companyId com type|startDate|endDate, branch/:branchId com type|stockItemId|startDate|endDate, `/:id`, POST; **registro de entrada POST /entry {stockItem,branch,quantity,unitCost,batchNumber,supplier?,expiryDate?,user,reason?,notes?} → {movement,batch} (server auto-cria batch; NÃO chamar /stock-batches depois); saída POST /exit {stockItem,branch,quantity,user,orderId?,reason?,notes?} → [movement] consumo FIFO**; user obrigatório → página lê `localStorage user._id`). Página com seletor de empresa + 3 abas (Itens CRUD, Lotes list+branch filter+alerts low-stock via AlertTriangle, Movimentações com resumo por branch + forms entrada/saída). Branches via stockService.listBranches (`GET /branches/company/:companyId`). (2) `Reviews.jsx` (rota /reviews, Sidebar "Avaliações" Star): `reviewService` — `GET /reviews/company/:companyId?page=&limit=` → **`{success,data:{data,total,page,pages}}`**, service retorna `r.data` (a página lê `res.data`/`res.total`); endpoint filtra type='store' e popula customer name; 1-indexado (page 1). Moderação read-only por empresa (seletor companyService.getCompanies); NOTE: ObjectId inválido → CastError 500 (rota sem guard; a tela só envia ids válidos do select). (3) `Dispatch.jsx` (rota /dispatch, Sidebar "Despacho" Radio): `dispatchService` — `/deliveryMan/queue` (filtros status/order/limit/...), `/queue/status/:status?initial=`, `/queue/:queueId/status` PUT {status} (PUT `/queue/:queueId` NÃO usado — gated em attempt/historicDeliveryMan/deliveryManProcess), `back-to-queue` PUT {order,deliveryMan?}, `/race/list?order=` (400 sem order "Falha ao encontrar a corrida cancelada"), `/race/canceled` POST {deliveryMan,order,date} (400 "Informe o id da ordem que foi cancelada"), `/race-history` POST {deliveryMan,order,statusRace ACCEPTED|REFUSED|CANCELED}, `/online-last-week/:deliveryMan` → {list,totalMedia}, `/offline/:deliveryMan` PUT (sem body), `/delivery-price/:orderId`; abas Fila (DataTable com status filter + update status + back-to-queue), Races (list por order + form cancelado/histórico), Online (busca por entregador + offline). `vite build` OK (1574 módulos). Validado em runtime (admin): branches/stock-items/stock-batches/alerts/stock-movements 200 (0 docs); stock create {} → 400 mongoose (company/name required); stock entry {} → 400 (stockItem/branch/quantity/initialQuantity/unitCost/batchNumber required); reviews company real → 200 {data:[],total:0,page:1,pages:0} (o primeiro 500 foi bug do script de teste do PowerShell interpolando `$cid?page` → companyId vazio); dispatch queue 200 [], race/list sem order → 400, race/canceled {} → 400. Nenhum dado criado (leftover 0). Sem alteração de backend. **Não commitado (aguardando usuário).** |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Banco de Imagens + Notificação Tópico v2. (1) `ImageBank.jsx` (rota /image-bank, Sidebar "Banco de Imagens" Image): CRUD via `/imageBank` (**sem authenticate** no mount); `imageBankService` — paginator por barcode/nome/category (`/imageBank/list/:barcode/:pageIn/:size`, listPorNome, listPorCategory; busca vazia usa `'null'` como literal para trazer tudo); create (`/create` — requer `file:{url}` para extrair `images[]`), register (`/register` — aceita body direto), update (`/update/:id` — se `file` fornecido reextrai images, senão envia images direto), delete (`/delete/:id`). Model ImageBank: barcode/productName/productAccent/keywords?/description?/packing (ObjectId Packing, populate name)/packingAmount/images?/category?/brand/timestamps. Resposta de listagem: `{lista:[], numeroItens}` (NÃO `{list,total}` — `lista` e não `data`; UI renderiza `lista`). Abas Barcode/Nome/Categoria cada com paginação 0-based + form create/edit (barcode, productName, packing picker/ID, brand, packingAmount, keywords comma, category comma, images[]) + excluir. (2) `NotificationTopic.jsx` (rota /notification-topic, Sidebar "Notif. Tópico" Send): `notificationTopicService` — `POST /v2/notification-topic` (payload `{token, topics:[{name,value?}]}`) + `POST /v2/notification-topic/send` (`{topic, title, subject, franchise?, priority?}` → FCM condition). **Sem model nem GET/list** — rota create usa FCM subscribeToTopic (não DB), mantém lista local da sessão. POSTs retornam 400 "FCM não configurado" (esperado) — tratado como alert (estilo MobilityTopics). `vite build` OK (1576). Validado em runtime: imageBank list → 200 (`{lista:[], numeroItens}`); create {} → 400 "Imagens inválidas" (requer file). notification-topic create + send → 400 FCM não configurado (esperado). Leftover 0. Sem alteração backend. **Não commitado (aguardando usuário).** |
| 06/09/2026 | Fase 2.3 Validação em tela cheia + fix picker de franquia. Achado: `GET /franchises` só retorna franquias NÃO soft-deleted → no dev as 2 franquias de seed são soft-deleted (Fase 1), logo todos os selects de franquia das páginas ficavam vazios e impossibilitavam criar peakhours/supportsubjects (que exigem franchise → 400 "Id da Franquia é inválido"). Fix: trocadas as 8 páginas que populam picker de franquia de `franchiseService.getFranchises()` (não-deletadas, `{data,total}`) para `franchiseService.listAll()` (`GET /franchises/list-all` → array incluindo soft-deleted; `extractList` já lida com array): Groups.jsx, MobilityNotifications.jsx (×2), MobilityNotificationsApp.jsx (×2), MobilityPassengers.jsx, MobilityPeakHours.jsx, MobilitySliders.jsx, MobilitySupportSubjects.jsx, Vouchers.jsx. `Franchises.jsx` (gestão) mantém `getFranchises` (correto — não lista deletadas). `vite build` OK (1576). Validação pós-fix em runtime: create peakhours/supportsubjects com franchise seed `6a96e8aca1004c9beb885f2f` → 200 (`{franchise,start,end,status:true}`); `POST /group` → 200 quando com `description` (400 "Informe a descrição" sem ela — modal da tela já envia name+description); `POST /v2/vouchers` → 200 com `name` (model exige name; tela envia). Demais checagens em tela cheia: drivers/passengers/persons vazios = estado real (sem seed), não bug; `GET /deliveryMan/delivery-price/:orderId` → 404 limpo p/ id dummy; `/reviews/order` e `/reviewable` 200. Dados de teste removidos via mongosh (coleções reais: `peakHour`, `supportSubject`, `group`, `voucher`; banco é `ecbr`, auth admin/admin123): leftover 0 em todas. **Não commitado (aguardando usuário).** |
| 06/09/2026 | Fase 2.3 Backend hardening — ObjectId inválido em endpoints das telas novas (500 CastError → 400/404). Mapeado via sweep de `abc123`: a maioria das rotas já tinha guard (dispatch "Informe uma fila/entregador válido"/"Id inválido", imagebank "Id do registro inválido", peakhours/supportsubjects "Id inválido"); as 3 que davam 500 foram corrigidas: (1) `review.service.listByCompany` + `listByOrder` — adicionado `mongoose.isValidObjectId` → 400 "Informe uma empresa válida"/"Informe um pedido válido" (import de mongoose adicionado); (2) `delivery-dispatch.service.deliveryPrice` — guard `isValidId(orderId)` → 400 "Informe um pedido válido" (mantido 404 "Pedido não encontrado" p/ id válido inexistente). `tsc --noEmit` OK, admin-api reiniciado. Validado em runtime: reviews/company/abc123 → 400 "Informe uma empresa válida", reviews/order/abc123 → 400 "Informe um pedido válido", delivery-price/abc123 → 400 "Informe um pedido válido". Observação: stock-items/company, stock-batches/alerts, stock-movements/company já retornavam 400 (mensagem crua do CastError, aceitável — não 500). **Não commitado (aguardando usuário).** |
| 06/09/2026 | Fase 2.3 Tela QR Code do motorista (último item do audit de CRUDs) - `MobilityQrCodes.jsx` (rota /mobility/qr-codes, Sidebar "QR Codes" QrCode — import adicionado ao lucide): `mobileQrCodeService` novo — `GET /v1/mobility/qrcode/generate-driver?driver=<id>` → `{code, qrcode:<dataURL>}` (400 "Informe um motorista válido" p/ id inválido) e `GET /v1/mobility/qrcode/list-driver-code?code=` → `{_id, code, driver:{_id,name,activeRunStatus}, createdAt}` (400 "Informe um código com pelo menos 6 caracteres" p/ <6, "Código informado não existe ou expirado" 1h, "Motorista não localizado"); seletor de motorista via `driverService.getDrivers({limit:100})` + fallback de ObjectId manual quando lista vazia; exibição do dataURL do QR + código copiável + expiração (1h, calculada client-side pois generate não devolve createdAt) + seção Verificar. `vite build` OK (1577 módulos; 1º build sem o import QrCode no Sidebar → corrigido). Validado em runtime (admin): generate com id inválido → 400; com driver real (criado via POST /drivers, model exige name/email/phone) → código 9 chars + dataURL 1026; list-driver-code do código → 200 com driver populado; código curto → 400. Dados de teste removidos via mongosh (col `qrCodeDriver` e driver `qr.teste@toop.com.br`: leftover 0 em ambas). **Não commitado (aguardando usuário).** |
| 08/09/2026 | Emulação de produção local via hosts do Windows + Vite `allowedHosts`. (1) Adicionadas 5 entradas ao `C:\Windows\System32\drivers\etc\hosts` (IDs 127.0.0.1; backup `hosts.bak-goja`): `gojadelivery.app.br`→landpage 4205, `admin.`→painel 4202, `app.`→cliente 4200, `loja.`→loja 4203, `entregador.`→entregador 4204; `ipconfig /flushdns` OK. (2) Todos os apps respondiam 403 via domínio → anti DNS-rebinding do Vite dev server (default só permite localhost/127.0.0.1); adicionado `allowedHosts: ['.gojadelivery.app.br']` nos 5 `vite.config.*` (frontend-react/vite.config.js + web-client, store, deliveryman-app, landpage) e containers reiniciados. (3) Validação: 5 domínios → 200 com título correto; proxy do painel via domínio: `GET http://admin.gojadelivery.app.br:4202/health` → 200 `{"status":"ok"}` (catch-all `/` com bypass SPA); com `Accept: text/html` → index.html do SPA. Notas: hosts só mapeia host→IP → acesso é `http://<dominio>:<porta>`; navegador usa WinINET direto (proxy de sistema desativado) → funciona; socket.io dos apps cliente/entregador segue hardcoded `http://localhost:8100` no ternary `window.location.port === '4200'/'4204'` (porta se mantém com o domínio → continua OK cross-origin via CORS `*`); landpage `APP_URLS` em `src/constants.ts` aponta para `localhost:4200/4203/4204` (links do site — caso queira, mapear p/ os domínios). Ferramentas CLI: env `NO_PROXY` só cobre 127.0.0.1/localhost → usar `--noproxy "*"` no curl ao testar domínios (sem `http_proxy` set, só reporta o no_proxy). **Não commitado (aguardando usuário).** |
| 08/09/2026 | Categorias de App (painel React) - `AppCategories.jsx` (rota /app-categories + Sidebar "Categorias App" com ícone AppWindow): `appCategoryService` novo — `GET/POST /application/category` (paginator 1-indexado `{data,total,page,pages}` com filtros type/status; listCategories em app.service.ts), `GET/PUT/DELETE /application/category/:id` (soft delete via deletedAt). Pagina DataTable: colunas Nome/Tipo (SUPERMERCADO/RESTAURANTE/ACESSÓRIOS)/Segmento/Ordem/No App/Home/Status/Criado; filtros de tipo (`type` enum supermarket|restaurant|accessories) e status (`status=true|false`); modal de criação/edição com name/type/keyword/segment/order/showInApp/showHome/status/images (URLs vírgula). `vite build` OK (1578). Validado em runtime via proxy do domínio: `GET /application/category?page=1&limit=10` com token → `{data:[],total:0,page:1,pages:0}`. **Não commitado (aguardando usuário).** |
| 08/09/2026 | Landpage com Supermercados e Franquias (emulação de produção local). (1) Novas páginas: `SupermarketLandingPage.tsx` (rota /supermercados; hero + como-funciona + recursos supermercado: catálogo por código de barras/ECBR, departamentos online, agendamento de entrega, estoque, pagamento no app; CTA aponta loja 4203) e `FranchiseLandingPage.tsx` (rota /franquias; hero verde `.lp-hero-franchise` + programa/recursos multi-loja: painel único, relatórios por unidade, repasses centralizados; CTA "Entrar" → admin 4202 e "Falar com Vendas" → /contato). (2) `SiteFrame.tsx`: NAV + FOOTER_LINKS + union `active` com Supermercados/Franquias. (3) `LandingPage.tsx`: nova seção "Supermercados e Franquias" (4 features: Supermercado Online, Gestão Multi-Loja, Franquia de Delivery, Repasses da Rede) + chip "🛒 Mercado" no mock do phone. (4) `constants.ts`: APP_URLS agora usa os domínios (app/loja/entregador + novo `admin`); adicionado `vite-env.d.ts` (o `tsc` do build quebrava no import CSS — faltava `/// <reference types="vite/client" />`). `vite build` landpage OK (1649 modules). Validado: /supermercados, /franquias e / → 200 via `gojadelivery.app.br:4205`; admin /app-categories 200. **Não commitado (aguardando usuário).** |
| 08/09/2026 | Fase 2.3 - itens HelpDesk/FAQ (linha 48) e Campanhas (linha 57) fechados/validados. Backend e React já existiam (batches anteriores); revalidado em runtime via admin-api 8100 com token. HelpDesk: `HelpDesk.jsx` (aba Tickets: CRUD + detalhe por protocolo `GET /helpdesk/tickets/protocol/:protocol` com timeline de interações + enviar interação origin company; aba FAQ: CRUD) - create ticket TKT-VAL01 (interação inicial auto origin user criada pelo service), list total=1, protocol interactions=1, create interaction, PUT status=SOLVED, DELETE soft (list 0); faq create/update(status false)/delete (count 0). Campanhas: `Marketing.jsx` (/marketing/campaign, listagem paginada {data,total,page,pages}; create/update(veiculo sms, downloads 20)/delete) e `Cashback.jsx` (abas Campanhas - create `balance=amount` 1000, update status false percent 15 balance 1500, delete soft → list exclui deletado; aba Cashback Utilizado - `/cashback/used/paginator` 200 {list:[],total:0}). Limpeza: mongosh removeu dados de teste (helptickets=1, ticketinteractions=3/2 do fluxo, campaign=0, cashback_campaign=2 incluindo leftover "Cashback Teste 10%" da Fase 1.2 que não havia sido limpo); faqs já em 0. **Não commitado (aguardando usuário).** |
| 08/09/2026 | Landpage - navbar única (fix de consistência). A navbar variava por página porque cada página passava `cta` próprio (`SiteFrame` recebia `cta: NavCta[]` e cada página definia Entrar/Cadastrar/Contato diferentes). Padronizado: removida a prop `cta` do `SiteFrame` (e o `NavCta` interface) e cada página passou a usar `<SiteFrame active="...">` sem botões; a navbar agora usa um `NAV_CTA` fixo (Entrar → app cliente, Cadastrar → registro cliente) em todas as rotas; footer já era única. `vite build` OK (1649). Validado: todas as rotas (home/supermercados/franquias/lojista/motorista/contato) → 200. **Não commitado (aguardando usuário).** |
| 06/09/2026 | Fase 2.3 Telas paralelas (agentes) - Mobility: Push/Fatos, Avaliações, Relatórios. (1) `MobilityNotifications.jsx` (rota /mobility/notifications, Sidebar "Mobility Push / Fatos" Bell): abas Push (`/v1/mobility/push-notification` paginator + create; model PushNotification: franchise/topic/user/title/message/status wait|success|error; create exige `topic` ou `user` → 400; sem PUT → create-only) e Cupom Desconto (`/v1/mobility/discount` paginator/create/update; model VoucherDiscount: franchise/name/price|percent/startDate/endDate/type single|monthly|period/amountAvailable/active; **sem DELETE no backend** — tela omite excluir). (2) `MobilityEvaluations.jsx` (rota /mobility/evaluations, Sidebar "Mobility Avaliações" Star): `/v1/mobility/evaluation` list (passageiro→motorista, pageIn/pageOut) + `/driver/paginator` (motorista→passageiro, filtro driver/passenger) + `/evaluation/:rated` média 6 meses; model Evaluation: typeEvaluator/typeRated/idEvaluator/idRated/stars/description; **corrigido no backend**: rota `/:rated` vinha ANTES de `/driver/paginator` em mobility-evaluation.routes.ts, então `GET /evaluation/driver/paginator` casava `:rated='driver'` → movida a rota estática para antes da dinâmica; admin-api reiniciado; `/driver/paginator` passou a 200. (3) `MobilityReports.jsx` (rota /mobility/reports, Sidebar "Mobility Relatórios" BarChart3): `/v1/mobility/report/adm/driver`+`/balance` (approved_count/cancelled_count/totalRevenue/totalDriverEarnings=80% hardcoded), `/adm/passenger`+`/balance` (approved/cancelled/total), `/adm/races`+`/balance`; paginator pageIn/pageOut 0-based; filtros startDate+endDate (ambos obrigatórios juntos)/status/ids; map-monitoring disponível no service (pageIn 1-based, array cru). Services novos em api.js: `mobilityNotificationService`, `mobilityEvaluationService`, `mobilityReportService`. `vite build` OK (1558 módulos). Validado em runtime: todos os endpoints 200 (eval list + driver/paginator pós-fix, report driver/passenger/races + balances, push/discount paginators); POSTs de validação — push sem topic/user → 400; **discount create {}** dava 500 (CastError) e foi corrigido no backend (guards `Informe um nome válido` + `Informe uma franquia válida` em mobility-discount.service.create) → 400 limpo. Seed vazio (franchises/persons 0) — nenhum teste criado, leftover 0. Comitado. |
| 06/09/2026 | Fase 2.3 Tela Ofertas (painel React) - `offerService` em `src/services/api.js` (listOffers GET /offer/list, createOffer POST /offer/register, updateOffer PUT /offer/update/:id, deleteOffer DELETE /offer/delete/:id). Criada `src/pages/Offers.jsx` (rota /offers + item "Ofertas" no Sidebar com ícone BadgePercent), padrão TabloidTab: DataTable com colunas Nome/Custo (R$)/Descrição/Status/Grupo, botão Novo (modal com nome, custo numérico "10.00", descrição, imagens por URL vírgula, checkboxes Ativo/Grupo de empresa), edição via onEdit (recarrega form e salva com `file` para as imagens - contrato do update), exclusão com confirm. `vite build` OK (1543 módulos). Validado em runtime no admin-api: create via /offer/register com status booleano + images array (2 imagens persistidas, cost 12.5), list count 1, update via /offer/update/:id com status "false"→false + file substituindo imagens + cost 15, delete hard → count 0. Observacao: /offer/register persiste o corpo cru (comportamento legado) - status vazio "" causa CastError 500 nesse endpoint; a tela cria sempre com booleano (checkbox). Comitado. |
| 06/09/2026 | Fase 1.17 Customer + Indication no fluxo Person (backend) - criado model `Customer` (colecao `customer`, mesma do legado: iugu_id/email/ddi +55/phone/person ref obrigatorio/device/instanceIdToken/token/favoriteRestaurants/favoriteSupermarkets/termsNotAccepted default true/sku/appVersion/deletedAt/rating{stars,comment,dateRating}; indices phone/email/person/createdAt/updatedAt/token; removido `required:false` solto no sub-schema rating que quebra o mongoose 8). customer.service/controller/routes montados em routes/index.ts (`/customer`, paths legados; `/search` PUBLICO como no legado, demais com authenticate): paginator (exige page/limit → 400 "Dados da paginação inválidos"; $lookup customer_delivery_address main+isDeleted:false + person com unwind preserveNull, escopo geo $geoWithin opcional via `franchise` query usando location da Franchise moderna; retorna {list,total}); list e list/:id (populate person; id invalido → 400 "Usuário não encontrado"); listPorNome (agrega por nome da person com regex - corrige bug legado que filtrava campo `name` inexistente em customer); create (sku unico gerado 18-char+timestamp; sem mailer nem warnDefinedList de notification - externos desacoplados); update (ddi decode com try/catch, findOneAndUpdate SEM upsert, gera sku quando ausente; sync passenger/topics desacoplado); remove hard delete; search (phone regex/email/person, exige filtro → 400 "Filtro é obrigatório", backfill ddi no customer+person quando ausente, retorna customer populado; login GOOGLE_EMAIL/emissão de JWT desacoplado); search-customer (email ou phone → 400 "Informe um email ou telefone", regex, select person/email/phone/instanceIdToken, limit default 10); search-person-customer (name >=2 chars → 400 "Informe um campo para filtrar"; person status:true + deletedAt ausente, nome/email/telefone via $expr $regexMatch com `$toString` - sem crash do `$toLong` legado em telefones nao numericos; $lookup customer com deliveryAddress main via customer_delivery_address; escopo geo opcional; limit 10). Fechamento do item: `person.service.update` agora **cria a `Indication`** quando `code` fornido (valida referralCode → 400 "Código informado inválido", cria {person: id, personReceive: dono do código, referralCode, total: 20}) usando o model Indication ja migrado (1.27); e `register-duplicates` ganhou os branches `customer`/`phone`|`email` (mesmo $group do legado). Validado em runtime: personA(com referralCode MIGRA1)+personB, customer create (sku gerado, person populada), search publico por email, search-customer, search-person-customer (com customerId), paginator (total 1, address null sem endereco), list, list/:id, listPorNome, update (phone/appVersion/instanceIdToken), personB update com code MIGRA1 → Indication criada (person=B, personReceive=A, total 20) e listada em /v1/mobility/indication?personReceive=A, delete customer + register-duplicates customer (0). `tsc --noEmit` OK, admin-api reiniciado (2x - 1o restart pegou schema invalido de `rating.required:false`, corrigido). Dados de teste removidos (customer=0, indication MIGRA1 removida via mongosh, persons soft-deletadas). Comitado. |
| 08/09/2026 | Testes automatizados - Fase 5.3 concluida (linhas 287-289). (1) Backend (jest/ts-jest, src/__tests__): baseline 4 suites/31 testes com 2 falhas - corrigido mock de chain do mongoose em order.service.test.ts (objeto thenable: populate: jest.fn().mockReturnThis() + then resolvendo o valor). Novas suites marketing.service.test.ts (campaign create/list/update com status/bonus/veiculo), cashback.service.test.ts (create balance=amount, used/paginator com populate, update status/percent/balance, delete soft) e helpdesk.service.test.ts (ticket create com interacao inicial origin user, protocolo, interacoes, faq CRUD) - mocks de models com as any. Resultado: 7 suites, 63 testes passando (antes 31); tsc --noEmit limpo; arquivos de teste ficam no ignore pattern do eslint (0 errors). (2) UI (painel React): Vitest ^3.2.7 + jsdom ^26.1.0 + @testing-library/react ^16.3.3 + @testing-library/jest-dom ^6.9.1 instalados no container de dev (devDeps no host via bind mount), script `test`: vitest run + vitest.config.js (environment jsdom, setupFiles ./src/test-setup.js, include src/__tests__/*.{test,spec}.{js,jsx}) + src/test-setup.js com import @testing-library/jest-dom/vitest. Suites: DataTable.test.jsx (estado vazio, spinner loading, render de colunas, onView/onEdit/onDelete por posicao dos botoes) e AppCategories.test.jsx (vi.mock de ../services/api -> appCategoryService.paginator; titulo + empty message + linhas com within(table) - evitado ambiguity de texto com option do select - + abertura do modal Nova Categoria). 7 testes passando; vite build OK. Atencao JSX: sem casts TS (as) em arquivos .jsx (esbuild). Notas: npm run lint do frontend-react falha pre-existente (nao existe config de ESLint no projeto); lint do backend ja tinha 146 erros pre-existentes (socket.ts etc.). Nao commitado (aguardando usuario). || 08/09/2026 | App Entregador (deliveryman) - correcao de dados + build. (1) Backend: order.service.updateStatus agora seta deliveredAt e credita a carteira do entregador na transicao delivering->delivered (creditDeliverymanWallet: deliveryFee*(1 - feePct/100), feePct de SettingsModel.deliverymanFeePercentage default 2, descricao Entrega {orderNumber} - taxa de entrega; erros capturados); tsc --noEmit OK. (2) EarningsPage: a extração de transacoes lia .transactions (o endpoint GET /wallet/transactions retorna depois do interceptor {data,total,page,pages}) -> corrigido para 	.data || t.transactions || []; rides com fallback arrays. (3) Dashboard: stats agora sao reais de HOJE (filtro deliveredAt/updatedAt com toDateString); adicionados 2 cards - Corridas Hoje (bookings status=completed de hoje) e Saldo (walletService.getBalance); polling 30s. (4) Fix build TS pre-existente (19 erros): AuthContext.deliveryman ampliado (available/isDriver/driverOnline/driverAvailable/totalTrips/address/addressLat/addressLng + index any); interfaces Order com deliveryAddress string|objeto (ActiveDelivery/AvailableOrders); openInMaps guard null + customerPhone tipado; useRef<HTMLInputElement|HTMLDivElement|null>(null) + cast nas refs (React 18.3.31 tipo RefObject<T|null> vs LegacyRef) em DocumentsPage/ProfilePage/ActiveRidePage. npm run build OK (tsc && vite build); container reiniciado; :4204 200. (5) Dados: wallet do Entregador Teste estava zerada apesar de corrida completada BK1788894802707279 (final 418.11, totalTrips 1) - backfill no banco (mongosh) com driverEarning = final - 20% = 334.49 + WalletTransaction status completed ref booking; validado via API com token JWT (auth usa payload _id/email/role): wallet balance 334.49/totalEarnings 334.49, transactions count 1, bookings completed 1 (hoje), orders delivered 2. Nao commitado (aguardando usuario). |
| 09/09/2026 | Restante das migracoes - itens acionaveis: (1) Passo 5.2 CI/CD: nao existia .github/workflows/ (os ci.yml/ci-cd.yml citados nunca foram criados). Criado .github/workflows/ci.yml (GitHub Actions, Node 20, concurrency por ref): job backend (working-directory delivery_toop-admin/backend: npm install + npm run build=tsc + npm test=jest 7 suites/63 testes), job admin-panel (frontend-react: npm install, sem lock commitado + vite build + vitest run 7 testes) e job web-apps em matrix x4 (delivery_toop-web-client/store/deliveryman/landpage: setup-node com cache npm + cache-dependency-path, npm ci - lockfiles confirmados em sincronia via npm ci --dry-run nos 4 containers - + npm run build = tsc && vite build). Para o CI ficar verde, corrigido o build do web-client (4 erros TS pre-existentes): HomePage getCompanyId aceita string | {_id;name?;logo?} (antes Company|Order - order.company era string|objeto); AuthContext User ganhou phone? (ProfilePage lia user.phone); RideRequestPage destructuring removido setter nunca usado (setPickupComplement) mantendo o value pickupComplement usado no payload; RideTrackingPage openChat com guard if (id) antes de messageService.markAsRead (id de useParams string|undefined). web-client build OK (tsc && vite build, chunk>500kB warning apenas). YAML do workflow validado via js-yaml (jest dep) no container admin-api. (2) Passo 5.4 Monitoramento: validado GET /metrics no admin-api:8100 (middleware/metrics.ts - formato Prometheus texto/plain: http_requests_total 7168 com labels status/metodo + http_request_duration_seconds_avg 0.0227s/p95). Documentado como pendente de provisionar Prometheus+Grafana na stack (scrape admin-api:8100/metrics). Itens restantes do MIGRACAO continuam bloqueados por externo (gateways pagamento 1.9/fluxo shopping 1.8/finance subord 1.1, ERP 4.1/4.2, S3 compressao/syncImagem 1.16, OTA nativo 3.2, rotacao JWT/revogar service account/.env producao 5.1). Nao commitado (aguardando usuario). |
| 09/09/2026 | Fases 1.1/1.8/1.9/1.16/2.2/4.2/5.1/5.4 - backend moderno completo (novas rotas implementadas no proprio admin-api). (1) Fase 1.8 fluxo shopping no backend: models `ShoppingCart` (colecao shoppingCart, status open|checked_out|cancelled, ref `'User'`), `ShoppingCartItem` (shoppingCartItem, ref `'User'`+`'ShoppingCart'`), `OrderStatus` (orderStatus, 14 status legados WAIT_COMPANY..CANCELED, refs `'Company'`/`'Deliveryman'`), `OrderTracking` (orderTracking, 2dsphere, ref `'Deliveryman'`) e `PixGenerate` (pixGenerate) - além da correcao de refs erradas (`'Users'`→`'User'` etc.) que causavam MissingSchemaError em ShoppingCart/OrderStatus/OrderTracking; `Order` ganhou campos aditivos opcionais shoppingCart/deliveryMode (own|online)/schedule/tip/pixTxid. Services `shopping-cart.service` (paginator/list/getActive/create+addItem/updateItem/removeItem/recalcTotal/update/softDelete/reorder/checkout -> cria Order + OrderStatus WAIT_COMPANY, `Promise<any>` p/ spread toJSON) e `shopping-order.service` (paginator/getById+statusHistory/costFreight GET+PUT/set-getDeliveryMode/startScheduleOrder). Controllers + rotas montadas em routes/index.ts: /shopping/cart, /shopping/order, /pix (POST /generate-charge + GET /verify/:txid, sem endpoint de verify no microservico - lê PixGenerate local e retorna tip sobre webhook), /braspag (transactions/transaction/capture; receive/notification -> 400 claro). (2) Fases 1.9/2.2 gateway de pagamento no backend (proxy para microservico 8400, rotas reais conferidas em routes/index.routes.ts + pagarme.routes.ts): `payment-gateway.controller` + rotas em payment.routes.ts (repasseSummary/repasseSplits, tokenizeCard com card_tokenize, cardByToken, charge, cancel, cancelPartial, pixCharge, invoice CRUD/receivable, recipient CRUD, splitAfter, transactionInfo, transactionsList) montadas sob /payments; `payment-gateway.service` com métodos Promise<any>; shopping-payment-method.service.create reescrito - tokeniza (payload Braspag), exige cardToken (400), persiste cartao mascarado (ultimos 4) + isMain unico; degrade claro 400/501 substitui o anterior ("Gateway de pagamentos nao configurado: informe PAYMENT_APP_TOKEN e PAYMENT_APP_SECRET..."). (3) Fase 1.16 tools: `compressImage` (sharp ^0.33.4 import dinamico + `declare module` em src/types/sharp.d.ts + fallback degradado) e `syncImage` (axios arraybuffer -> uploads/, 400 sem PRODUCT_IMAGE_API) em tools.controller + POST /tools/compress e /tools/sync-image em tools.routes; config PRODUCT_IMAGE_API agora `z.union([z.string().url(), z.literal('')])`. (4) Fase 4.2: scaffold `delivery_toop-integrations` (Express 8501, zod config, /health, /v1/token 501 claro, /integrations/connectors 400 degrade com credenciais ERP). (5) Fase 5.1: `.env.production.example` atualizado (PAYMENT_URL/NOTIFICATION_URL/DELIVERYMAN_URL ativas + INTEGRATION_URL/PRODUCT_IMAGE_API/LTS/PAYMENT_APP_TOKEN/PAYMENT_APP_SECRET/GRAFANA_USER/GRAFANA_PASSWORD placeholder). (6) Fase 5.4: servicos `prometheus` (9090, retencao 15d) + `grafana` (3000, GRAFANA_USER/PASSWORD, dashboard home toopdelivery-overview.json) adicionados ao docker-compose.dev.yml com volumes prometheus_data/grafana_data (YAML validado via js-yaml, 16 services) - pull de imagens bloqueado inicialmente (erro de credenciais); apos o reboot do Windows (09/09) o pull funcionou e a stack subiu via `docker compose up -d prometheus grafana`: `toop-prometheus-dev` e `toop-grafana-dev` Up; targets do Prometheus - **admin-api:8100/metrics UP** (job admin-api), microservicos legados nao expoem /metrics publico (deliveryman/payment: 401, notification: 404 - targets down, esperado); Grafana 13.2.1 `/api/health` ok em `http://localhost:3000` (login `admin`/GRAFANA_PASSWORD do .env). `tsc --noEmit` do admin-api limpo; validado em runtime: fluxo cart->item->checkout->order com statusHistory=1, degrade gracioso p/ payment-method/pix/braspag/compress/sync-image. CV perdeu o item (1.1 confirma cleave nos deltas do MIGRACAO: repasse resolvido no proprio backend - sem endpoint do legado). Nao commitado (aguardando usuario). |
|| 09/09/2026 | Taxa da plataforma configurável + Saques no painel admin + Ganhos líquidos + Sidebar em menus/submenus. (1) Backend - taxa: Settings ganhou `platformFeePercentage` (default 20); novo `settings.service.ts` com `getPlatformFeePercent()`; `booking.service.complete()` agora persiste `platformFee`/`driverEarning` no Booking e usa taxa dinâmica (removido `platformFeePercent: 20` do CANCEL_FEE_CONFIG); `mobility-report.service.ts` usa `driverRate = (100 - fee)/100` no lugar do 0.8 hardcoded. (2) Saques (admin): `wallet.service.ts` - `enrichWithdrawals` (resolve driver name/email/model via DriverModel+DeliverymanModel, pois o wallet real usa id de deliveryman apesar de `ref: 'Driver'`), `listWithdrawals` (query type=debit, status default pending, populate wallet driver/pixKey/pixType/balance, paginado), `approveWithdrawal` (pending→completed, guard `ObjectId.isValid` → 404 "Saque não encontrado"), `rejectWithdrawal` (pending→failed + estorno saldo/totalWithdrawals + crédito "Estorno de saque rejeitado" via withTransaction); rotas GET /wallet/withdrawals + POST /wallet/withdrawals/:id/approve/reject; tela `Withdrawals.jsx` (filtros pending/completed/failed/all, cards resumo, modal confirmar aprovar/rejeitar) com rota /wallet/withdrawals e item no Sidebar. (3) Entregador (ganhos líquidos): `EarningsPage.tsx` lê settings (pfFeePct default 20), usa `driverEarning` do Booking quando existe senão `(1 - pfFeePct/100)`; `HistoryPage.tsx` com pfFeePct + líquido por corrida/total. (4) Cliente: recibo da corrida usa `booking.platformFee` (fallback 0.20). (5) Sidebar reescrito (com API do usuário: motorista≡entregador, cliente≡pessoa, empresa≡loja): topo Dashboard + "Motoristas & Entregadores" (→ /deliverymen); grupos recolhíveis CSS (.nav-section* em global.css) Negócios/Clientes/Mobilidade/Financeiro/Marketing/Sistema/Loja(recolhido - operação de loja fora da vista do dono); removidos do menu /drivers, /persons, /mobility (hub) e /notification-topic (rotas mantidas no App). (6) Fusão de telas: `Fleet.jsx` (rota /deliverymen) com abas Entregadores (Deliverymen com docReview) / Motoristas (Drivers), usando .om-tabs/.om-tab. Validação: tsc --noEmit limpo (backend/deliveryman/web-client); smoke runtime - saque R$50 aprovado (driver entregador@teste.com.br resolvido como Deliveryman, pix cpf), saque R$40 rejeitado com estorno (saldo 334.49, totalWithdrawals 0); approve/reject id inválido → 404. Build do painel (vite) pendente de validação porque o Docker Desktop estava fora do ar ao final. Nao commitado (aguardando usuario). |
| 09/09/2026 | Pagamento compartilhado do sistema (delivery) + Meus Cartoes no web-client. DECISAO (item 73): supermercado = categoria do delivery (sem sistema paralelo); metodos de pagamento e gateway (/payments/* -> micro 8400) viram camada DO SISTEMA p/ delivery e mobilidade. Implementado: order.service.create com validacao aritmetica (subtotal=sum item.total e total=subtotal+frete-desconto; mismatch 400) e debito server-side na criacao - card (credit_card/debit_card) exige paymentMethodId de cartao salvo do cliente (404 se nao pertence a ele), cobra via pagarmeTransaction com o payload real do legado payloadPayment.js (amount em centavos, card_id=cardToken, card_cvv=verifierCode, customer.documents tipo CPF, billing do endereco, postback /pagar-me/driver/status) -> paymentStatus=paid; pix gera pixCharge({amount}) e persiste pixTxid+pixQrcode (campo aditivo novo) -> paymentStatus=pending; ambos registram PaymentTransaction (charge/pix_charge). Falha ou degrade de gateway -> 400 claro e pedido NAO criado. Order ganhou paymentMethodId/pixQrcode; validator tambem aceita discount/tip. Web-client: PaymentMethodsPage (rota /payment-methods, menu Meus Cartoes) lista/novo (POST /shopping/payment-method/:customer tokeniza e persiste mascarado)/definir principal/remover; shopping-payment-method.service.create aceita valid MM/AA (normaliza p/ gateway e converte p/ Date fim-do-mes no storage); CheckoutPage carrega cartoes salvos p/ credit/debit (principal por padrao), envia paymentMethodId e trata erros do gateway em toast; OrderDetail mostra banner PIX com codigo copiavel. tsc OK, jest 67/67 (5 testes novos de order.service), web-client build OK. Pendente: wire do mesmo pagamento na mobilidade (bookings). Nao commitado (aguardando usuario). |
| 09/09/2026 | PAYMENT_DEV_MODE (sandbox de pagamento) para desenvolvimento. Apos fechar item 73, o salvamento de cartao e a cobranca falhavam de proposito (degrade 400) porque o ambiente nao tem credenciais de gateway (PAYMENT_APP_TOKEN/SECRET ausentes; BRASPAG/PAGARME/IUGU placeholder no .env). Implementado modo sandbox no payment-gateway.service: com PAYMENT_DEV_MODE=true, tokenizeCard devolve token devtok_, pagarmeTransaction devolve id devtrx_ + status paid, pixCharge devolve txid devpix_ + pix_qr_code fake, cancel deletes simulados; nehum request sai para o microservico 8400 (getToken nem chega a ser chamado) e o Order/Cartao/PaymentTransaction fluem 100% locais. Env: adicionado PAYMENT_DEV_MODE no schema zod (default false); docker-compose.dev.yml injeta PAYMENT_DEV_MODE default true (sobrescrevel via .env); .env.production.example documenta = false (prod nunca simula). Testes: novo src/__tests__/payment-gateway.service.test.ts (4 testes sandbox, axios mockado para provar que nao ha rede); suite total 71/71 (8 suites), tsc OK. Validado runtime via API: salvar cartao -> devtok_ persistido mascarado (isMain); pedido credit_card -> paymentStatus=paid + PaymentTransaction charge succeeded devtrx_; pedido pix -> paymentStatus=pending + pixTxid/pixQrcode + PaymentTransaction pix_charge processing devpix_. Nao commitado (aguardando usuario). |
| 09/09/2026 | Categorias por loja: ajuste front. O admin da plataforma tinha uma tela 'Categorias' (colecao categories) que criava categorias GLOBAIS (sem company) e o painel da LOJA (toop-store) ao cadastrar produto pegava /categories/public sem filtro - ou seja, via categorias da plataforma/de outras lojas, e a loja nao conseguia criar as proprias. (1) painel LOJA ganhou menu 'Categorias' + rota /categories (CategoriesPage.tsx + CategoryModal.tsx): CRUD proprio com company=companyId do dono (POST/PUT/DELETE ja suportados pelo backend), DELETE desativa (active=false). (2) ProductModal da loja agora busca /categories?company=companyId (so as da propria loja) em vez de /categories/public, com aviso quando nao ha categoria criada. (3) ADMIN: removida a tela 'Categorias' (rota /categories, menu e paginas Categories.jsx/CategoryModal.jsx) - fica so 'Categorias App' (appcategories, globais do app). Endpoints /categories e /categories/:id seguem no backend p/ uso do admin em produtos. Validado: builds store (tsc+vite) e admin (vite) OK; runtime API - criar/editar/desativar categoria por loja OK, listagem ?company so retorna as da loja. Obs: web-client HomePage ainda usa /categories/public sem company no carrosel da home (categorias de cardapio de todas as lojas) - candidato a limpeza futura. Nao commitado (aguardando usuario). |
| 09/09/2026 | Limpeza do painel admin (telas inuteis). Removidos menu+rota+pagina dos seguintes blocos legados/orfaos: SUPERMERCADO (rotas /supermarket, /shopping-departments, /shopping-payment-methods, /shoppers, /shopping-schedules + paginas Supermarket/ShoppingDepartments/ShoppingPaymentMethods/Shoppers/Schedules), PRODUTOS no admin (/products + Products.jsx + ProductModal.jsx - ProductModal so era usado por Products), COUPONS (/coupons + Coupons.jsx - mantidos /promos e Promos.jsx como unica tela de cupom; backend /coupon inalterado para apps que compram cupom), e rotas orfas: /drivers (rota removida, paginas Drivers.jsx/Deliverymen.jsx MANTIDAS pois sao abas do Fleet em /deliverymen), /persons (Persons.jsx), /notification-topic (NotificationTopic.jsx), /mobility hub (Mobility.jsx - submenu /mobility/* preservado no Sidebar). Verificacao previa por import-grep: nenhuma pagina removida era importada por outra. vite build OK (1567 modules, chunk>500kB warning apenas); container toop-frontend-react-dev reiniciado. Nao commitado (aguardando usuario). |
| 10/09/2026 | Limpeza geral do Sidebar admin (só o funcional; operações de loja fora do admin; identidade unificada). Auditoria prévia: fonte da verdade = apps web em execução (web-client/store/deliveryman; mobile/ React Native é legado fora da stack). Achados: auth cria UserModel para todos (roles customer/deliveryman/store/admin) e Booking.client ref 'User' — Customer/Person/Passenger são modelos legados do mobile; web-client consume /banners/active e /promo/validate (sliders/coupon/offer/cashback/voucher/Tip/search/application-category etc. não consumidos por apps web). REMOVIDOS (rota+menu+pagina): Clientes (Customer legado), Pré-registro (legado), Indicações (Person legado), Passageiros (Person/Passenger legado), Conteúdo & Notificação da Mobilidade (Sliders/Push/App-Notif/Tópicos/Mensagens - mobile/FCM), Financeiro (/finance legado), Cashback, Vouchers, Campanhas (marketing legado), Config. Domínio, E-mails, Gorjetas, e o grupo Loja (Operação) exceto Banners/Promoções e Cupons: Painel, Pedidos, Despacho, Categorias App (não funcional), Sliders (dup de banners), Ofertas, Embalagens, Estoque, Alertas de Produto, Sites de Empresa, Acessórios, Banco de Imagens, Busca, Config. Empresa (14). MANTIDO/renomeado: Usuários Admin -> item top-level "Usuários / Clientes" (UserModel unificado), Motoristas & Entregadores (Fleet com abas Entregadores=Deliveryman e Motoristas=Driver - mesmo profissional via app entregador), Mobilidade funcional (Serviços, Tipos de Doc, Documentos, Pico Horário, Assuntos, Corridas, Monitor, Extrato, Avaliações, QR Codes, Relatórios), Financeiro (Wallet, Saques, Pagamentos, Relatórios), Marketing (Banners/Promoções + Cupons), Sistema (Config, HelpDesk/FAQ, Avaliações, Access Flow, Log, Monitor/Tools, ACL), Negócios (Empresas, Franquias, Grupos). Verificação por import-grep: nenhuma página deletada importada por outra (falha única = __tests__/AppCategories.test.jsx, removido junto). vite build OK (1537 modules, bundle 803kB->525kB), vitest 4/4 (DataTable). Container toop-frontend-react-dev reiniciado. Nao commitado (aguardando usuario). |

## 10/09/2026 - Limpeza painel admin (fase 2) + Categorias do App
### Decisoes
- App cliente unico (delivery + mobilidade); entregador == motorista (mesmo exercicio via app entregador). Fleet (deliverymen com abas Deliverymen/Drivers) e a tela certa; rota orfa /drivers removida (paginas mantidas pois sao tabs do Fleet).
- Identidade unificada: auth cria UserModel para todos (roles admin/manager/operator/deliveryman/customer/store); Booking.client ref User; Customer/Person/Passenger/Driver(paralelo) sao modelos legados do mobile. /users lista o UserModel (Usuarios / Clientes).
- Fonte da verdade = apps web no docker-compose; mobile/ (React Native) e legado, fora da stack.
- Categorias de topo do app (Pizzaria, Restaurantes, Agua e Gas, Supermercados) sao responsabilidade do ADMIN via tela "Categorias do App" (/application/category, AppCategories.jsx). Subcategorias por loja (/categories?company) sao exclusivas do estabelecimento e NAO entram no carrossel do app.

### Removidos (30 paginas + 30 rotas + teste AppCategories)
Pages deletadas: Customers, PreRegister, Indications, MobilityPassengers, MobilitySliders, MobilityNotifications, MobilityNotificationsApp, MobilityTopics, MobilityMessages, Finance, Cashback, Vouchers, Marketing, DomainSettings, Email, Tips, Painel, Orders, Dispatch, AppCategories (RESTAURADA nesta fase), Sliders, Offers, Packings, Stock, AlertProducts, Sites, Accessories, ImageBank, Search, CompanyConfig.
Rotas orfas removidas; App.jsx e Sidebar reescritos (grupos: Dashboard | Motoristas & Entregadores | Usuarios / Clientes | Negocios | Catalogo | Mobilidade | Financeiro | Marketing | Sistema). No admin: Banners + Promos mantidos (web consume banners/active e promo/validate); Cupons fora.

### Correcoes aplicadas nesta fase
1. Backend: NOVA rota publica GET /application/category/public (sem authenticate) em app.routes.ts, metodo listPublic em app.controller.ts/app.service.ts ([status:true, showInApp:true, sem deletedAt], ordenado por order). Demais rotas continuam autenticadas.
2. Admin: restaurado pages/AppCategories.jsx + __tests__/AppCategories.test.jsx via git (estavam deletados na fase 2); rota /app-categories re-adicionada no App.jsx; sidebar grupo "Catalogo > Categorias do App" (icone LayoutGrid).
3. Web-client: HomePage.tsx carrossel do topo agora usa GET /application/category/public (antes /categories/public, que misturava subcategorias de loja); filtro de empresas segue por company.category; mapa categoryIcons ampliado (Pizzaria/Restaurantes/Supermercados/Agua e Gas/Padaria/Farmacia/etc).
4. Web-client: corrigidos 2 erros TS pre-existentes no CheckoutPage.tsx (c implicito any no load de cartoes salvos) que quebravam o build tsc+vite.

### Validacoes
- Builds OK: admin vite (1538 modules, 533 kB), web-client tsc+vite (129 modules), backend npx tsc --noEmit.
- vitest 7/7 (DataTable 4 + AppCategories 3).
- Runtime: GET /application/category/public sem token -> 200 []; POST /application/category (token) -> aparece no publico; DELETE soft -> some. Container reiniciados (admin-api, frontend-react, web-client).
- Nenhum commit feito ainda (conforme combinado).

## 10/09/2026 - Categorias do App: tipos + icone + loja passa a usar categorias do app
### Mudancas
1. Model AppCategory: enum de TYPE ampliado (22 valores: supermarket, market, restaurant, pizza, hamburger, japanese, arabic, meal, acai, bakery, coffee, icecream, candy, drinks, hortifruti, gas_water, pharmacy, convenience, pet, technology, fashion, accessories) + novo campo opcional icon (emoji ou URL de imagem).
2. Backend: GET /application/category/public agora retorna tambem icon (select ampliado). Sem outras mudancas de rota.
3. Admin (Categorias do App): filtro e select de Tipo com todas as novas opcoes (rotulos PT-BR); novo campo "Icone (emoji)" no formulario; coluna "Icone" na tabela; payload envia icon.
4. Loja (painel store):
   - SettingsPage: select "Categoria" (aba dados da loja) passa a carregar GET /application/category/public (antes usava /categories/public = subcategorias da propria loja, que misturava tudo). Valor salvo = nome da categoria do app (company.category string).
   - RegisterPage: campo Categoria de texto livre virou select carregado de /application/category/public (evita categoria inconsistentes no cadastro).
5. Web-client HomePage: chip do carrossel agora renderiza icon da categoria (emoji) OU cat.images[0] como <img> se for URL (fallback categoryIcons[nome] e depois ??).

### Notas
- Foi encontrado seed existente de categorias do app ativas: Hamburgueria, Restaurante, Supermercados (aparecem no /public e foram mantidas; devem ser revisadas/expandidas pelo admin).
- Subcategorias da loja (/categories?company e CategoriesPage/CategoryModal) permanecem exclusivas por estabelecimento e NAO entram no carrossel do app.

### Validacoes
- Builds OK: backend npx tsc --noEmit, admin vite (1538 modules), web-client tsc+vite, store tsc+vite.
- vitest 7/7 (DataTable 4 + AppCategories 3).
- Runtime (token admin): criar categoria type=hortifruti + icon emoji -> aparece no /public com icon; POST/PUT type=technology aceitos (enum ampliado); PUT icon=URL persistido; soft-delete remove do /public. Containers reiniciados (admin-api, frontend-react, web-client, store).
- Nenhum commit feito ainda (conforme combinado).

## 10/09/2026 - Banner: altura maior e sem titulo
### Mudancas
1. Web-client HomePage: carrossel do topo agora exibe somente a imagem (removido titulo/subtitulo sobreposto).
2. Web-client index.css: .banner-slide com height 240px; blocos CSS mortos de banner removidos.

### Validacoes
- Build web-client tsc+vite OK; container toop-web-client-dev reiniciado.
- Nenhum commit feito ainda (conforme combinado).

## 10/09/2026 - Acompanhamentos (adicionais por produto)
### Mudancas
1. Backend: novo modelo Addon (escopo por empresa): `company, name, price, active, deletedAt`. Service `addon.service.ts`, controller `addon.controller.ts`, rotas `addon.routes.ts` montadas em `routes/index.ts` sob `/addons` (list por `?company=`, POST, PUT, DELETE soft).
2. Backend: Product ganhou `addons: [ObjectId ref 'Addon']`; `product.service` popula `addons` (list, listByCompany, getById) via `populate({ path: 'addons', match: { deletedAt: null }, select })`.
3. Backend: Cart.items agora aceita `addons: Array<{ addonId, name, price }>` (snapshot). `cart.service.addItem(customerId, companyId, productId, quantity, notes?, addons?: string[])`: valida que cada addon pertence ao produto (populate) e esta ativo; `basePrice = promoPrice||price`; `total = (basePrice + somaAddons) * qty`; dedup por `productId|notes|addonKey` (ids ordenados). `updateItemQuantity` recalcula total com addons.
4. Backend: Order.items ganhou `addons` (mesmo snapshot); `order.service.create` aceita e persist; subtotal continua validado server-side pela soma dos `item.total`.
5. Store (painel loja): nova pagina Acompanhamentos (`AddonsPage.tsx` + `AddonModal.tsx`), rota `/addons` e item de menu "Acompanhamentos" (icone Pizza) no Layout. ProductModal permite marcar quais acompanhamentos ficam vinculados ao produto (checkboxes carregados dos ativos da loja, valor por item) e envia `addons: [ids]` no payload de criar/editar.
6. Web-client: CartContext suporta `addons` no item (local e servidor — preco unitario local inclui a soma dos adicionais; dedup local por `productId|notes|addonIds`; body do servidor manda `addons: [ids]`). ProductModal do cliente exibe checkbox de acompanhamentos com preco, recalcula o total e o onConfirm retorna `(quantity, notes, addons)`.
7. Web-client/store: CartPage, CheckoutPage, OrderDetailPage (cliente) e OrdersPage/PainelPage (loja) exibem os acompanhamentos de cada item e usam `item.total` no fechamento.

8. Backend (hardening): `product.service` valida no create/update que os `addons` vinculados pertencem à MESMA empresa do produto (retorno 400 "Acompanhamento inválido ou de outra loja") — defesa contra requisição adulterada.

### Notas
- Acompanhamentos sao **EXCLUSIVOS por loja** (escopo `company`): a loja so lista/cria/vincula os dela (AddonsPage, ProductModal com `?company=` da loja logada); o cliente ve apenas `product.addons` do produto da loja; o carrinho revalida o documento com `company` + `active`. Uma loja de acai so ve leite em po/morangos e a hamburgueria so ve borda de catupiry/cheddar.
- Legado `/food/product-complement` (accessories) permanece sem uso.
- Addons sao globais por empresa; o vinculo por produto decide o que aparece no cardapio ao cliente.

### Validacoes
- Builds OK: backend npx tsc --noEmit, web-client tsc+vite, store tsc+vite.
- Runtime (token admin, empresa 6aa06d35b69d5e179d6f19e3): criar addons "Borda de Catupiry" (+12) e "Cheddar Extra" (+8); vincular ao produto "Duplo" (PUT /products/:id addons) -> GET /products?company retorna addons populados; POST /cart/:company/items qty=2 com addons -> item total (17+12+8)*2=74 e subtotal 74; re-post dedup: mesmo item passa a qty 3 / total 111; addon de outro produto -> 400 "Acompanhamento invalido para este produto"; POST /orders com items{addons} -> pedido criado e itens com snapshot de addons (pedido de teste removido). Containers reiniciados (admin-api, store, web-client).
- Nenhum commit feito ainda (conforme combinado).

## 10/09/2026 - Admin Empresas: editar/excluir corrigidos + vincular usuário admin à empresa
### Mudancas
1. Backend (bugfix editar): validators de Company (`createCompanySchema`/`updateCompanySchema`) agora aceitam `address` e `status` — antes o zod descartava e endereço/status não eram persistidos ao editar/criar pelo painel.
2. Backend: CompanyService normaliza `status` -> `active` no create/update (schema usa `active`); list/getById populam `owner` (name/email).
3. Backend (novo): endpoints de admin da empresa em `/companies/:id/admins` (GET listar, POST criar usuário admin, DELETE /:userId remover). `addAdmin` cria User com role `store`, `company` = empresa (senha bcrypt) e seta `company.owner` se vazio; `removeAdmin` desativa o usuário, desvincula da empresa e repassa o owner para outro admin ativo se havia.
4. Admin painel: Companies.jsx ganhou coluna "Admin" (nome/email do owner) e corrigida a coluna Status para ler `active`; CompanyModal corrige o checkbox "Status Ativo" (antes lia `status` inexistente) e ganhou a seção "Usuário Admin" (lista admins do vínculo, formulário nome/email/senha p/ adicionar e botão para remover).
5. Backend (bugfix excluir): Company ganhou `deletedAt` (soft-delete); DELETE /companies/:id agora seta `active:false` + `deletedAt` e a listagem do admin filtra `deletedAt: {$exists:false}`, então a empresa excluída some da lista (antes o botão "excluir" deixava a linha visível).

### Validacoes
- Builds OK: backend npx tsc --noEmit, admin vite (1538 modules).
- vitest 7/7.
- Runtime (token admin): criar empresa com address+status=false persistiu; PUT com address+status=true persistiu; POST /companies/:id/admins criou user role=store vinculado e setou owner; GET lista o admin; login do admin da loja retorna token role=store; DELETE desativa e bloqueia login ("Conta desativada"); DELETE da empresa (soft) OK. Containers reiniciados (admin-api, frontend-react).
- Runtime (exclusao): criar empresa -> aparece na lista; DELETE -> active=false + deletedAt e some da listagem (total 6 -> 5). Container admin-api reiniciado.
- Nenhum commit feito ainda (conforme combinado).

## 10/09/2026 - Admin: Wallet com motoristas + relatorios agrupados em submenu
### Mudancas
1. Admin Wallet (/wallet): a pagina listava somente a colecao Driver (mobilidade), que estava vazia, e mostrava "Nenhum motorista encontrado". A carteira na verdade ja e unificada: o WalletController resolve o id pelo usuario (driver OU deliveryman) e o Wallet doc e chaveado por esse id. Agora o Wallet.jsx carrega em paralelo `/drivers` + `/deliverymen` e une as duas listas, com badge "motorista"/"entregador" em cada item; saldo/transacoes/credito/debito continuam funcionando para ambos (mesma rota, id passado como driverId).
2. Admin menu lateral (Sidebar.jsx): relatorios espalhados foram agrupados num menu proprio "Relatorios" (icone BarChart3) com submenus: Financeiros (/reports), Mobilidade (/mobility/reports), Extrato do Motorista (/mobility/extract). Removidas as entradas duplicadas de /reports (Financeiro), /mobility/reports e /mobility/extract (Mobilidade).

### Validacoes
- Build admin vite OK (1538 modules). Container toop-frontend-react-dev reiniciado: o vite em dev com bind mount WSL nao propaga eventos de file-watch, entao sem o restart ele seguia servindo o modulo antigo (motivo do "nenhum motorista" persistir na pagina).
- Runtime: /drivers vazio (0 docs) x /deliverymen 1 doc; pagina /wallet passa a exibir o entregador cadastrado com saldo 0 (wallet criado sob demanda).
- Nenhum commit feito (conforme combinado).

## 10/09/2026 - Admin Wallet residuo: saques com filtros e lista de motoristas
### Mudancas
1. backend wallet.service.listWithdrawals: quando o filtro era "all" o codigo caia no else e forcava status=pending, entao o botao "Todos" da pagina nunca mostrava aprovados/rejeitados. Removido o else: "all" agora nao aplica filtro de status (continua filtrando type=debit).
2. Admin Wallet (/wallet) ja carrega drivers + deliverymen (sessao anterior) - confirmado servindo apos restart do container (vite em bind mount WSL nao detecta mudanca sem restart).

### Validacoes
- Backend npx tsc --noEmit OK; container toop-admin-api-dev reiniciado.
- Runtime: GET /wallet/withdrawals?status=all total=2 (R$1 e R$334, Entregador Teste, pix 123456/random); pending=0; completed=2; failed=0. Campos populados (driver.nome, wallet.pixKey/pixType).
- Nenhum commit feito (conforme combinado).

## 10/09/2026 - Admin /users: pagina de usuarios/clientes corrigida
### Mudancas
1. Backend User ganhou `deletedAt` (soft-delete). user.service.delete seta `active:false` + `deletedAt`; user.service.list filtra `deletedAt: {$exists:false}` - excluir agora remove da listagem.
2. Frontend Users.jsx: coluna Status passou a ler `active` (antes lia `status` inexistente e mostrava todo mundo "Inativo"); getUsers com limit=100 (antes so trazia 10).
3. Frontend UserModal.jsx: formulario e payload usam `active` em vez de `status` (o checkbox de status funcionava e o backend ignorava).

### Validacoes
- Backend npx tsc --noEmit OK; builds admin vite OK; containers admin-api e frontend-react reiniciados (vite WSL precisa de restart).
- Runtime: GET /users?limit=100 total=16; POST /users cria role=operator active; PUT /:id active=false aparece como Inativo; DELETE seta deletedAt e some da lista (total volta de 17 para 16). Usuario de teste 6aa2f385e3c7855569a60017 criado e excluido (soft).
- Nenhum commit feito (conforme combinado).

## 10/09/2026 - Relatorios mobilidade (driver name) + Extrato de mobilidade
### Mudancas
1. backend mobility-report.service.ts: `$lookup` para nome do motorista agora busca em `drivers` E `deliverymen` (antes so buscava em `driver` - colecao inexistente; a real e `drivers`). Tambem corrigido para `admDriverReport`, `admRacesReport` e `mapMonitoring`.
2. backend mobility-report-extra.routes.ts: rota `/extract` (mobility-extract.routes.ts) nunca foi montada no index.ts; agora esta em `router.use('/extract', authenticate, extractRoutes)`.
3. Frontend MobilityExtract.jsx: unifica drivers + deliverymen (como no /wallet), para que entregadores com corridas aparecam na lista lateral.

### Validacoes
- Backend tsc --noEmit OK; admin-api reiniciado (stop/start) e frontend-react-dev reiniciado.
- Runtime: GET /v1/mobility/report/adm/driver total=2; primeira linha driver=Entregador Teste status=completed final=418.11 - nome aparece corretamente.
- Runtime extract: GET /v1/mobility/extract/6a95eb5d3a016b0799194771 success=true data.2026-09 items=1 price=418.11 - extrato do entregador carrega com os bookings.
- Nota: client nas corridas continua vazio (booking.client aponta para user que nao existe no banco - dado legado).
- Nenhum commit feito (conforme combinado).
