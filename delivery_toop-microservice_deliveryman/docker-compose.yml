version: '3'

services:
  find_delivery:
    container_name: "find_delivery"
    image: node:10
    env_file: ./.env
    volumes:
      - .:/usr/src/
    working_dir: /usr/src/
    expose:
      - "8500"
    ports:
      - 8500:8500
    command: bash -c 'yarn && yarn start'
    