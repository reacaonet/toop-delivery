{
  "name": "goja-delivery-cliente",
  "version": "1.0.0",
  "private": true,
  "description": "GoJá Delivery - App do Cliente (WebView apontando para https://app.gojadelivery.com.br)",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "build:android": "cap build android",
    "build:ios": "cap build ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.0.0"
  }
}