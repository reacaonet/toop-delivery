import {StyleSheet} from 'react-native';

const markerSize = 30;

const styles = StyleSheet.create({
  container: {
    width: markerSize,
    height: markerSize,
  },
  markerImage: {
    flex: 1,
    width: undefined,
    height: undefined,
  },
});

export default styles;
